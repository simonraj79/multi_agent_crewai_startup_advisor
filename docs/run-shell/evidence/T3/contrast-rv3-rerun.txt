$ node scripts/contrast-audit.mjs            # from frontend/, plain reporter
$ node scripts/contrast-audit.mjs --markdown # from frontend/, the committed artifact
# RV3 SECOND PASS, branch run-shell/cast at 16f3be5, 2026-09-05T16:00:38+08:00

RESULT
  plain run    exit 1
  --markdown   exit 1
  330 pairings, 328 in scope, 1 failing.

  The --markdown output is BYTE-IDENTICAL to the committed
  docs/run-shell/evidence/T3/contrast.md:
    md5  2f6f1c9fc2f46dfbc826b18ab9fd2eb1   docs/run-shell/evidence/T3/contrast.md
    md5  2f6f1c9fc2f46dfbc826b18ab9fd2eb1   (this re-run)
  So the committed file is what the script writes today. Nothing was copied.

WHAT CHANGED SINCE THE FIRST PASS, and it is the important half

  The first pass reported the script exiting 0 over 300 pairings while 17 of
  them missed AA, because the exit code counted W5's 80 rows only. W5 has
  removed that carve-out: the header of contrast.md now says in its own
  words that the owner column is "not an exemption: the exit code counts
  every row", and the script proves it by exiting 1 on the one row left.
  Sixteen of the seventeen were fixed; the catalogue also grew from 300
  pairings to 330. That is a real repair and the gate is now a real gate.

WHY T3.3 IS STILL RECORDED AS FAIL

  DEFINITION-OF-DONE T3.3: "every text/background pairing used by the run
  shell measures >= 4.5:1 (normal text) or >= 3:1 (large text, UI
  components), in both themes". One does not, the script says so, and the
  script's own exit code is now 1. There is nothing to interpret.

THE ONE, verbatim from the plain run
===========================================================================
FAILURES - every one of them, grouped by the file it lives in
==============================================================================================================
  --- W4 ---
  light  1.29 < 4.5  a rail kicker  [--accent-cyan on rail]  ChatRail.vue:383 / DialogueRail.vue:467 .section-kicker  -> var(--on-accent-cyan)

==============================================================================================================
OUT OF SCOPE - measured, printed, never counted (the builder’s own canvas)
==============================================================================================================
  dark   5.02 >= 4.5  quiet card text, THE BUILDER  node-card.css .node-eyebrow (design canvas)  -> var(--text-meta) in node-card.css, plus a regeneration of the 8 light builder baselines - SHELL-SCOPE.md 6.6
  light  4.31 < 4.5  quiet card text, THE BUILDER  node-card.css .node-eyebrow (design canvas)  -> var(--text-meta) in node-card.css, plus a regeneration of the 8 light builder baselines - SHELL-SCOPE.md 6.6

  330 pairings, 328 in scope, 1 failing.
