T2.5 - the character's state is driven by run events only, never by a clock.

REGENERATED BY RV3, fifth pass, branch run-shell/cast at c2966e7, 2026-09-05T19:10:32+08:00.
Round six added 24 lines to useRunChoreography.ts, which is the file all three
hits live in, so this is the one that had to be re-run rather than cited.

===========================================================================
1. THE CRITERION'S OWN GREP
===========================================================================

$ grep -rnE 'setTimeout|setInterval|Date\.now' \
      frontend/src/characters frontend/src/components/AgentCharacter.vue \
      frontend/src/composables/useRunChoreography.ts

frontend/src/composables/useRunChoreography.ts:254:  const now = options.now ?? (() => Date.now())
frontend/src/composables/useRunChoreography.ts:285:  const receiptTimers = new Map<string, ReturnType<typeof setTimeout>>()
frontend/src/composables/useRunChoreography.ts:821:      setTimeout(() => {

THREE hits, all in one file - the same three every pass has found, moved
down by the lines round six inserted above them.

===========================================================================
2. THE HALF THAT MATTERS - the character system itself, with the two other
   ways a clock reaches code added to the pattern
===========================================================================

$ grep -rnE 'setTimeout|setInterval|Date\.now|performance\.now|requestAnimationFrame' \
      frontend/src/characters/ frontend/src/components/AgentCharacter.vue

(no output; exit status 1)

ZERO, for the fifth pass running. frontend/src/characters/pip.ts and
AgentCharacter.vue cannot read a clock by any of the five routes.

===========================================================================
3. EACH OF THE THREE HITS, AND WHY IT IS NOT STATE
===========================================================================

  254:  const now = options.now ?? (() => Date.now())
  285:  const receiptTimers = new Map<string, ReturnType<typeof setTimeout>>()
  821:      setTimeout(() => {

  The first is an INJECTABLE clock with a real-clock default, used to
  timestamp a handoff so a token can be expired. Every test passes its own.
  It decides no character state.

  The other two are the one-shot arrival receipt: a node's medallion carries
  'is-receiving' for RECEIPT_MS so a CSS pulse can play when a token lands.
  REVEAL PACING, which the criterion allows in so many words. It sets a
  decoration class, not data-state.

The five states are derived from frame kind and stage alone;
frontend/tests/characterStates.spec.ts asserts each of them from fixture
frames with no timers involved, and it is green in this pass's
npx vitest run (93 files / 1987 tests).
