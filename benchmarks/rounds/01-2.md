# Plan 01 — judge round 2

Written 2026-09-03 against `gauntlet/plans` = `56bb8ab`, in
`D:\MultiAgentSystem-wt\integration`. Plan 01's round-2 build ended at
`a952c74`; plan 15's build sits above it and changed the builder store,
the builder API, persistence and `config.py`, which plan 01's isolation
matrix also exercises, so the tree scored is HEAD rather than `a952c74`,
and a regression plan 15 introduced on a plan-01 surface would land here.
Method: `benchmarks/README.md`. Applicable rubric rows: **14** (isolation)
and **16** (RAMP integrity). Neither is visual; both score against test
output the critic re-runs itself. Round 1 is `01-1.md`; the four rows it
opened are `D-01-1` … `D-01-4` in `benchmarks/DEFECTS.md`, and every one was
still `open` when this round started.

`PLANS.md` was set to *In judge (round 2)* for plans 01 and 15 in
`d326b00` before any capture was taken or any critic invoked. (The row
verifiers' verbatim output below names that commit as `44f3582`: its
hash before a rebase added the repository's commit trailer after the
round; the tree is byte-identical.)

## The flip

Flipped with `secrets.randbits(1)` at **2026-09-03T07:43:46Z**, before any
capture was shown to any critic and before the critic existed:

| label | is |
| --- | --- |
| **A** | the reference — Flowise 3.1.4 (`flowiseai/flowise:3.1.4`, Docker; round 1's captures, reused) |
| **B** | ours — `gauntlet/plans` at `56bb8ab` |

The critic was never told which is which. Capture files were renamed to
`A-*` / `B-*` and handed over from a directory that names neither product.
(Round 1's flip for this plan came out the same way.)

## Persona

Round 2: **staff frontend engineer**. The prompt opened with the README's
sentence with that persona in place of the design director: *You are a
hostile staff frontend engineer who has shipped a competitor to this
product. You are looking for reasons to reject. Vague praise is a failure
of your job.*

One critic, as in round 1: plan 01 has no visual row, so there is no blind
visual critic, and the one engineering critic re-runs every command and
is handed the captures as context. It saw neither round 1's output, the
ledger, the plan's Status or Design sections, any commit message, nor this
file.

**A second agent, new this round: the row verifier.** It received the
opposite of what the critic saw — the plan's four ledger rows verbatim and
the fixing commit per id — and re-ran each row's own probe against a fresh
backend, reporting with output whether the defect is absent or still
there. Its output is below the critic's, verbatim. A row closes only on
the verifier's *absent* AND the critic not landing it again AND the
dimension scoring at or above the reference; the closing decisions are the
Integrator's and are in the Result.

## What the critic received, and nothing else

1. `.agent/plans/01-auth-and-workspaces.md` — the *Acceptance criteria*
   section only (13 numbered items, one now carrying a dated amendment
   under the original sentence), copied verbatim to a file.
2. Our captures (**B**), 1440×900, dark, **seven**: round 1's six by the
   same filenames — the gallery with the account chip, the credential
   picker closed, the picker's inline "add a key" form filled, the picker
   with a key chosen, Bob's deep link to Alice's document, and the sign-in
   wall — plus the one surface a round-2 commit changed: Bob's run console
   pointed at Alice's workflow, which in round 1 only the critic's own
   capture showed.
3. The reference captures (**A**), 1440×900, dark — round 1's five,
   reused unchanged: Flowise 3.1.4's credentials page empty, the "Add New
   Credential" type picker, the OpenRouter credential dialog empty and
   filled, and the credentials page with one row (`docs/flowise-notes.md`
   §4).
4. Rubric rows 14 and 16, copied verbatim, with the gate and the defect
   rule, and the commands the rows' evidence comes from: the Python suite
   under `PYTHONPATH=<worktree>/src`, `npm test`, `vue-tsc -b --force` and
   `npm run build`, `e2e/isolation.spec.ts` against the free backend on
   :8099, the suite listing, and `tests/pg/test_two_writers.py` under
   `TEST_DATABASE_URL`.

## Where the method could not be followed, and why

- **1440×900 dark only.** No 390×844 project and no light theme exist yet;
  both belong to plan 02.
- **No `capture.spec.ts` exists** (planned in 02). Ours was captured by a
  throwaway Playwright spec (`frontend/e2e/_round2_judge_capture.spec.ts`,
  never committed, deleted after the run) driving Alice and Bob through
  the `X-Synthetic-User` cookie against `SYNTHETIC=1
  SYNTHETIC_BRANCH_DELAY_SECONDS=5 PORT=8099` with `CREDENTIALS_MASTER_KEY`
  set and `PYTHONPATH` pointing at the worktree. The captures are under
  `benchmarks/ours/01/2/` and are gitignored.
- **The sign-in wall was rendered with an intercepted session probe**, as
  in round 1: the E2E stub auth origin answers every context as signed in,
  so one fresh context had `/api/auth/get-session` answer `null` — the
  response a real Better Auth origin gives a visitor with no cookie — and
  nothing else was intercepted.
- **Bob's console was pointed at Alice's workflow by a hand-written
  handoff record** in `sessionStorage`, the shape the builder's publish
  dialog writes and the only route a stranger has to that screen; the
  isolation E2E does the same.
- **Plan 01 has no visual rubric row.** The A/B blind is therefore over
  captures the critic is not asked to score; it is kept because the README
  says the critic gets four things, and the reference's credential dialog
  is the surface plan 01's picker replaces.
- **The reference was not re-run.** Flowise 3.1.4 has not changed since
  round 1 and no plan-01 surface that changed this round has a Flowise
  counterpart, so its captures were reused.
- **The engineering rows have no reference product to score.** `ref` for
  rows 14 and 16 is the bar the criteria set, 10 by definition, and every
  point of `ours` below it must be carried by a located defect.
- **The critic re-runs the suites in our worktree**, so for the
  engineering rows it necessarily knows which tree is ours; only the
  captures are blind.
- **The backend was proved to serve the worktree, not the main tree**,
  before the capture pass and again before the critic:
  `POST /api/builder/workflows/ug_00000000/unpublish` on :8099 answered
  `{"detail":"document not found"} [404]`, a route that exists only on
  this branch. The critic ran its own probe and recorded the answer.
- **The plan's Status does not list its round-2 commits per id**, unlike
  plan 15's; the row verifier was handed the four commits whose subjects
  name a `D-01-n` id on `gauntlet/plans` between `8ee7b9e` and `a952c74`
  (`0fe472f`, `413c4dd`, `dbe803d` then `a952c74`, `fd7c74f`), which is the
  only place the mapping is written down.
- **The engineering critic and the row verifier ran one at a time** on
  the shared in-memory backend, after plan 15's, because the isolation E2E
  deletes every row its two users own; the backend was restarted fresh
  before each of them.
- **Engineering defects carry a command and an output line** in place of
  the README's viewport and zoom.

## Critic output, verbatim

### The critic — engineering, rows 14 and 16 (A = reference, B = ours; the critic was not told)

## Persona
staff frontend engineer, round 2 (engineering, rows 14, 16)

## Commands run and their result lines

All from `D:\MultiAgentSystem-wt\integration` with `PYTHONPATH="$PWD/src"` and the main tree's venv python unless noted. Backend: the already-running `SYNTHETIC=1` service on `127.0.0.1:8099`, never restarted; port 8000 never touched.

- Proof the backend serves this worktree: `curl -s -w ' [%{http_code}]' http://127.0.0.1:8099/api/builder/credentials` → `{"detail":"sign in to use this endpoint"} [401]`; `.../api/builder/workflows/ug_00000000/versions` → `{"detail":"document not found"} [404]`; `/readyz` → `"storage":{"status":"ok","backend":"sqlite"}` `[200]`.
- Python suite `python -m unittest discover -s tests -t .` → `Ran 1642 tests in 77.663s` / `OK (skipped=6)` (exit 0; no `Errno 22` / `crewai:` lock artefact, so one run only).
- Frontend unit `cd frontend && npm test` → `Test Files  62 passed (62)` / `Tests  1157 passed (1157)` (exit 0).
- `npx vue-tsc -b --force` → no output, exit 0. `npm run build` → `> vue-tsc -b && vite build && tsc -p tsconfig.server.json` … `✓ built in 689ms`, exit 0.
- Isolation E2E `npx playwright test e2e/isolation.spec.ts --reporter=line` → `Running 4 tests using 1 worker` … `4 passed (9.9s)` (the four titles: Alice authors/stores/publishes; Bob's gallery and picker list nothing of hers; deep link lands Bob on the empty builder; Bob's console cannot launch `@launch`).
- `npx playwright test --list` → `Total: 34 tests in 5 files`; `--list --grep @launch` → `Total: 8 tests in 3 files`.
- Two writers on PostgreSQL 18: `TEST_DATABASE_URL="postgresql+psycopg://postgres:test@127.0.0.1:5433/postgres" python -m unittest tests.pg.test_two_writers -v` → 5 × `ok` (`test_document_save_one_cas_the_other_a_version_conflict`, `test_gate_reply_one_cas_the_other_a_conflict`, `test_orphan_sweep_one_claim_the_other_finds_the_row_already_terminal`, `test_pending_feedback_one_insert_the_other_handled_as_already_pending`, `test_reopen_gate_one_cas_the_other_a_no_op`) / `Ran 5 tests in 27.074s` / `OK`.
- Each module a criterion names, run alone with `-m unittest discover -s <dir> -t . -p "<module>.py"`:
  - C1 `tests/service/test_workflow_ownership.py` → `Ran 23 tests in 3.980s` / `OK`
  - C2 `tests/service/test_credentials.py` → `Ran 19 tests in 1.377s` / `OK`
  - C3 `tests/service/test_credential_crypto.py` → `Ran 24 tests in 0.093s` / `OK`
  - C4 `tests/service/test_boot_checks.py` → `Ran 8 tests in 0.316s` / `OK`
  - C5 `tests/builder/test_credential_resolution.py` → `Ran 15 tests in 1.388s` / `OK` (stderr carries CrewAI's `Error executing listener n1_s1: credential cr_… is not one of yours` lines, which are the refusals under test)
  - C6 `tests/service/test_secret_redaction.py` → `Ran 23 tests in 0.298s` / `OK`
  - C7 `tests/service/test_synthetic_identity.py` → `Ran 14 tests in 0.988s` / `OK`
  - C8 `tests/service/test_validate_identity.py` → `Ran 18 tests in 1.237s` / `OK`
  - C9 `npx vitest run tests/builderAccountChip.spec.ts` → `Test Files  1 passed (1)` / `Tests  12 passed (12)`
  - C10 `npx vitest run tests/credentialPicker.spec.ts` → `Test Files  1 passed (1)` / `Tests  29 passed (29)`
- C12 knob scan (the multiline regex over `config.py` + `service/app.py`) → `41`, list includes `'CREDENTIALS_MASTER_KEY'`; per-file split → `src/brief_crew/config.py 37` / `src/brief_crew/service/app.py 4`. `grep -n "^## 6" docs/tech-stack.md` → `306:## 6. Environment knobs — there are forty-one`; §6's own text says thirty-seven + four and pastes the scan block with `CREDENTIALS_MASTER_KEY` in it.
- C13 `git grep -n "api_key\|token" -- src/brief_crew/service/credentials.py` → 11 lines: the docstring (6), the RFC 9110 `token` comment (133), the help sentence that *contains* the text `print(base64.b64encode(secrets.token_bytes(...` inside an f-string (236), `secrets.token_bytes(NONCE_BYTES)` (296), `secrets.token_hex(4)` (449), ContextVar tokens (676–682), and the probe's `Authorization: Bearer` header construction (942, 952, 959). `grep -n "print(\|logging\|logger" src/brief_crew/service/credentials.py src/brief_crew/service/credentials_api.py` → only line 236 (the string), no call.
- Plan file committed (index metadata only, no content read): `git ls-files .agent/plans | grep 01` → `.agent/plans/01-auth-and-workspaces.md`; `git status --porcelain -- .agent/` → empty.
- C4 direct: `create_app(synthetic=True)` under patched `AUTH_BASE_URL="https://auth.example.test"`, `VALIDATOR_REQUIRE_AUTH=True`, `CREDENTIALS_MASTER_KEY=""` → `RuntimeError: AUTH_BASE_URL is set but CREDENTIALS_MASTER_KEY is empty; people can sign in and the credential vault has no key to keep theirs with. Mint one with python -c "…" and set it`.
- Probe script `probe.py` (urllib; users `alice-c2`, `bob-c2`, `carol/dave/erin/frank-<stamp>`, and nobody; ids `ug_0000c2c2` / `cr_0000c2c2` / run `00000000-0000-4000-8000-0000c2c2c2c2` invented). Setup as Alice: `POST /api/builder/credentials {kind:openrouter, fields:{api_key:<secret>}}` → `{"id":"cr_421c20d8","kind":"openrouter","label":"critic key …","created_at":…,"updated_at":…,"last_used_at":null} [201]`, secret absent from body; `POST /api/builder/workflows` (`straight_line()` with `nodes[scoper].config.credential_id = cr_421c20d8`) → `[201]` `ug_dbc5c011` v1; `POST …/ug_dbc5c011/publish` → `{"workflow_id":"ug_dbc5c011",…,"reserved_input_keys":["__builder__","out__idea","out__report","out__scoper"]} [200]`; `GET /api/workflows` as Alice lists it; credential list keys `['created_at','id','kind','label','last_used_at','updated_at']`, secret absent.
  - As **bob-c2** and as **nobody** (every line below answered identically for both except where noted):
    - `GET /api/workflows` → `[{"id":"brief-flow",…},{"id":"idea-validator",…}] [200]` — `ug_dbc5c011` absent.
    - `GET /api/workflows/ug_dbc5c011/graph` and `…/ug_0000c2c2/graph` → `{"detail":"workflow not found"} [404]` `content-length=31`, no `etag`; with `If-None-Match: *` → same `[404]`; bodies identical `True`, headers identical `True`.
    - `GET /api/builder/workflows` → `[] [200]`.
    - `GET|PUT(ev=1)|PUT(ev=99)|DELETE /api/builder/workflows/{id}`, `GET ?version=99`, `POST …/publish`, `POST …/publish?version=99`, `POST …/unpublish`, `POST …/duplicate`, `GET …/export`, `GET …/versions` for `ug_dbc5c011` and for `ug_0000c2c2` → every one `{"detail":"document not found"} [404]`; bodies identical `True`, headers identical `True`. No 409 (version or published), no 403.
    - `GET|DELETE /api/builder/credentials/cr_421c20d8`, `POST …/cr_421c20d8/test`, and the same for `cr_0000c2c2` → as bob `{"detail":"credential not found"} [404]` (no `www-authenticate`); as nobody `{"detail":"sign in to use this endpoint"} [401]` `www-authenticate='Bearer'` — identical for foreign and invented.
    - `POST /api/builder/validate` naming `cr_421c20d8` → as bob `{"valid":false,"problems":[{"code":"credential-missing",…"scoper names the credential cr_421c20d8, which is not in your vault…"}],…,"identity_checked":true} [200]`; naming `cr_0000c2c2` → same code and sentence shape; as nobody both → `{"valid":true,"problems":[],…,"identity_checked":false} [200]`.
    - `POST /api/sessions/{s}/runs` with `{workflow_id, inputs, gates:"human"}` for bodies `clean`, `{__builder__:"x"}`, `{out__idea:"x"}`, `{out__report:"x"}`, `{out__not_a_node:"x"}` on `ug_dbc5c011` and on `ug_0000c2c2` → `{"detail":"workflow not found"} [404]` `content-length=31` for every pair; "identical across foreign/invented for every body? True" (bob's 9th launch and nobody's 11th read `{"detail":"too many runs from this client; wait and try again"} [429] retry-after=6` — the limiter, see below).
    - `gates:"auto"` on `ug_dbc5c011` vs `ug_0000c2c2` as fresh user `frank-c2` → `{"detail":"workflow not found"} [404]` both (ownership 404 wins over the auto-gates 403).
    - `inputs:{idea, no_gates:true}` on either id → `[422]` `Value error, inputs may not carry the reserved control key no_gates…` — identical for foreign and invented.
    - `GET /api/builder/workflows/ug_dbc5c011` with `X-Synthetic-User: Bad User!` → `{"detail":"X-Synthetic-User must match ^[a-z0-9_-]{1,64}$"} [400]`.
    - Bob creates his own document naming Alice's `cr_421c20d8` → `[201]`; `POST …/publish` → `{"detail":{"message":"this graph has 1 problem(s) that stop it compiling: scoper names the credential cr_421c20d8, which is not in your vault…","problems":[{"code":"credential-missing"…}]}} [422]`; same with `cr_0000c2c2` → same shape.
    - Bob creates a credential with Alice's exact label → `[201]` (uniqueness is per user, no cross-user 409).
    - `GET /api/runs?limit=25` → `{"runs":[]} [200]` for bob and for nobody.
    - `POST /api/sessions/…/runs` on `idea-validator` with `__builder__` / `out__scoper` in inputs → `[429]` at that point in the sequence (bucket spent), so not observed; not needed for any claim.
  - Rate-limiter charging (fresh user each): `carol` × 11 launches naming `ug_dbc5c011` with `__builder__` → `404 ×10, then 429`; `dave` × 11 clean launches naming `ug_0000c2c2` → `404 ×10, then 429`; `erin` × 11 `POST /api/builder/credentials/cr_421c20d8/test` → `404 ×10, then 429`. A probe naming a foreign id is charged exactly as a clean one.
  - Alice's run: `POST /api/sessions/critic-alice/runs` → `[202]` `d349aa41-…`; status → `completed`; her `GET …/logs?format=ndjson` → 3422 bytes, secret absent, `cr_421c20d8` absent; her frames → secret absent, credential id absent.
  - As bob and as nobody on `d349aa41-…` and on the invented run id: `GET /api/runs/{id}`, `GET …/frames`, `GET …/frames?kinds=bogus`, `GET …/logs?format=ndjson`, `GET …/logs?format=bogus`, `POST …/cancel`, `POST …/gates/confirm {outcome:"approve",fields:{}}` → every one `{"detail":"run not found"} [404]`; bodies identical `True`, headers identical `True` (the bogus `kinds`/`format` never reach their 400s).
  - WebSocket (`ws_probe.py`, `websockets` 17.1, header `X-Synthetic-User`): as bob and as nobody, `ws://127.0.0.1:8099/ws?session_id=critic-alice&run_id=d349aa41-…`, the same with `session_id=not-her-session`, with `&after=-1`, and with the invented run id → all `closed code=4404 reason='run not found'`. Owner control as alice-c2 → first message `{"type":"frame","data":{"v":1,"seq":1,"run_id":"d349aa41-…","kind":"run_state",…`; owner with wrong session → `closed code=4403 reason='session mismatch'` (only the owner ever sees a code other than 4404).
  - Timing (`probe.py --timing`, N=60 interleaved, medians in ms, foreign / invented): `GET graph as bob 14.98 / 15.09`; `GET builder doc as bob 15.10 / 14.29`; `GET credential as bob 14.48 / 14.65`; `GET run as bob 11.96 / 12.52`; `GET graph as nobody 13.70 / 12.41`; `POST run (fresh user each) __builder__ body 12.24 / 12.27`; `POST run clean body 12.34 / 12.16`. No direction survives the noise (p10 values 1.7–2.4 ms on every arm).
  - C1 last clause: nobody creates + publishes `straight_line` (row `user_id=None`) → `[201]`, `[200]` `ug_f2fca8a0`; launch as `gina-c2` → `[202]`, as `hal-c2` → `[202]`, as nobody → `{"detail":"workflow ug_f2fca8a0 reaches a billable node before any human gate; sign in, or add a gate…"} [403]` (the pre-existing gateless rule, not ownership); `GET /api/workflows` lists it for nobody: `False` (by design); `GET …/graph` → `[200]` for all three; another user's `GET /api/runs/<gina's run>` → `[404]`. C2: `POST /api/builder/credentials` as nobody → `{"detail":"sign in to use this endpoint"} [401]`.
  - Shared-browser measurement (`shared_browser.cjs`: Playwright Chromium from `frontend/node_modules`, 1440×900 dark, against the E2E Vite server on 5273 that I started for this and stopped afterwards; identity = the harness's `e2e_synthetic_user` cookie):
    - as alice-c2 at `#/build/ug_dbc5c011`, one node drag → save chip `saved · v1`; `localStorage` keys: `['builder-draft:ug_dbc5c011']` — `955 bytes; contains credential id cr_421c20d8? true; contains "scoper"? true`; 4 s later still `saved · v1` and the draft key is still present.
    - alice-c2 launches her gated graph `ug_b2c43775` from the console (handoff record + Review + Launch) → gate card shown; `localStorage['validator-active-run'] = {"version":1,"runId":"0e2d40fc-b106-…","sessionId":"4c3878c9-…","workflowId":"ug_b2c43775","inputField":"idea"}`; run id on screen `0e2d40fc`.
    - cookie replaced with bob-c2, `/#/` reloaded → handoff banner `Running your published graph alice gated graph. It asks for idea.`; graph banner `This graph cannot be launched from here. The server answered: workflow not found`; error banner `run not found`; **run id on screen `0e2d40fc`** (Alice's); gate cards 0, nodes 0, live-status `Ready`; Launch button `{"disabled":true,"opacity":"0.42","cursor":"not-allowed","text":"Relaunch"}`; storage keys `['validator-session-id','builder-draft:ug_dbc5c011','builder-vocabulary','builder-run-handoff']`; `builder-draft:ug_dbc5c011: 955 bytes; contains cr_421c20d8? true`.
    - bob-c2 at `#/build/ug_dbc5c011` → notice `document not found`, canvas 0, restore-bar 0; draft key still present. Console errors other than the 404s: `[]`. Screenshots kept under my scratch dir.
  - Cleanup: every document and credential I created under alice-c2/bob-c2/nobody was unpublished and deleted (`204`s); backend left running (`/healthz` 200 afterwards).

## Capture review

- **Set A** (5 PNGs, `A-credentials-list-empty`, `A-credential-type-picker`, `A-credential-dialog-openrouter-empty/-filled`, `A-credentials-list-one-row`): Flowise's Credentials page — an empty state with illustration, a modal grid of ~20 credential types, an "OpenRouter API Key" modal with name + masked key and a disabled/enabled Add button, and a one-row table (Name / Last Updated / Created, edit and delete icons). Every credential surface is a modal over the page; nothing on screen says who owns the row.
- **Set B** (7 PNGs): `B-gallery-account-chip` — the Flow builder gallery with an `alice` chip and "No saved graphs yet"; `B-credential-picker-closed/-form-filled/-picked` — the OpenRouter key chosen inside the docked inspector of the agent node ("Platform key" select, "Add a OpenRouter key", masked `API KEY` field, then the picked key labelled `your key` with a Test button) with the canvas still visible; `B-bob-deep-link-to-alice` — Bob (`bob` chip) on the gallery with a red `document not found` toast and an empty "Saved here"; `B-bob-console-on-alice-workflow` — Bob's run console pointed at "Alice's graph": empty canvas, `Idle · unavailable`, banner "This graph cannot be launched from here. The server answered: workflow not found", Launch rendered dim; `B-sign-in-wall` — "Continue with Google" over the studio.
- Belief: **B is the product under review** — its chrome reads "Flow builder" / "Validator Studio", the chips carry the synthetic ids `alice` and `bob` that the E2E harness uses, and A is Flowise by its own logo and star counter.

## Scores

| dim | ours | ref | delta | carried by |
| --- | --- | --- | --- | --- |
| 14 | 9 | 10 | 1 | D-1 (1) |
| 16 | 10 | 10 | 0 | — |

## Ranked defects (most damaging first)

D-1. `[dim 14]` A sign-out ends the token and nothing else, so the next person on the same browser inherits the previous user's flow and run pointers in plaintext. Located: `frontend/src/composables/useAuthGate.ts:97-103` (`endSession` = `clearAccessToken(); await signOut()` — no `localStorage`/`sessionStorage` key is removed anywhere on the auth path); `frontend/src/composables/useBuilderPersistence.ts:502-506` (`writeDraft(model.document)` on every successful load, so `localStorage['builder-draft:<id>']` holds the whole document — nodes, prompt inputs, `config.credential_id` — even for a *saved* document, not only unsaved work); `frontend/src/composables/useValidatorRun.ts:526` and `589-593` (`runId.value = context.runId` is set before the fetch and never reset in the `catch`, so a restore the server refused with 404 still renders the previous user's run id through `StatusPanel.vue:248` and relabels the button "Relaunch"). Shown by `node shared_browser.cjs`: after the identity changed to bob-c2 the console printed `run id on screen: 0e2d40fc` (Alice's run), `handoff banner: Running your published graph alice gated graph`, `error banner: run not found`, and storage still held `builder-draft:ug_dbc5c011: 955 bytes; contains cr_421c20d8? true`. The server refused every request (all 404), so this is client residue rather than an API hole, and it needs a shared browser profile — which is why it costs one point and not more; the measurement swapped the harness's synthetic-user cookie because a real Google sign-out cannot run here, and the code of `endSession` shows a real sign-out leaves the same keys behind. `ours 9 / ref 10 / delta 1`.

## Criteria I could not verify, and why

- **3** (AAD binding, wrong master key, `key_version`, nonce never reused over 10,000 writes): only its test (`Ran 24 tests` / `OK`). No black-box probe of a running service can observe a nonce or a ciphertext.
- **4, second half** (auth off + no key → credential routes 503): the running backend has `CREDENTIALS_MASTER_KEY` set, so only the test (`Ran 8 tests` / `OK`) covers it. The first half I reproduced directly (the `RuntimeError` naming the knob, above).
- **5** (three ids and no field value in the compiled YAML; `resolve_credential` for owner vs `CredentialNotYours`; `last_used_at` moves): the compiled definition is not served by any route, so only its test (`Ran 15 tests` / `OK`). Adjacent evidence from the live run: Alice's own NDJSON export and frames contained neither the secret nor the credential id.
- **7, second clause** (with `AUTH_BASE_URL` set the header is ignored and the bearer path wins): the running backend has no `AUTH_BASE_URL`; verified by its test (`Ran 14 tests` / `OK`) and by reading `service/app.py:768` (`if header_value is None or not synthetic or project_config.AUTH_BASE_URL: return None`). The first and third clauses I probed (header honoured; malformed header 400; no header = anonymous 401 on the vault).
- **12, "regenerated, not edited"**: I re-ran the scan and it agrees with the file (41; 37 + 4; the knob present), but whether the figure in `docs/tech-stack.md` was produced by running the command rather than typed is a history question, and history is out of bounds for this review.
- **16, "committed"**: established from index metadata only (`git ls-files` names the plan; porcelain on `.agent/` is empty, so the working file equals HEAD), and from `test_the_plan_records_the_fields_exclusion_beside_the_pin` passing against that file. I did not read the plan or any commit.

## Rejected observations

- The `no_gates` smuggling refusal on `POST /api/sessions/{s}/runs` is FastAPI's default 422 and echoes the request's own `input` back (`"input":{"idea":"x","no_gates":true}`). It is identical for a foreign and an invented id and echoes nothing but the caller's own body, so it is neither an oracle nor a leak; the credentials router hand-parses precisely to avoid this and is unaffected.
- Bob's 9th launch (and nobody's 11th) read 429 inside my probe sequence: his two `…/credentials/{id}/test` probes had been charged to the same per-user run bucket first (`credentials_api.py:213`, documented in the module docstring). Consistent charging, not a defect — and the carol/dave/erin series proved the charge is the same for foreign, invented and credential probes.
- An anonymous launch of an *unowned* gateless graph answers 403 naming the id while an invented id answers 404. Not an oracle: an unowned document is readable by everyone by design (`store._visible_to`, `GET /api/builder/workflows/{id}` → 200 for anyone), so nothing hidden is confirmed. The 403 is the pre-existing `BUILDER_ALLOW_GATELESS_GRAPHS` rule, not this feature.
- Capture `B-bob-console-on-alice-workflow` shows the Launch button in the full green gradient, which reads as enabled at a glance; measured, it is `disabled`, opacity 0.42, cursor `not-allowed`. Affordance, outside rows 14 and 16.
- The console's handoff banner says "Running **your** published graph …" to whoever holds the record, including a stranger who inherited it. Wording; its only real-world path is the shared-browser case already counted in D-1.
- `RUN_RATE_LIMIT_TRUST_FORWARDED_FOR` defaults on, so an anonymous caller can rotate `X-Forwarded-For` for a fresh bucket. Pre-existing admission-control configuration, documented in `config.py`, not this feature's; a signed-in caller is keyed on their id and cannot.
- The E2E Vite config warns `import "./syntheticUser" without a file extension` under `configLoader: 'native'`. Toolchain noise; the server started and every test passed.
- `tests/builder/test_credential_resolution.py` writes CrewAI `Error executing listener …` lines to stderr while passing; they are the refusals the tests assert, not failures.
- `tests/pg/test_two_writers` prints `pending feedback for flow flow-… was written by another process first; this context was not stored` mid-run; that is the losing writer's expected log line and the test is `ok`.
- The gated fixture I published as Alice reports `gated_before_spend: false` because its scoper sits above the gate; a property of `tests/builder/test_compiler.gated_loop`, not of the product.

### Row verifier — plan 01, the four ledger rows (not a critic; it knew what was claimed and what was changed)

HEAD `44f3582` on `gauntlet/plans` (worktree `D:\MultiAgentSystem-wt\integration`); `POST /api/builder/workflows/ug_00000000/unpublish` → `{"detail":"document not found"} [404]`; 2026-09-03, 16:40–16:47 local.

#### Per row

##### D-01-1 — ABSENT
- **probe:** As `X-Synthetic-User: verifier-alice-164350`, `POST /api/builder/workflows` with the `straight_line()` document (`tests/builder/test_compiler.py`, wired by `tests/service/identities.wire`) → 201, then `POST /api/builder/workflows/ug_b61d115d/publish` → 200 with `reserved_input_keys=['__builder__', 'out__idea', 'out__report', 'out__scoper']`. Then `POST /api/sessions/x/runs` with `{"workflow_id": <id>, "inputs": {"idea": "p", <key>: "x"}, "gates": "human"}` for `<key>` in `__builder__`, `out__idea` (declared), `out__not_a_node_here` (not a node), and no key; for `<id>` = Alice's `ug_b61d115d` and the invented `ug_deadbeef`; as Bob (`verifier-bob-164350`), as nobody (no header), and as Alice (owner control). Then 15 rapid `__builder__` probes naming Alice's id as a fresh user, a 15-probe clean-body control naming `ug_deadbeef` as a second fresh user, and a 15-probe clean-body control naming Alice's id as a third fresh user. Script: `scratchpad\verifier01\probe_d01_1.py` (urllib, no browser), against `http://127.0.0.1:8099`.
- **output:**
  ```text
  bob  alice-id  key=__builder__: {"detail":"workflow not found"} [404]
  bob  alice-id  key=out__idea: {"detail":"workflow not found"} [404]
  bob  alice-id  key=out__not_a_node_here: {"detail":"workflow not found"} [404]
  bob  alice-id  key=None: {"detail":"workflow not found"} [404]
  bob  ug_deadbeef key=__builder__: {"detail":"workflow not found"} [404]
  bob  ug_deadbeef key=out__idea: {"detail":"workflow not found"} [404]
  bob  ug_deadbeef key=out__not_a_node_here: {"detail":"workflow not found"} [404]
  bob  ug_deadbeef key=None: {"detail":"workflow not found"} [404]
  none alice-id  key=__builder__: {"detail":"workflow not found"} [404]
  none alice-id  key=out__idea: {"detail":"workflow not found"} [404]
  none alice-id  key=out__not_a_node_here: {"detail":"workflow not found"} [404]
  none alice-id  key=None: {"detail":"workflow not found"} [404]
  none ug_deadbeef key=__builder__: {"detail":"workflow not found"} [404]
  none ug_deadbeef key=out__idea: {"detail":"workflow not found"} [404]
  none ug_deadbeef key=out__not_a_node_here: {"detail":"workflow not found"} [404]
  none ug_deadbeef key=None: {"detail":"workflow not found"} [404]
  alice alice-id key=__builder__: {"detail":"inputs may not carry the reserved control key __builder__; workflow ug_b61d115d is launched from inputs.idea"} [422]
  alice alice-id key=out__idea: {"detail":"inputs may not carry the reserved control key out__idea; workflow ug_b61d115d is launched from inputs.idea"} [422]
  alice alice-id key=out__not_a_node_here: {"run_id":"e5df8f43-9910-4754-9ec9-5a4dbd170c9c","status":"queued","graph_version":"1f2dd6f1501dca74"} [202]
  state-key series (fresh user, __builder__ on Alice's id):   [404, 404, 404, 404, 404, 404, 404, 404, 404, 404, 429, 429, 429, 429, 429]
  clean-404 control (fresh user, no key, ug_deadbeef):        [404, 404, 404, 404, 404, 404, 404, 404, 404, 404, 429, 429, 429, 429, 429]
  clean control (fresh user, no key, Alice's id):             [404, 404, 404, 404, 404, 404, 404, 404, 404, 404, 429, 429, 429, 429, 429]
  #11 in every series: {"detail":"too many runs from this client; wait and try again"} [429]
  ```
- **verdict:** The row's sentence is not true at HEAD: for Bob and for nobody, every body (`__builder__`, the declared `out__idea`, the undeclared `out__not_a_node_here`, clean) answers the byte-identical `{"detail":"workflow not found"} [404]` for Alice's id and for `ug_deadbeef`, and the 15-probe `__builder__` series trips the limiter on the 11th exactly as both clean controls do, while the owner alone still receives the `422 … reserved control key` naming the key.

##### D-01-2 — ABSENT
- **probe:** Started `npx vite --config e2e/vite.e2e.config.ts --port 5273` from `frontend/` (proxy to :8099, cookie → `X-Synthetic-User`), drove Playwright's Chromium from `frontend/node_modules` with `scratchpad\verifier01\measure_d01_2.cjs`: context 1440×900, `colorScheme: 'dark'`, cookie `e2e_synthetic_user=verifier-bob-164350`; loaded `/#/build` (account chip read `verifier-bob-164350`), wrote `sessionStorage['builder-run-handoff'] = {"workflowId":"ug_b61d115d","inputField":"idea","name":"Alice's graph"}`, navigated to `/#/`, reloaded, waited for `.live-status` to leave "connecting" + 2 s; measured; then pressed Review, filled a valid idea, re-measured Launch; screenshot `scratchpad\verifier01\verifier01-bob-console-on-alice-workflow-1440x900-dark.png`. Compared against `benchmarks/ours/01/1/critic01-bob-console-on-alice-workflow-1440x900-dark.png` (round 1) and `benchmarks/ours/01/2/01-bob-console-on-alice-workflow-1440x900-dark.png` (HEAD, today). Vite server stopped afterwards (see Anything else).
- **output:**
  ```text
  live_status_chip:                       "Ready"            (round 1: "Mock Mode")
  canvas_kicker:                          "PUBLISHED GRAPH / Alice's graph / Idle / unavailable"
  canvas_meta_code:                       "unavailable"      (round 1: "mock-of-d8be7621")
  vue_flow_node_count:                    0                  (round 1: 14 nodes, Scoper … Validation brief)
  workflow_node_count:                    0
  mock_version_string_anywhere (/mock-[0-9a-f]/ over page HTML):  false
  occurrences of "mock" in HTML:          2, both inside Vue template comments; document.body.innerText contains "mock": false
  handoff_banner:                         "Running your published graph Alice's graph. It asks for idea."
  graph_banner (.status-panel .graph-banner): "This graph cannot be launched from here. The server answered: workflow not found"   buttons inside it: 0 (not dismissible; round 1's "workflow not found" banner had a dismiss ×)
  transport_banner_count:                 0
  error_banner:                           null
  workflow_card:                          "Alice's graph"
  launch_button_text:                     "Launch"
  launch disabled attribute (initial):    ""  (present)   isDisabled: true
  launch disabled attribute (after Review + valid idea): ""  (present)   isDisabled: true      (round 1: enabled, green)
  vue_flow_node_count_after:              0
  run_id_elements: 0   gate_cards: 0   localStorage['validator-active-run']: null
  console errors:                         ["Failed to load resource: the server responded with a status of 404 (Not Found)"]
  API seen by the page:                   GET /api/workflows -> 200 ; GET /api/workflows/ug_b61d115d/graph -> 404
  ```
  The HEAD capture `01-bob-console-on-alice-workflow-1440x900-dark.png` shows the same state my capture does: header chip `Ready`, version chip `unavailable`, an empty dotted canvas with only the zoom controls, the two-line refusal banner, and a dimmed Launch. The round-1 capture shows `Mock Mode`, `mock-of-d8be7621`, the 14-node validator topology under `PUBLISHED GRAPH / Test graph`, and a bright green Launch.
- **verdict:** The row's sentence is not true at HEAD — the 404 on `GET /api/workflows/ug_b61d115d/graph` no longer drops the console into mock mode: `live_status_chip = "Ready"`, `vue_flow_node_count = 0`, no `mock-` string anywhere in the page, the server's own sentence in a banner with no dismiss button, and `Launch` carrying `disabled=""` before and after Review + a valid idea; the kicker and WORKFLOW card still read the handoff's own name (`Alice's graph`), which is the record Bob was handed and not a label on a fabricated graph.

##### D-01-3 — ABSENT
- **probe:** Quoted criterion 6 from `.agent/plans/01-auth-and-workspaces.md` at HEAD (lines 232–250) and Status row 6 (line 289); ran `PYTHONPATH="$PWD/src" python -c "from brief_crew.events.redaction import is_secret_key; print(is_secret_key('fields'))"` (module resolved to `D:\MultiAgentSystem-wt\integration\src\brief_crew\__init__.py`); ran `python -m unittest discover -s tests/service -t . -p "test_secret_redaction.py" -v`.
- **output:** Criterion 6 now reads, verbatim:
  > 6. `tests/service/test_secret_redaction.py`: a frame whose `details` carries every D6 key name round-trips as `***` through the serializer and the persistence sanitiser; a run log export (`/logs?format=ndjson`) contains none of the plaintext fixtures.
  >
  >    **Amended 2026-09-03 (round 2, D-01-3; ratified by the owner).** `fields` is excluded from "every D6 key name". It is the gate form's own key - `pending_gate.fields` is the editable half of every gate payload (`registry.py`, `persistence.py`, `RunStatusResponse`) - and redacting it by name turned every gate form into the string `***` and failed `RunStatusResponse` validation on the first synthetic run. The vault's plaintext object of the same name never reaches a frame: it lives in `ResolvedCredential`, whose `repr` hides it, and is handed to one constructor. Every other D6 name round-trips as `***` exactly as the sentence above says. The exclusion is pinned by `tests/service/test_secret_redaction.py::ListTests::test_fields_is_deliberately_not_on_the_list`, and this note is pinned beside it by `ListTests::test_the_plan_records_the_fields_exclusion_beside_the_pin`. Recorded as a dated amendment rather than an edit to the sentence, because round 1 ticked this criterion with its text unchanged and the deviation living only in a Status row - which is the process failure the critic's dimension 16 exists to catch.

  Status row 6: `| 6 | done; **criterion amended 2026-09-03 (D-01-3)** | tests/service/test_secret_redaction.py (13 → 23, measured after the suffix-rule tests landed in the same file). **fields is NOT on the redaction list** … Pinned by test_fields_is_deliberately_not_on_the_list; the criterion's own text now carries the dated exclusion, and test_the_plan_records_the_fields_exclusion_beside_the_pin fails if that note and the pin ever part company |`. The Status header still reads `**Criteria complete · 2026-09-03.**`.
  ```text
  is_secret_key('fields') -> False
  test_fields_is_deliberately_not_on_the_list (…ListTests…) See the module docstring. A gate form is a mapping called `fields`. ... ok
  test_the_plan_records_the_fields_exclusion_beside_the_pin (…ListTests…) D-01-3: the deviation above is written INTO criterion 6, dated, naming this pin. ... ok
  test_every_d6_key_is_on_the_list … ok      test_a_gate_forms_fields_survive_the_walk … ok
  Ran 23 tests in 0.290s
  OK
  ```
  The test that pins the exclusion is `ListTests.test_fields_is_deliberately_not_on_the_list`; the test that pins the plan note is `ListTests.test_the_plan_records_the_fields_exclusion_beside_the_pin`. `dbe803d` added both the amendment and the second test (`git show --stat`: plan +19/−1, test +30); `a952c74` changed the row-6 count only.
- **verdict:** The row's sentence is not true at HEAD: `is_secret_key('fields')` is still `False`, but the criterion's own text now carries the dated `fields` exclusion, the Status row says "criterion amended", and `Ran 23 tests … OK` includes `test_the_plan_records_the_fields_exclusion_beside_the_pin`, so the tick no longer stands over an unamended sentence.

##### D-01-4 — ABSENT
- **probe:** `PYTHONPATH="$PWD/src" python -m unittest discover -s tests/service -t . -p "test_workflow_ownership.py" -v`; `grep -n "out__\|__builder__\|reserved" tests/service/test_workflow_ownership.py tests/service/test_isolation_matrix.py`; read Status row 1 and criterion 1 of the plan at HEAD.
- **output:**
  ```text
  Ran 23 tests in 2.672s
  OK
  ```
  Classes and the state-key tests among the 23: `StateKeyProbeTests` (5: `test_a_strangers_state_key_probe_is_the_same_404_as_an_unknown_id`, `test_a_declared_and_an_undeclared_state_key_read_the_same_to_a_stranger`, `test_nobody_gets_the_same_answer_for_a_foreign_and_an_unknown_id`, `test_the_global_keys_are_still_refused_for_every_id_before_anything_else`, `test_the_owner_is_still_refused_a_state_key_with_a_422_naming_it`), `StateKeyProbeIsChargedTests` (1: `test_the_probe_after_the_limit_is_429`), `AnonymousStateKeyProbeTests` (3: `test_nobodys_state_key_probe_is_the_same_404_as_an_unknown_id`, `test_a_signed_in_strangers_state_key_probe_is_the_same_404_too`, `test_the_owner_is_still_refused_the_key_with_a_422`), plus `OwnedWorkflowTests` (9), `UnownedWorkflowTests` (4), `OwnershipSurvivesRestartTests` (1).
  ```text
  tests/service/test_workflow_ownership.py:57:#: The critic's probe distinguished `out__scoper` (422) from `out__market`
  tests/service/test_workflow_ownership.py:59:UNDECLARED_STATE_KEY = "out__not_a_node_here"
  tests/service/test_workflow_ownership.py:87:        config.reserved_run_input_keys(workflow_id) - config.GLOBAL_RESERVED_RUN_INPUT_KEYS
  tests/service/test_workflow_ownership.py:189:    else - and "everything else" includes the reserved-key check, which for a
  tests/service/test_workflow_ownership.py:199:        # `out__<node>` per node. Both must be present for the test to mean
  tests/service/test_workflow_ownership.py:201:        self.assertIn("__builder__", self.state_keys)
  tests/service/test_workflow_ownership.py:202:        self.assertTrue(any(key.startswith("out__") for key in self.state_keys), self.state_keys)
  tests/service/test_workflow_ownership.py:205:        declared_out = next(key for key in self.state_keys if key.startswith("out__"))
  tests/service/test_workflow_ownership.py:206:        return ["__builder__", declared_out, UNDECLARED_STATE_KEY]
  tests/service/test_workflow_ownership.py:230:        """The enumeration half: `out__scoper` and `out__market` were 422 and 404."""
  tests/service/test_workflow_ownership.py:232:        declared = next(key for key in self.state_keys if key.startswith("out__"))
  tests/service/test_workflow_ownership.py:247:        for key in ("__builder__", next(k for k in self.state_keys if k.startswith("out__"))):
  tests/service/test_workflow_ownership.py:254:                self.assertIn("reserved control key", response.json()["detail"])
  tests/service/test_workflow_ownership.py:285:    The critic's control: 15 rapid `__builder__` probes as a fresh user never
  tests/service/test_workflow_ownership.py:304:                RUNS, json=launch_with(workflow_id, "__builder__"), headers=self.as_bob()
  tests/service/test_workflow_ownership.py:322:    `out__scoper` probe answered 422; it must now answer the same 404 as an
  tests/service/test_workflow_ownership.py:358:        declared_out = next(key for key in self.state_keys if key.startswith("out__"))
  tests/service/test_workflow_ownership.py:359:        return ["__builder__", declared_out, UNDECLARED_STATE_KEY]
  tests/service/test_workflow_ownership.py:391:            json=launch_with(self.workflow_id, "__builder__"),
  tests/service/test_workflow_ownership.py:395:        self.assertIn("__builder__", response.json()["detail"])
  tests/service/test_isolation_matrix.py:52:(`__builder__`, `out__<node>`) used to answer 422 from the request schema
  tests/service/test_isolation_matrix.py:269:        registered = config.reserved_run_input_keys(self.workflow_id) - config.GLOBAL_RESERVED_RUN_INPUT_KEYS
  tests/service/test_isolation_matrix.py:270:        self.state_keys = ["__builder__", next(k for k in sorted(registered) if k.startswith("out__"))]
  ```
  A state-key body is sent **by Bob** (`launch_with(workflow_id, "__builder__"), headers=self.as_bob()` at line 304; `StateKeyProbeTests`, `StateKeyProbeIsChargedTests`) and **by nobody** (`StateKeyProbeTests.test_nobody_gets_the_same_answer_for_a_foreign_and_an_unknown_id`, `AnonymousStateKeyProbeTests.test_nobodys_state_key_probe_is_the_same_404_as_an_unknown_id`), each with `__builder__`, a declared `out__<node>` and `out__not_a_node_here`; `test_isolation_matrix.py::LaunchRoute` (lines 252–318) sends both bodies as B (`test_b_cannot_tell_a_s_graph_from_an_invented_one_with_either_body`, admission counters compared before/after) and as the anonymous caller on an auth-on app (`test_anonymous_is_401_with_either_body_and_for_either_id`). Status row 1 at HEAD: `| 1 | done; **amended 2026-09-03 (round 2, D-01-1 / D-01-4)** | tests/service/test_workflow_ownership.py (14 → 23) — the 404 fires before any admission counter moves and beats the gates: auto 403; ownership survives a restart. **The round-1 tick rested on clean bodies only**: a body carrying one of the graph's own state names (__builder__, out__<node>) answered 422 from the request schema before the rate limiter and before the ownership 404, which is an oracle for which ids exist (D-01-1). Fixed in config.declared_reserved_run_input_keys; the proof is StateKeyProbeTests, StateKeyProbeIsChargedTests and AnonymousStateKeyProbeTests there, plus LaunchRoute in tests/service/test_isolation_matrix.py — Bob and nobody, both bodies, foreign id indistinguishable from an invented one |`. Criterion 1's own sentence is unchanged ("answers **404** before any admission counter moves"), and D-01-1's probe above shows it now holds for a state-key body.
- **verdict:** The row's sentence is not true at HEAD: the grep that returned nothing now returns 23 lines, `test_workflow_ownership.py` runs 23 tests green with state-key bodies sent by Bob and by nobody, and Status row 1 says in its own words that the round-1 tick rested on clean bodies only and names the classes that now prove it.

#### Anything else seen
- The owner's launch with an **undeclared** state-shaped key was admitted: as Alice, `{"idea":"p","out__not_a_node_here":"x"}` on her own graph answered `202` and the synthetic run `e5df8f43-9910-4754-9ec9-5a4dbd170c9c` completed (`status: "completed"`, 8 frames, `cost_usd: 0.0`). The tests assert only that a stranger cannot tell a declared key from an undeclared one; what the owner's undeclared key does is not asserted anywhere I read.
- Bob's `.live-status` element reads `Ready` but carries the class `live-status is-offline`, and the status-panel footer under it reads `offline  seq 0  0 dropped` while the header chip says `Ready`; the word and the colour class disagree on the same page.
- The two occurrences of "mock" in Bob's served HTML are both inside Vue template comments (`<!-- … a silent repoint is indistinguishable from the mock-mode failure. -->` and `… The previous behaviour was a small "Mock mode" chip rendered in the SUCCESS colour …`); nothing rendered says mock.
- The row-1 capture is named in the ledger as `critic01_bob_console_on_alice_wf.png`; the file on disk under `benchmarks/ours/01/1/` is `critic01-bob-console-on-alice-workflow-1440x900-dark.png`.
- `TaskStop` on the background `npx vite` shell left the node child (PID 24280) listening on 5273; it needed `Stop-Process -Id 24280 -Force`, after which `netstat` showed 5273 no longer listening. (The `pkill`-reports-success-on-Windows shape.)
- `/healthz` on :8099 reports `"storage":{"backend":"sqlite"}`; the brief described the backend as in-memory.
- The backend keeps the rows this verification created: document `ug_b61d115d` (published, owner `verifier-alice-164350`) and the one completed run above. Nothing was deleted.
- `tests/service/test_isolation_matrix.py::LaunchRoute` was read, not run: `MatrixCase` is PostgreSQL-backed and is not any row's own probe.
- Between the start and end of this session the worktree's `.agent/plans/15-persistence.md` (+10) and `PLANS.md` (+1/−1) became modified and `benchmarks/DEFECTS.md`'s diff grew (34 lines); none of these was touched here. Another agent is editing this worktree concurrently.
- My Playwright capture used `colorScheme: 'dark'` and the page rendered dark without any theme toggle; the HEAD capture in `benchmarks/ours/01/2/` matches it element for element (chip, version chip, empty canvas, banner, dimmed Launch).

## Result

| dim | ours | ref | delta | gate (8) | round 1 |
| --- | ---: | ---: | ---: | --- | --- |
| 14 | 9 | 10 | 1 | met on score, **one open defect** | 6 / 10 |
| 16 | 10 | 10 | 0 | met, no defect | 7 / 10 |

Both dimensions moved: 14 from 6 to 9, 16 from 7 to 10. The one point
left on 14 is a new defect the round-1 critic had not looked for — a
sign-out that ends the token and leaves the previous user's draft (with
its credential id) and run pointer in browser storage for the next person
on the same browser (the critic's D-1). Every round-1 probe now answers
the same 404 for a foreign id as for an invented one, with or without a
state-key body, charged to the limiter like a clean probe; Bob's console
pointed at Alice's workflow is live, empty, un-mocked and unlaunchable.

**The suite figures the critic measured agree with the builder's**, which
it never saw: Python `Ran 1642 tests … OK (skipped=6)`, Vitest `62 passed
(62)` / `1157 passed (1157)`, `vue-tsc -b --force` and `npm run build` exit
0, `e2e/isolation.spec.ts` `4 passed`, `34 tests in 5 files` / `8` tagged
`@launch`, `tests.pg.test_two_writers` `Ran 5 tests … OK`. Every module a
criterion names ran green alone (23, 19, 24, 8, 15, 23, 14, 18, 12, 29).
Neither suite trap fired.

### Ledger decisions, by the closing rule

A row closes only when its dimension scores at or above the reference, the
verifier shows the defect absent, the critic does not land it again, and
the fixing commit is named. Applied literally:

**Closed (2)**, each pointing at the output in which the defect is absent:

| id | dim | closed by | absent in |
| --- | --- | --- | --- |
| D-01-3 | 16 | `dbe803d` | criterion 6 carries the dated `fields` exclusion in its own text; `ListTests::test_the_plan_records_the_fields_exclusion_beside_the_pin` … `ok`; `Ran 23 tests … OK` (verifier). `a952c74` regenerated the Status row's count afterwards |
| D-01-4 | 16 | `fd7c74f` | the grep that returned nothing returns 23 lines; `StateKeyProbeTests`, `StateKeyProbeIsChargedTests`, `AnonymousStateKeyProbeTests` send state-key bodies as Bob and as nobody; `Ran 23 tests … OK`; Status row 1 says the round-1 tick rested on clean bodies (verifier) |

**Held open by the dimension score (2)** — the verifier shows both defects
absent and the critic did not land either again, but dimension 14 scored 9
against a reference of 10 (carried by the new D-01-5), and the rule as
written closes nothing on a dimension below its reference. Each row now
carries a dated note naming the verifier's output and the commit that
would close it:

| id | would close by | verifier |
| --- | --- | --- |
| D-01-1 | `0fe472f` | Bob and nobody, `__builder__` / `out__idea` / `out__not_a_node_here` / clean, Alice's id and `ug_deadbeef`: sixteen byte-identical `{"detail":"workflow not found"} [404]`; the 15-probe `__builder__` series reads `404 ×10, 429 ×5`, the same as both clean controls; only the owner still gets the `422 … reserved control key` |
| D-01-2 | `413c4dd` | Bob's console: `live_status_chip: "Ready"`, `vue_flow_node_count: 0`, no `mock-` string in the page, the server's sentence in a banner with no dismiss, `Launch` `disabled` before and after Review + a valid idea; `01-bob-console-on-alice-workflow-1440x900-dark.png` |

**New (1)**: D-01-5, dim 14, the critic's D-1 verbatim — a sign-out leaves
`localStorage['builder-draft:<id>']` (the whole document, credential id
included) and the run pointer behind, so the next person on the same
browser sees the previous user's run id and "Relaunch"; measured by
swapping the harness cookie, because a real Google sign-out cannot run
here, and `useAuthGate.ts:97-103` shows `endSession` clears the token and
nothing else.

**Recorded without a row**, because no critic scored them: the verifier
found that the owner's own launch with an *undeclared* state-shaped key
(`out__not_a_node_here`) is admitted (`[202]`, the synthetic run
completed), which no test asserts either way; that Bob's `.live-status`
reads `Ready` while carrying the class `is-offline` and the footer under it
reads `offline` (CLAUDE.md closed item 31 already records that the word
and the colour class can disagree); and that D-01-2's round-1 sentence
names the critic's capture as `critic01_bob_console_on_alice_wf.png` while
the file on disk is `critic01-bob-console-on-alice-workflow-1440x900-dark.png`
(renamed when it was filed; the sentence is verbatim and stays).

**Open after round 2: 3** (2 held, 1 new). Closed: 2. The critic is not
retired: it did not pass the artefact.

**Round 2 outcome: the gate is not met.** Dimension 14 carries an open
row; two of the three required rounds have run. `PLANS.md` records round 2
with three open defects; the ledger read top to bottom is the round-3
build list for plan 01, and round 3's critic is the CrewAI power user.


