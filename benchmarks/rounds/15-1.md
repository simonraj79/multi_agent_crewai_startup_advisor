# Plan 15 — judge round 1

Written 2026-09-03 against `gauntlet/plans` = `d11598b`, in
`D:\MultiAgentSystem-wt\integration`. Method: `benchmarks/README.md`.
Applicable rubric rows: **4** (inspector ergonomics — here the version
browser, criterion 15.4, scored against Flowise), **11**, **12**, **14**
and **16** (scored against test output the critic re-runs itself).

## The flip

Flipped with `secrets.randbits(1)` at 2026-09-03T00:15:27Z, before any
capture was shown to any critic and before the critic existed:

| label | is |
| --- | --- |
| **A** | ours — `gauntlet/plans` at `d11598b` |
| **B** | the reference — Flowise 3.1.4 (`flowiseai/flowise:3.1.4`, Docker) |

The critic was never told which is which. Capture files were renamed to
`A-*` / `B-*` and handed over from a directory that names neither product.

## Persona

Round 1: **design director**. The prompt opened with the README's sentence
verbatim: *You are a hostile design director who has shipped a competitor
to this product. You are looking for reasons to reject. Vague praise is a
failure of your job.*

**Two critics, one persona.** Row 4 is scored blind from the captures by a
critic that runs nothing and reads nothing else; rows 11, 12, 14 and 16 are
scored by a second critic that re-runs every command in the worktree and
therefore cannot be blind about which tree is ours. Splitting them is what
keeps the visual score blind at all; both outputs are below, verbatim.

## What the critic received, and nothing else

1. `.agent/plans/15-persistence.md` — the *Acceptance criteria* section
   only (11 numbered items), copied verbatim.
2. Our captures (**A**), 1440×900, dark: the canvas with the document bar,
   the document bar's overflow menu (Versions / Export / Import / Duplicate
   / Delete), the version browser at head, the version browser viewing v1 of
   v2 read-only, the docked delete confirm, the duplicate opened, and the
   result of importing an exported file.
3. The reference captures (**B**), 1440×900, dark, on the Agentflow v2
   canvas (`/v2/agentcanvas`, the surface the README names): the settings
   menu on an unsaved flow (Load only), the first-save name dialog, the
   settings menu on a saved flow (View Messages / View Leads /
   Configuration / Save As Template / Duplicate / Load / Export / Delete),
   the canvas after Export (which downloads a bare `{nodes, edges}` JSON
   at once, no dialog), the "Export As Template" dialog — Flowise's
   nearest thing to save-as — and the delete confirmation
   (`docs/flowise-notes.md` §3). Flowise has no version history and Load
   opens the operating system's file picker, so neither has a capture.
4. Rubric rows 4, 11, 12, 14 and 16, copied verbatim, with the commands the
   engineering rows' evidence comes from: the Python suite under
   `PYTHONPATH=<worktree>/src`, `npm test`, `e2e/isolation.spec.ts` and the
   version-browser step of `e2e/builder.spec.ts` against the free backend
   on :8099, and `tests/pg/test_two_writers.py` under `TEST_DATABASE_URL`.

It did not receive the plan's *Design* or *Status* sections, the
Integrator's report, CLAUDE.md, or any build history.

## Where the method could not be followed, and why

- **1440×900 dark only.** No 390×844 project and no light theme exist yet;
  both belong to plan 02.
- **No `capture.spec.ts` exists** (planned in 02). Ours was captured by a
  throwaway Playwright spec (`frontend/e2e/_round1_capture.spec.ts`,
  deleted after the run, never committed) against `SYNTHETIC=1
  SYNTHETIC_BRANCH_DELAY_SECONDS=5 PORT=8099` with `CREDENTIALS_MASTER_KEY`
  set. The captures are under `benchmarks/ours/15/1/` and are gitignored.
- **Rows 11 and 12 belong to plans 09 and 12**, whose harnesses
  (`tests/builder/test_failure_modes.py`, the twenty-fixture determinism
  harness) do not exist yet. They were put to the critic as the rubric
  words them; what the critic can score today is the part plan 15 owns —
  that the export/import/duplicate/restore paths give an identical document
  back (11) and that their refusals are legible and recoverable (12).
- **The engineering rows have no reference product to score.** `ref` for
  rows 11, 12, 14 and 16 is the bar the criteria set, 10 by definition, and
  every point of `ours` below it must be carried by a located defect.
- **The critic re-runs the suites in our worktree**, so for the engineering
  rows it necessarily knows which tree is ours; only the captures are blind.
- **The reference image had to be pulled.** The local
  `flowiseai/flowise:latest` is 2.2.4, not 3.1.4; `flowiseai/flowise:3.1.4`
  was pulled for this round.
- **The pinned image does not boot on its own database.** `flowiseai/flowise:3.1.4`
  crashes at startup with `TypeError: this.db.exec is not a function` from
  `connect-sqlite3` (its session store) and never listens. Flowise picks the
  session store by `DATABASE_TYPE`, so the reference was run against the
  local PostgreSQL 18 container (`pg18-test`, a throwaway `flowise` database
  reached through `host.docker.internal:5433`, dropped afterwards). Same
  version, same UI; only the storage behind it differs.
- **Reference captures were taken by hand through the Playwright MCP**, at
  1440×900 with Flowise's own dark-mode switch on, after the first-run
  account setup and after dismissing the "We're sunsetting Flowise" banner
  the 3.1.4 build shows on every page. They are under
  `benchmarks/reference/flowise/` and are gitignored.
- **The import capture shows the result, not a notice.** The throwaway
  spec exported Alice's document (which carried a credential) and imported
  the file back; the capture records whatever the product rendered. It is
  named `15-import-result`, not `15-import-notice`, on purpose.
- **Engineering defects carry a command and an output line** in place of
  the README's viewport and zoom, which have no meaning for a refusal seen
  in a probe; a defect seen in a capture carries the capture's viewport and
  zoom as written.

## Critic output, verbatim

### Critic 1 of 2 — visual, row 4, blind (A = ours, B = reference; the critic did not know)

## Persona
design director, round 1 (visual, dimension 4 only)

## Capture review

All measurements are read off the 1440x900 PNGs by pixel position. Neither set has a zoom readout; I give node-card widths instead.

**Set A** — a three-column shell: palette x0–235, canvas x236–1100, inspector x1100–1440. Document bar at y64–104.

- `A-canvas-document-bar-1440x900-dark.png` — bar holds back chevron, title "Minimal gated agent", status chip "unsaved changes · Ctrl+S", undo/redo (disabled), keyboard, kebab ⋮ (32x32 at x925–956, y68–100), Publish (x965–1060), right chevron. Cost meter y126–275. Four node cards 186px wide in a column (Request 01 INPUT, Confirm the request 02 GATE, Draft 03 AGENT, Result 04 OUTPUT); all four visible, canvas bottom y818. Library: "No saved graphs yet."
- `A-document-menu-1440x900-dark.png` — status now "saved · v2". Menu drops left from the kebab: x745–957, y108–288, 212px wide, five rows at 34px pitch: Versions, Export .builder.json, Import .builder.json…, Duplicate, Delete…. All five rows identical in colour and weight; no separator before Delete; Delete sits 34px under Duplicate. A fifth node (Transform 1, error badge) has been added at x763–905, partly under the minimap. Library row "Minimal gated agent v2 DRAFT".
- `A-version-browser-head-1440x900-dark.png` — Versions is a docked panel inside the canvas column, y120–245 (125px), header "VERSIONS / 2 stored", close × 32x32 at x1028–1060. Rows: "v2 [HEAD][DRAFT] 🕐 3 Sept, 00:19 — 1.4 KB" (highlighted, 28px tall) and "v1 [DRAFT] 🕐 3 Sept, 00:19 — 1.2 KB" (26px). No chevron, button, or hover state on either row. Cost meter and canvas shifted down ~133px; Result and Transform 1 are now below y818 — 2 of 5 nodes off-screen, no re-fit.
- `A-version-viewing-v1-read-only-1440x900-dark.png` — banner y178–217 (amber tint, eye icon): "Viewing v1 of v2 — read-only. Restore it to make it the next version, or go back to v2." with "↩ Back to v2" (98x28, secondary) and "Restore v1" (104x28, filled, primary). v1 row highlighted. Canvas shows v1's four nodes; Draft is clipped at y788–830 and Result is off-screen. Inspector NAME and RUN INPUT fields are greyed/disabled. **But** the document-bar chip now reads "✓ saved · v1" in the same style as "saved · v2", Publish is full-colour, the palette is undimmed, and the node cards carry no lock or dimming.
- `A-delete-confirm-docked-1440x900-dark.png` — delete is a maroon docked strip y250–355 (105px) stacked under the still-open Versions panel: "Delete **Minimal gated agent** and every stored version of it? Deleting a published graph unregisters it; running graphs are not affected. Type **Minimal gated agent** to confirm." Text input 489x36, "Keep it" 67x36, "Delete" 67x36 disabled until the name matches. Canvas now starts ~y520; only Request and Confirm are visible — 3 of 5 nodes hidden.
- `A-duplicate-opened-1440x900-dark.png` — title "Minimal gated agent copy · saved · v1"; header toast is plain text `duplicated as "Minimal gated agent copy".` at x1113–1338, no icon, no dismiss. Versions panel shows "1 stored". Canvas re-fitted (cards ~150px). Library: "Minimal gated age… v1 DRAFT" (selected; "copy" truncated away) and "Minimal gated agent v2 DRAFT".
- `A-import-result-1440x900-dark.png` — title "Minimal gated agent · saved · v1"; header toast `imported alice.builder.json as a new draft, "Minimal g…` truncated at x1335. Library now has three rows: "Minimal gated agent v1 DRAFT" (the import), "Minimal gated age… v1 DRAFT" (the copy), and a third clipped at y≈895 with no scrollbar. Two rows carry the identical name.

**Set B** — a full-bleed canvas with a 66px top bar; single green "Start" node 204x130 at x618–822, y420–550.

- `B-agentcanvas-settings-menu-1440x900-dark.png` — "Untitled Agent", save icon 34x34 (x1332–1366), gear 34x34 (x1381–1415). Gear menu x1248–1440, y68–168, **one item**: Load Agents.
- `B-agentcanvas-first-save-name-dialog-1440x900-dark.png` — centred modal "Save New Agents" 444x190 (x498–942, y355–545) on a dimming backdrop; input placeholder "My New Chatflow"; Cancel (text) / Save (disabled). Covers ~95% of the Start node.
- `B-agentcanvas-settings-menu-saved-1440x900-dark.png` — "Round 1 reference flow" + pencil; `</>` icon added. Gear menu x1213–1440, y68–522, 227px wide, 8 rows at 51px pitch: View Messages, View Leads, Configuration, Save As Template, Duplicate Agents, Load Agents, Export Agents, Delete Agents. No Versions/History row. Delete identical in styling to the rest, 51px under Export. Menu right edge flush with x1440.
- `B-agentcanvas-after-export-1440x900-dark.png` — pixel-identical to the saved canvas with the menu closed. No toast, filename, or state change.
- `B-agentcanvas-save-as-template-dialog-1440x900-dark.png` — modal titled "Export As Template" (menu said "Save As Template"), 600x610 (x420–1020, y145–755): Name*, Description, Badge, Usecases; Cancel / Save Template (filled blue). Covers the Start node 100%.
- `B-agentcanvas-delete-confirm-dialog-1440x900-dark.png` — modal "Delete / Delete Agent Round 1 reference flow?" 444x150 (x498–942, y375–525); Cancel (text) / Delete (**filled blue**, ~75x36 at x858–933, y480–516 — the same blue as Save Template). One click; no scope statement; no undo. Covers ~80% of the Start node.

**Pointer-action counts from a saved document** (S = seen in a capture, I = inferred):

| action | A | B |
| --- | --- | --- |
| (a) see stored versions | 2 — kebab, Versions (S) | **impossible** — no item exists (S) |
| (b) open an older one | +1 — click the v1 row (I: no affordance visible on the row) | n/a |
| (c) get back | 1 — Back to v2 (S) | n/a |
| (d) restore as next head | 1 — Restore v1 (S; whether a confirm follows is not captured) | n/a |
| (e) export | 2 (S; result not captured) | 2 (S; no feedback of any kind) |
| (f) import | 4 — kebab, Import…, pick file, Open (last 2 I) | 4 — gear, Load Agents, pick, Open (last 2 I) |
| (g) duplicate | 2 (S) | 2 (I; result not captured) |
| (h) delete | 3 pointer + typing the 19-character name (S) | 3 (S) |
| precondition on a new flow | none seen | +2 and a modal: save icon → name → Save, before the 8-item menu exists (S) |

A's connected version journey (a→b→c→d) is 5 pointer actions, inside the eight-action budget, and Restore is non-destructive (it creates head+1; v2 stays in the list), so a wrong restore is undone by restoring again. B cannot perform (a)–(d) at all.

## Scores
| dim | A | B | delta (B − A) |
| --- | --- | --- | --- |
| 4 | 7 | 3 | −4 |

A is under the 8 gate on D-3 and D-4. B is beaten by A and has open defects regardless of its absolute score.

## Ranked defects (most damaging first)

D-1. [dim 4] B has no version-history surface: the saved-flow actions menu (8 rows, none named Versions/History) and a save icon that overwrites in place mean (a)–(d) cost ∞ pointer actions and a bad save cannot be walked back; the actions menu, seen in `B-agentcanvas-settings-menu-saved-1440x900-dark.png` at 1440x900, zoom n/a (Start node 204x130px); 0 of 4 version actions reachable. A 7 / B 3 / −4.

D-2. [dim 4] B's delete confirmation is one click on a filled-blue "Delete" (75x36 at x858–933, y480–516) in the same primary blue as "Save Template" and "Save", with no type-to-confirm, no statement of what else is removed and no undo, whereas A disables Delete until the 19-character document name is typed and names what goes with it; the delete dialog, `B-agentcanvas-delete-confirm-dialog-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

D-3. [dim 4] A's read-only state is contradicted by its own document bar: while viewing v1 the status chip reads "✓ saved · v1" in the identical style to the editable "saved · v2", Publish stays full-colour and enabled-looking (x965–1060, y66–102), the palette is undimmed and node cards show no lock — the only read-only cues are a 39px banner (y178–217) and greyed inspector fields 700px away on the right; the document bar, `A-version-viewing-v1-read-only-1440x900-dark.png`, 1440x900, node cards 186px. A 7 / B 3 / −4.

D-4. [dim 4] A's docked surfaces stack and push the canvas down without re-fitting: the Versions panel (y120–245, 125px) alone hides Result and Transform 1 below the y818 canvas bottom (2 of 5 nodes), and with the delete strip docked beneath it (y250–355, 105px) the canvas starts at ~y520 and only Request and Confirm remain (3 of 5 hidden) — the operator confirming a delete cannot see most of the graph they are deleting; the version list and the delete strip, `A-version-browser-head-1440x900-dark.png` and `A-delete-confirm-docked-1440x900-dark.png`, 1440x900, node cards 186px. A 7 / B 3 / −4.

D-5. [dim 4] B's every document-action dialog is a centred modal on a dimming backdrop that covers the graph: "Export As Template" is 600x610 (x420–1020, y145–755) and covers the only node (x618–822, y420–550) 100%; the delete dialog (y375–525) covers ~80% of it; the first-save dialog (y355–545) ~95%; the dialogs, `B-agentcanvas-save-as-template-dialog-1440x900-dark.png`, `B-agentcanvas-delete-confirm-dialog-1440x900-dark.png`, `B-agentcanvas-first-save-name-dialog-1440x900-dark.png`, 1440x900, Start node 204x130px. A 7 / B 3 / −4.

D-6. [dim 4] A's version rows cannot be told apart by what they display: v1 and v2 both read "3 Sept, 00:19" (minute resolution), both carry DRAFT, neither has a label, message or node count, and the only differing value is "1.4 KB" vs "1.2 KB" in ~11px right-aligned text at x1020–1050 — choosing which to restore is guesswork; the version list, `A-version-browser-head-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

D-7. [dim 4] A's library is ambiguous after duplicate + import: the import keeps the exact original name so two rows read "Minimal gated agent" distinguished only by a ~10px v1/v2 badge, the duplicate's row truncates to "Minimal gated age…" (~110px of text) so the word "copy" is lost, and a third row is clipped at y≈895 with no scrollbar — the delete target is not identifiable from the list; the saved-graphs library, `A-import-result-1440x900-dark.png` and `A-duplicate-opened-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

D-8. [dim 4] B's export gives zero on-screen feedback — the after-export capture differs from the saved canvas only by the closed menu; no toast, no filename, no failure state, so a failed export is indistinguishable from a successful one; the canvas, `B-agentcanvas-after-export-1440x900-dark.png`, 1440x900, Start node 204x130px. (A's export result was not captured, so A is not credited either.) A 7 / B 3 / −4.

D-9. [dim 4] A's success feedback is a bare text string in the header with no icon, no dismiss and no action, and it truncates: `imported alice.builder.json as a new draft, "Minimal g…` (x1047–1335, ~288px) cuts off the new document's name — the one fact needed to find it in a library that now has two rows with that name (D-7); the header toast, `A-import-result-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

D-10. [dim 4] Both menus put Delete directly under a safe item with identical styling, no separator and no destructive colour: A "Delete…" 34px below "Duplicate" (x745–957, y248–282); B "Delete Agents" 51px below "Export Agents" (x1213–1440, y447–497). A's type-to-confirm catches the slip; B's one blue click (D-2) does not; the actions menus, `A-document-menu-1440x900-dark.png` and `B-agentcanvas-settings-menu-saved-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

D-11. [dim 4] B's dialogs are titled differently from the items that open them: "Save As Template" opens "Export As Template"; "Save New Agents" carries the placeholder "My New Chatflow" — an operator checking they hit the right row sees a different noun and has reason to cancel; the dialogs, `B-agentcanvas-save-as-template-dialog-1440x900-dark.png` and `B-agentcanvas-first-save-name-dialog-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

D-12. [dim 4] B's actions menu on an unsaved flow has one row (Load Agents, x1248–1440, y68–168), so export, duplicate and delete are unreachable until a first save: +2 pointer actions and a 444x190 modal before the 8-row menu exists; the actions menu, `B-agentcanvas-settings-menu-1440x900-dark.png`, 1440x900, zoom n/a. A 7 / B 3 / −4.

## Rejected observations
- A's Publish button looks identical with "1 error" in the dock and with "Ready to publish" — real, but it is the publish surface, not the document-actions surface this round scores.
- A's version rows show no click affordance, so (b) is inferred as a row click; I cannot measure a hit target I cannot see (rows are 26–28px tall).
- Whether A's "Restore v1" asks for a confirmation is not captured; the count of 1 may be 2.
- Whether B's "Load Agents" on a saved flow overwrites the current canvas — the verb suggests it, no capture shows it.
- B's menu is flush with the viewport's right edge at x1440; item text keeps ~30px padding, so I cannot confirm anything is clipped.
- A's disabled "Delete" in the delete strip reads as low-contrast dim red on maroon, but I cannot extract reliable colour values from a rendered PNG.
- A's minimap (x903–1087) overlaps the right edge of the Transform 1 card (to x905) in `A-document-menu…` — a canvas-layout issue, not this dimension.
- A 60x22 grey rectangle at x1380–1440, y878–900 in every B capture — unidentified; could be an attribution badge or a rendering artefact.

### Critic 2 of 2 — engineering, rows 11, 12, 14, 16

## Persona
design director, round 1 (engineering, rows 11, 12, 14, 16)

## Commands run and their result lines
(all from `D:/MultiAgentSystem-wt/integration`, `PYTHONPATH="$PWD/src"`, backend `127.0.0.1:8099`; scratch under `.../scratchpad/critic15/`)

- `curl -s -w ' [%{http_code}]' http://127.0.0.1:8099/api/builder/workflows/ug_00000000/versions` → `{"detail":"document not found"} [404]` (worktree code confirmed)
- `python -m unittest discover -s tests -t .` → `Ran 1548 tests in 144.016s` / `FAILED (errors=17, skipped=6)` — all 17 are `OSError: [Errno 22] Invalid argument: 'C:\\Users\\Simon\\AppData\\Local\\Temp\\crewai:39753d906eb75cdb6b088459cfceda20.lock'` raised from `crewai_core/lock_store.py:111` via `crewai/flow/persistence/sqlite.py:74`, in `tests/builder/test_gates.py` (10), `tests/service/test_builder_runner.py` (6), `tests/builder/test_credential_resolution.py` (1). None is a plan-15 module.
- `unittest discover -s tests/builder -t . -p test_export.py` → `Ran 38 tests` / `OK`
- `... -p test_upgrade.py` → `Ran 12 tests` / `OK`
- `unittest discover -s tests/service -t . -p test_builder_import.py` → `Ran 21 tests` / `OK`
- `... -p test_builder_duplicate.py` → `Ran 16 tests` / `OK`
- `... -p test_builder_delete.py` → `Ran 11 tests` / `OK`
- `... -p test_builder_versions.py` → `Ran 14 tests` / `OK`
- `... -p test_builder_export_route.py` → `Ran 13 tests` / `OK`
- `... -p test_additive_migration.py` → `Ran 19 tests` / `OK`
- `... -p test_isolation_matrix.py` → `Ran 16 tests` / `OK`
- `... -p test_run_retention.py` → `Ran 24 tests` / `OK`
- `TEST_DATABASE_URL=postgresql+psycopg://postgres:test@127.0.0.1:5433/postgres python -m unittest tests.pg.test_two_writers -v` → `Ran 5 tests in 50.092s` / `OK`; server answers `PostgreSQL 18.6 (Debian 18.6-1.pgdg13+2)`
- `cd frontend && npm test` → `Test Files  61 passed (61)` / `Tests  1131 passed (1131)`
- `npx vitest run tests/<file>.spec.ts` → versionBrowser `23 passed (23)`; builderPersistence `33 passed (33)`; builderImport `9 passed (9)`; builderExport `15 passed (15)`; documentLifecycle `10 passed (10)`
- `npx playwright test e2e/isolation.spec.ts --reporter=line` → `4 passed (12.9s)`
- `npx playwright test e2e/builder.spec.ts -g "opens a prior version read-only" --reporter=line` → `1 passed (7.0s)`
- knob scan (the multiline regex over `config.py` + `service/app.py`) → `41`; `docs/tech-stack.md:306` reads `## 6. Environment knobs — there are forty-one`
- `git diff --stat main -- frontend/tests/builderPersistence.spec.ts` → empty (unchanged); `git ls-files .agent/plans/15-persistence.md` → tracked; `git status --short` → only `?? benchmarks/rounds/` and `?? frontend/e2e/_round1_capture.spec.ts`
- `.github/workflows/ci.yml:93-113` → job `postgres`, `image: postgres:18`, `TEST_DATABASE_URL: postgresql+psycopg://...`, guarded `if: github.ref == 'refs/heads/main'`
- `probe.sh` (curl as `X-Synthetic-User: critic_alice`, `critic_bob`, and no header) — result lines:
  - alice `POST /api/builder/credentials` → `[201]` `cr_9d849f1f`; create doc with `scoper.config.credential_id=cr_9d849f1f` → `[201]` `ug_6730814a`
  - `GET /export` → `[200]`, `content-disposition: attachment; filename="Test graph.builder.json"; filename*=UTF-8''Test%20graph.builder.json`; `secret bytes in export: 0   cred id in export: 0   user id in export: 0`; `needs_credentials ['scoper']`; document keys `['schema','name','input_field','nodes','edges','joins']`
  - alice import → `[201]` `ug_c4c3d1f4 draft v1`; bob import → `[201]` `ug_7d22f728 draft v1`; `export(orig) == export(alice import): True`; `export(orig) == export(bob import): True`; stored orig vs stored import differ only in `id` and `nodes[scoper].config.credential_id` (`cr_… → null`)
  - duplicate own → `[201]` `ug_a8c767a8 v1 draft 'Test graph copy'`, `duplicate identical minus id/version/name: True`; bob duplicate → `{"detail":"document not found"} [404]`; nobody duplicate → `[404]`
  - bob/nobody `GET`, `/versions`, `/export`, `PUBLISH`, `DELETE`, `PUT` on alice's doc → all `{"detail":"document not found"} [404]`; bob list → only his own; bob `GET /credentials/cr_9d849f1f` → `{"detail":"credential not found"} [404]`; nobody → `{"detail":"sign in to use this endpoint"} [401]`
  - stale save ×2 → `{"detail":"document ug_6730814a is at version 2, not 1; reload it before saving again"} [409]` both times; `expected_version=7` → `... is at version 2, not 7 ... [409]`; no `expected_version` → `[422]` with a sentence
  - `GET ?version=99` → `{"detail":"document not found"} [404]`; same for `/export?version=99`, `/duplicate?version=99`, `/publish?version=99`; `?version=0` / `?version=abc` → FastAPI list `[422]`
  - restore: `GET ?version=1`, `PUT` with `expected_version=2` → `[200]`, `restored version 3 expected head+1 = 3`, `restored document == v1 document (minus version): True`
  - publish → `[200]`; delete → `{"detail":"document ug_6730814a is published and registered as a launchable workflow, so it cannot be deleted; save a new version to return it to draft, then delete it"} [409]`; save → v4 `draft`; `/versions` → `[(4,'draft'),(3,'published'),(2,'draft'),(1,'draft')]`; delete → `[204]`; `GET`, `/versions` → `[404]`
  - second run of the same sequence with alice's header on the graph endpoint: `publish [200] graph [200] bob graph [404] nobody graph [404]`, then `save [200] graph still live [200] [(2,'draft'),(1,'published')] delete [204] graph after delete [404]`
  - import `{"foo":1}` → `{"detail":[{"type":"missing","loc":["body","export"],"msg":"Field required","input":{"foo":1}},…]} [422]`; raw document (no envelope) → `[422]` with the **entire document echoed** under `"input"`; `needs_credentials` ×500 → `[422]` echoing the 500-item list; `export=builder.flow/v9` → `{"detail":"export must be one of ['builder.flow/v1', 'builder.flow/v2']; this file says 'builder.flow/v9'"} [422]`; `export=v2, document.schema=v2` → `{"detail":"schema: Value error, unknown document schema 'builder.flow/v2'; this service compiles builder.flow/v1"} [422]`; `export=v2, document.schema=v1` → `[201]`; envelope naming bob's `cr_38a45053` imported by alice → `[201]`, `needs_credentials ['scoper']`, stored `credential_id [None]`; alice `POST /validate` naming bob's id → `credential-missing … not in your vault … [200]`; alice publish of a doc naming bob's id → `[422]` same sentence; export of it → `0` occurrences of the id; envelope smuggling `document.user_id`/`id` → `[201]`, server-minted id
  - anonymous `POST /workflows` → `[201]` `ug_8c0b01be`; anonymous import → `[201]`; bob `PUT` over the anonymous row → `[200]` `version 2`; alice `PUT` → `[200]`; alice `DELETE` → `[204]`; anonymous `GET` afterwards → `[404]`; anonymous export/duplicate/delete of an anonymous row → `[200] [201] [204]`
  - 16-node validator template (`frontend/tests/fixtures/builderValidatorTemplate.json`, 16 nodes / 22 edges / `joins {score: all}`): `export(orig).document == export(bob import).document: True`; stored orig vs bob import differ only in `['id']`; `duplicate identical minus id/version/name: True`; `restored (v3) == v1 minus version: True`

## Capture review
- **A** (7 PNGs, 1440×900 dark): `A-canvas-document-bar` — "Flow builder", document bar "Minimal gated agent · unsaved changes · Ctrl+S", palette of seven kinds, worst-case cost strip, Ready to publish. `A-document-menu` — overflow menu: Versions / Export .builder.json / Import .builder.json… / Duplicate / Delete…. `A-version-browser-head` — a VERSIONS dock (2 stored, v2 HEAD DRAFT, v1 DRAFT, sizes 1.4 KB / 1.2 KB). `A-version-viewing-v1-read-only` — banner "Viewing v1 of v2 — read-only. Restore it to make it the next version, or go back to v2." with Back to v2 / Restore v1; inspector fields greyed. `A-duplicate-opened` — "Minimal gated agent copy · saved · v1", notice "duplicated as “Minimal gated agent copy”." `A-import-result` — notice "imported alice.builder.json as a new draft, “Minimal g…" (truncated at 1440). `A-delete-confirm-docked` — docked strip: "Delete **Minimal gated agent** and every stored version of it? Deleting a published graph unregisters it; running graphs are not affected. Type … to confirm." with Keep it / Delete.
- **B** (6 PNGs): a Flowise-style Agentflow canvas ("Round 1 reference flow", Start node, cog menu with View Messages / View Leads / Configuration / Save As Template / Duplicate Agents / Load Agents / Export Agents / Delete Agents), a centred modal "Delete Agent Round 1 reference flow?", a modal "Save New Agents" with "My New Chatflow" placeholder, an "Export As Template" modal (Name/Description/Badge/Usecases), and the post-export canvas.
- **Belief:** A is the product under review — its copy is character-for-character the text in `frontend/src/components/builder/BuilderView.vue:1464-1466` and the routes it exercises (versions, export/import envelope, duplicate, docked delete) are the ones under `/api/builder/workflows`. B is the shipped competitor (Flowise Agentflow: modal dialogs, "Chatflow" vocabulary, no version browser, no needs-credentials notice).

## Scores
| dim | ours | ref | delta | carried by |
| --- | --- | --- | --- | --- |
| 11 | 10 | 10 | 0 | — (nothing landed; see D-7 for the one identity-adjacent leniency, filed under 12) |
| 12 | 7 | 10 | 3 | D-1, D-2, D-3 |
| 14 | 9 | 10 | 1 | D-4 |
| 16 | 8 | 10 | 2 | D-5, D-6 |

## Ranked defects (most damaging first)
D-1. [dim 12] Any signed-in user can overwrite, publish and delete an unowned document, and on this backend anybody can create one: `store._visible_to` (`src/brief_crew/builder/store.py:716-724`) is the sole gate on `load`, `save` (:483), `mark_published` (via `load`, :541) and `delete` (:586), so "an unowned row is readable by everyone" silently became "controllable by everyone"; seen with `POST /api/builder/workflows` (no header) → `[201] ug_8c0b01be`, then bob `PUT …/ug_8c0b01be` → `[200] version 2`, then alice `DELETE …/ug_8c0b01be` → `[204]`, then anonymous `GET` → `[404]`; `tests/service/test_isolation_matrix.py:266-284` covers unowned rows for GET/versions/export only, never PUT/DELETE/publish. ours 9 / ref 10 / delta 1. *(Filed under 14 in the table — it is the isolation gap; listed first because it destroys data.)*

D-2. [dim 12] An unknown version on the caller's own document is refused with the wrong sentence: `_guarded` (`src/brief_crew/service/builder_api.py:1027-1028`) flattens every `DocumentNotFound` to the constant `"document not found"`, discarding the version the store put in the message (`store.py:89-95`); `GET /api/builder/workflows/ug_6730814a?version=99` → `{"detail":"document not found"} [404]` while that document's head was 2 and on screen, and the client renders exactly that string (`BuilderView.vue:575 say(messageOf(error, …))`, `serverLimits.ts:49`). Same for `/export?version=`, `/duplicate?version=`, `/publish?version=`. The tests pin only the status (`tests/service/test_builder_duplicate.py:101-104`, `test_builder_export_route.py:148-150`). ours 7 / ref 10 / delta 1.

D-3. [dim 12] A malformed or foreign import file is refused with FastAPI's raw pydantic list that echoes the whole body back: `BuilderImportRequest` (`builder_api.py:262-285`) is typed, and `service/app.py` registers no `RequestValidationError` handler (grep `exception_handler` → nothing; `_validation_detail` at `app.py:448` serves only the WebSocket), so `POST /api/builder/workflows/import` with a raw document (no envelope) answered `[422]` with the entire document under `"input"`, `{"foo":1}` answered three fragments, and `needs_credentials` ×500 echoed all 500 entries — the very reflection the module's own docstring (`builder_api.py:196-201`) says it avoids for `document`. The client's `readErrorDetail` (`src/data/serverLimits.ts:50-54`) reduces this to `Field required; Field required; Extra inputs are not permitted`, naming nothing. ours 7 / ref 10 / delta 1.

D-4. [dim 12] The docked delete copy promises the opposite of what the server does, and the 409 it then shows is defeated by its own remedy: `BuilderView.vue:1464-1466` and `TemplateGallery.vue:345` read "Deleting a published graph unregisters it; running graphs are not affected" (capture `A-delete-confirm-docked-1440x900-dark.png`, 1440×900), while `DELETE` on a published head answers `[409] … cannot be deleted; save a new version to return it to draft, then delete it` (`builder_api.py:801-809`); following that sentence — `save [200]` → `/versions [(2,'draft'),(1,'published')]`, `GET /api/workflows/{id}/graph` still `[200]` — then `delete [204]` unregisters the still-live, still-launchable v1 (`graph after delete [404]`), which is the exact hazard the 409's docstring names (`:783-787`). One condition (`stored.status == STATUS_PUBLISHED`, `:801`) instead of `registered_workflow(id) is not None` alone is what makes the guard one save deep. ours 7 / ref 10 / delta 1.

D-5. [dim 16] Criterion 7 is ticked "done" while its text is false: it says `create_all` "yields all six new tables"; the DDL declares five (`GAUNTLET_TABLES`, `src/brief_crew/service/persistence.py:406-412`) and the test asserts five (`tests/service/test_additive_migration.py:186 test_all_five_tables_arrive_with_their_indexes_and_constraints`). The `## Status` table records "done, one correction … The test is right" and leaves the criterion unamended. ours 8 / ref 10 / delta 1.

D-6. [dim 16] Criterion 8 is ticked "done" while its text is false: it says the knob scan "answers forty"; my run of the scan answers `41`, and `docs/tech-stack.md:306` says forty-one. Criterion 10 is likewise ticked with a self-declared narrowing ("test-inputs row is covered at the table level … Stage 1 has no route"). The Status stamp is "Criteria complete", not "Built", which is honest — but every row is ticked. ours 8 / ref 10 / delta 1.

D-7. [dim 12, not scored] An envelope whose `export` says `builder.flow/v2` over a document whose `schema` says `builder.flow/v1` imports `[201]`: `builder_api.py:607` checks `export` for membership in `KNOWN_SCHEMAS` only and never against `document.schema`, so a hand-edited mismatch is accepted silently. No harm shown (the document's own schema is what is validated), so it carries no point; recorded because "foreign file" is in this row's remit.

## Criteria I could not verify, and why
- **8, second half** ("regenerated in the same commit"): needs `git log`, which I was told not to read. The two figures agree at HEAD (41 / forty-one); the commit boundary is not verified.
- **10** ("passes with the table in D9 exactly"): D9 lives in the plan's Design section, which I was told not to read. What I can say: 16 tests pass, and the Status itself says one row is covered at a different level than a route.
- **5, "from the UI"**: the row-and-versions removal is verified server-side by curl (`GET` and `/versions` → 404 after `DELETE`) and client-side by `documentLifecycle.spec.ts` (jsdom, 10 passed); no E2E I ran presses Delete in a browser.
- **9, "CI gains a job"**: the job exists in `ci.yml:93-113` with `postgres:18` and `TEST_DATABASE_URL`; that it runs green in GitHub is not something I can check without the network, and it is `main`-only.

## Rejected observations
- The 17 errors in the full Python run are a Windows path defect in `crewai_core/lock_store.py:111` (a lock filename containing `:`), not in this worktree's code; every plan-15 module passes alone. Recorded for whoever quotes "the suite is green" on this machine.
- `?version=0` / `?version=abc` answer FastAPI's default list; same root cause as D-3, not counted twice.
- `A-import-result` truncates its own notice at 1440 wide ("Minimal g…") — a visual-critic matter, not engineering.
- `GET /versions` can show two `published` rows at once (head and a still-registered older version) — the source explains it (`builder_api.py:961-976`) and my probe showed it exactly as described; I could not turn it into a wrong answer.
- Export drops `budget` and nulls `credential_id`, so a stored import differs from its source in those two places — both are the criteria's own demand, not a defect.
- The `X-Synthetic-User` path, credential vault masking and the 401 for anonymous credential reads all behaved; they belong to plan 01 and I found nothing to land there.

## Result

| dim | ours | ref | delta | gate (8) | critic |
| --- | ---: | ---: | ---: | --- | --- |
| 4 | 7 | 3 | −4 | **not met** (ours under 8; beats the reference) | visual, blind |
| 11 | 10 | 10 | 0 | met, no defect | engineering |
| 12 | 7 | 10 | 3 | **not met** | engineering |
| 14 | 9 | 10 | 1 | met on score, **one open defect** | engineering |
| 16 | 8 | 10 | 2 | met on score, **two open defects** | engineering |

Twelve located defects on ours, all accepted; none rejected. The ledger
rows in `benchmarks/DEFECTS.md` are **D-15-1 … D-15-6** for the visual
critic's A-side defects (its D-3, D-4, D-6, D-7, D-9, D-10, in that order;
its B-side defects D-1, D-2, D-5, D-8, D-11, D-12 are the reference's and
get no row) and **D-15-7 … D-15-12** for the engineering critic's D-1 … D-6
in its order. The engineering critic's D-7 is recorded above and given no
row, as the critic gave it no score.

**One transposition in the engineering critic's Scores table**, resolved
from its own per-defect lines rather than edited: the table's "carried by"
column names D-1 under dim 12 and D-4 under dim 14, while D-1's heading,
its score line ("ours 9 / ref 10") and its parenthetical ("filed under 14 in
the table — it is the isolation gap") place D-1 under **14**, and D-4's
score line ("ours 7 / ref 10") places it under **12**. The ledger follows
the per-defect lines: D-1 → dim 14, D-2/D-3/D-4 → dim 12, D-5/D-6 → dim 16.

**The engineering critic's full-suite run failed 17 tests that every
earlier run of this tree passed** — the Integrator's (1548 OK) and plan 01's
critic's (1548 OK, an hour earlier, same machine). All 17 are one
`OSError: [Errno 22] Invalid argument` from `crewai_core/lock_store.py:111`
naming a lock file `Temp\crewai:<md5>.lock`, in `test_gates.py`,
`test_builder_runner.py` and `test_credential_resolution.py`, none of them
plan 15's. The critic rejected it as a defect of this plan; every plan-15
module passed alone. **The Integrator reproduced it afterwards and found
the cause, which is environmental and cumulative, not code on this
branch:** on Windows the colon in `crewai:<md5>.lock` makes the lock an
NTFS *alternate data stream* on a zero-byte file `%TEMP%\crewai` (created
2026-08-29 by the first crewai lock ever taken here). Every
`SQLiteFlowPersistence` takes a lock named by its database path, every
persistence test uses a fresh temp path, and nothing ever removes a stream,
so the file had accumulated **2,520** lock streams by the critic's run and
NTFS began refusing new ones — a 13-character stream name still opened, the
37-character hashed ones did not, which is the per-file attribute-list
limit rather than a syntax error. Deleting `%TEMP%\crewai` resets it; the
Integrator did not, because this session fixes nothing and the count is the
evidence. It belongs in `docs/gotchas-and-insights.md` as a test that
passes for the wrong reason for weeks and then fails for a reason that is
not in the tree, and upstream as a `crewai_core` defect on Windows.

Everything the engineering critic scored, it ran itself; its figures agree
with the Integrator's, which it never saw. Its probes went past the suite
in the ways the method wants: the round trip was proved document-equal on
the 16-node validator template as well as the 4-node one, and the two data
defects (D-15-7 and D-15-10) are things no committed test asks.

**Round 1 outcome: the gate is not met.** Dimension 4 is under 8 and
dimensions 12, 14 and 16 carry open rows. `PLANS.md` records round 1 with
twelve open defects; the ledger read top to bottom is the round-2 build
list for plan 15. Neither critic is retired: neither passed the artefact.
