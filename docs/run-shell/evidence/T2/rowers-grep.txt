T2.9 - the two-rower crew and the icon medallion are REPLACED, not duplicated.

REGENERATED BY RV3, fourth pass, branch run-shell/cast at 8ae40ec, 2026-09-05T18:31:21+08:00.
W4 wrote the first copy at 7da9bf5; this is the criterion's grep plus the two
that actually carry the claim, re-run at the branch head from the repo root.

===========================================================================
1. THE CRITERION'S OWN GREP
===========================================================================

$ grep -nE 'node-rower|node-oar' frontend/src/components/WorkflowNode.vue
(no output; exit status 1)

READ THAT ONE CAREFULLY BEFORE TRUSTING IT, as W4's copy said: the classes
the boat actually carried were .node-crew-rower, .node-crew-oar and
.node-crew-hull, and 'node-rower' is not a substring of 'node-crew-rower'.
So the criterion's pattern answers nothing either way. The greps below are
the ones that carry the claim.

===========================================================================
2. THE GREPS THAT ACTUALLY ANSWER IT
===========================================================================

$ grep -rnE 'node-rower|node-oar' frontend/src/
frontend/src/assets/styles/node-card.css:210: * with `node-oar-stroke`, `node-rower-pull` and `node-hull-bob` behind it.

ONE hit in the whole of frontend/src, and it is a COMMENT in
assets/styles/node-card.css recording the retirement - the rule is gone and
its epitaph is not.

$ grep -rnE 'node-crew-rower|node-crew-oar|node-crew-hull' frontend/src/
(no output; exit status 1)

ZERO. Not one of the three real class names survives anywhere in the source.

===========================================================================
3. WHAT REPLACED IT, ASSERTED AT THE SHUTTER
===========================================================================

evidence/T2/node-running.png was captured by a throwaway spec (created, run,
deleted, never committed) which asserted on the running card at the moment
of the screenshot:

    .node-crew-oar        count 0
    .node-crew-rower      count 0
    .node-crew-hull       count 0
    [data-testid=node-agent-character]  count 1

and read the card's .pip as data-character='market evidence analyst',
data-state='working'. One cast, no leftover rowers.

The stage lane's validator boat is out of scope by the criterion's own
sentence and stays.
