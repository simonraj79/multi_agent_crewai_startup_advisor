# Plan 15 — judge round 2

Written 2026-09-03 against `gauntlet/plans` = `56bb8ab` (the last commit of
the round-2 build, above plan 15's `90699e9`; the only commit between them
is a docs reconciliation), in `D:\MultiAgentSystem-wt\integration`. Method:
`benchmarks/README.md`. Applicable rubric rows: **4** (inspector ergonomics
— here the document lifecycle and the version browser, criterion 15.4,
scored against Flowise), **11**, **12**, **14** and **16** (scored against
test output the critic re-runs itself). Round 1 is `15-1.md`; the twelve
rows it opened are `D-15-1` … `D-15-12` in `benchmarks/DEFECTS.md`, and
every one was still `open` when this round started, because closing one
is the critic's job and not the builder's.

`PLANS.md` was set to *In judge (round 2)* for plans 01 and 15 in
`d326b00` before any capture was taken or any critic invoked. (The row
verifiers' verbatim output below names that commit as `44f3582`: its
hash before a rebase added the repository's commit trailer after the
round; the tree is byte-identical.)

## The flip

Flipped with `secrets.randbits(1)` at **2026-09-03T07:43:34Z**, before any
capture was shown to any critic and before either critic existed:

| label | is |
| --- | --- |
| **A** | the reference — Flowise 3.1.4 (`flowiseai/flowise:3.1.4`, Docker; round 1's captures, reused) |
| **B** | ours — `gauntlet/plans` at `56bb8ab` |

The critics were never told which is which. Capture files were renamed to
`A-*` / `B-*` and handed over from a directory that names neither product.
(Round 1's flip for this plan came out the other way, A = ours.)

## Persona

Round 2: **staff frontend engineer**. Each critic's prompt opened with the
README's sentence with that persona in place of the design director: *You
are a hostile staff frontend engineer who has shipped a competitor to this
product. You are looking for reasons to reject. Vague praise is a failure
of your job.*

**Two critics, one persona**, as in round 1. Row 4 is scored blind from the
captures by a critic that runs nothing and reads nothing else; rows 11, 12,
14 and 16 are scored by a second critic that re-runs every command in the
worktree and therefore cannot be blind about which tree is ours. Neither
critic saw round 1's output, the ledger, the plan's Status or Design
sections, any commit message, or this file.

**A third agent per plan, new this round: the row verifier.** It received
the opposite of what the critics saw — the plan's twelve ledger rows
verbatim and the fixing commit(s) the plan's Status names per id — and
re-ran each row's own probe against the same fresh backend, reporting with
output whether the defect is absent or still there. Its output is below the
critics', verbatim. A row closes only on the verifier's *absent* AND the
critic not landing it again AND the dimension scoring at or above the
reference; the closing decisions are the Integrator's and are in the
Result.

## What the critics received, and nothing else

1. `.agent/plans/15-persistence.md` — the *Acceptance criteria* section
   only (11 numbered items, three of them now carrying a dated amendment
   under the original sentence), copied verbatim to a file.
2. Our captures (**B**), 1440×900, dark, **thirteen**: round 1's seven
   surfaces by the same filenames — the canvas with the document bar, the
   document bar's overflow menu, the version browser at head, the version
   browser viewing v1 of v2 read-only, the docked delete confirm stacked
   under the open version browser, the duplicate opened, and the result of
   importing an exported file — plus six for the surfaces a round-2 commit
   changed: the version rows after a restore (three rows, three origins),
   the palette's saved-graphs library after a duplicate and an import, the
   gallery's library with the same three rows, the overflow menu on a
   published document, the docked delete strip on a published document
   after the server refused the delete, and the gallery's own delete on
   the published row after the same refusal.
3. The reference captures (**A**), 1440×900, dark, on the Agentflow v2
   canvas — round 1's six, reused unchanged: the settings menu on an
   unsaved flow, the first-save name dialog, the settings menu on a saved
   flow, the canvas after Export, the "Export As Template" dialog and the
   delete confirmation (`docs/flowise-notes.md` §3).
4. Rubric rows 4, 11, 12, 14 and 16, copied verbatim, with the gate and the
   defect rule, and for the engineering critic the commands the rows'
   evidence comes from: the Python suite under `PYTHONPATH=<worktree>/src`,
   `npm test`, `vue-tsc -b --force` and `npm run build`,
   `e2e/isolation.spec.ts` and the version-browser step of
   `e2e/builder.spec.ts` against the free backend on :8099, the suite
   listing, and `tests/pg/test_two_writers.py` under `TEST_DATABASE_URL`.

## Where the method could not be followed, and why

- **1440×900 dark only.** No 390×844 project and no light theme exist yet;
  both belong to plan 02. The README's viewport and theme axes have one
  value each this round, as in round 1.
- **No `capture.spec.ts` exists** (planned in 02). Ours was captured by a
  throwaway Playwright spec (`frontend/e2e/_round2_judge_capture.spec.ts`,
  never committed, deleted after the run so the suite lists 34 tests
  again) against `SYNTHETIC=1 SYNTHETIC_BRANCH_DELAY_SECONDS=5 PORT=8099`
  with `CREDENTIALS_MASTER_KEY` set and `PYTHONPATH` pointing at the
  worktree, on a backend started fresh for the pass. The captures are under
  `benchmarks/ours/15/2/` and are gitignored. The builder's own untracked
  `frontend/e2e/_round2_capture.spec.ts` and its six PNGs under
  `docs/comparison/ours/round2/` (named `A-*` on the builder's assumption)
  were not used and the spec was deleted; the PNGs are ignored by the
  `*.png` rule and left where they were.
- **The reference was not re-run.** Flowise 3.1.4 has not changed since
  round 1, so its eleven captures were reused. Two of the surfaces our
  round-2 commits changed have a Flowise counterpart with no capture — a
  save snackbar beside our header toast, and a flow list holding a
  duplicated name beside our library. The Integrator chose not to boot
  Flowise for them: round 1 already scored Flowise's feedback surface from
  its after-export canvas (no toast at all) and its list was not part of
  either round's surface set, and the rows those two surfaces carry
  (`D-15-4`, `D-15-5`) close or stay open on *our* capture showing the
  defect absent, not on the reference's score. So A's toast and A's list
  are scored from the captures A has, and the gap is recorded here rather
  than papered over.
- **Rows 11 and 12 belong to plans 09 and 12**, whose harnesses
  (`tests/builder/test_failure_modes.py`, the twenty-fixture determinism
  harness) do not exist yet. As in round 1 they were put to the critic as
  the rubric words them, and what the critic can score today is the part
  plan 15 owns — that the export/import/duplicate/restore paths give an
  identical document back (11) and that their refusals are legible and
  recoverable (12).
- **The engineering rows have no reference product to score.** `ref` for
  rows 11, 12, 14 and 16 is the bar the criteria set, 10 by definition, and
  every point of `ours` below it must be carried by a located defect.
- **The engineering critic re-runs the suites in our worktree**, so for
  the engineering rows it necessarily knows which tree is ours; only the
  captures are blind.
- **The backend was proved to serve the worktree, not the main tree**,
  before any capture and again before each critic:
  `POST /api/builder/workflows/ug_00000000/unpublish` on :8099 answered
  `{"detail":"document not found"} [404]`, a route that exists only on
  this branch; the main tree has no such route. The engineering critic
  ran its own probe (`/versions`) and recorded the answer.
- **Engineering critics and row verifiers ran one at a time** on the
  shared in-memory backend, because the isolation E2E deletes every row
  its two users own; the backend was restarted fresh before the capture
  pass, before the plan-15 engineering critic, and before each row
  verifier. The blind visual critic runs nothing and ran alongside.
- **Engineering defects carry a command and an output line** in place of
  the README's viewport and zoom, which have no meaning for a refusal seen
  in a probe; a defect seen in a capture carries the capture's viewport and
  zoom as written.

## Critic output, verbatim

### Critic 1 of 2 — visual, row 4, blind (A = reference, B = ours; the critic did not know)

## Persona
staff frontend engineer, round 2 (visual, dimension 4 only)

## Capture review

Conventions used below: coordinates are x,y in px on the 1440x900 frame, read off the capture; S = seen in a capture, I = inferred; one pointer action = one click or tap; an OS file picker is counted as 2 pointer actions (select, Open); keystrokes are noted but never counted against the pointer budget. Neither product shows a zoom readout, so node-card widths are given instead.

**A set**

- `A-agentcanvas-settings-menu-1440x900-dark.png` — never-saved doc "Untitled Agent" (title x75–240, y32). Header controls: save icon x1333–1365, y17–49 (purple), gear x1383–1415. Gear menu open, panel x1248–1440, y70–168, ONE item: "Load Agents" (y117). Canvas: a single "Start" node, card x620–822 (202px wide), y420–550. Bottom control bar x652–820, y845–870 (zoom in, zoom out, fit, lock, two more); minimap x1226–1424, y733–880. No library, no saved/unsaved text anywhere.
- `A-agentcanvas-settings-menu-saved-1440x900-dark.png` — saved doc "Round 1 reference flow", rename pencil x350–378. Header: code icon x1283–1315, save x1333–1365, gear x1383–1415. Gear menu x1214–1440, y70–522, eight items at 51px pitch: View Messages y117, View Leads y167, Configuration y218, Save As Template y269, Duplicate Agents y320, Load Agents y370, Export Agents y421, Delete Agents y472. No separator anywhere; Delete Agents carries the same icon weight and colour as the rest. No Versions/History item, no Unpublish item. The save icon is pixel-identical to the never-saved capture. Card 202px.
- `A-agentcanvas-first-save-name-dialog-1440x900-dark.png` — centred modal "Save New Agents" x499–941 (442px), y355–545, over a dimmed canvas; input x522–918, y421–471, placeholder "My New Chatflow"; Cancel x807–852; Save x870–933 disabled until a name is typed. The modal fully covers the Start node (its green edge shows at y540–548). Two floating round icons appear at x1323–1355 and x1383–1415, y94–126.
- `A-agentcanvas-save-as-template-dialog-1440x900-dark.png` — modal titled "Export As Template" x421–1019 (598px), y147–755 (608px), reached from the menu item labelled "Save As Template". Fields: Name (required, prefilled) y247–286, Description y346–406, Badge y466–505, Usecases y566–613 with a hint line. Cancel x820–864; "Save Template" x881–1012, y711–745, primary blue. Three names for one action.
- `A-agentcanvas-delete-confirm-dialog-1440x900-dark.png` — modal "Delete" x499–941 (442px), y375–523; body "Delete Agent Round 1 reference flow?"; Cancel x797–841; "Delete" x859–933, y481–515, primary blue — the same blue as Save Template. No type-to-confirm, no mention of undo. Covers the Start node (green edge at y520–548).
- `A-agentcanvas-after-export-1440x900-dark.png` — state after choosing Export Agents: identical to the saved capture with the menu closed. No toast, no banner, no change in any header control. A download, if one fired, would be in cropped browser chrome.

**B set**

- `B-canvas-document-bar-1440x900-dark.png` — never-saved doc. Document bar y65–102: back x236–268, title "Minimal gated agent" x285–452, pencil x470–486, "unsaved changes · Ctrl+S" x495–640 (amber mono), undo x805–836 / redo x845–876 (disabled), keyboard x885–916, kebab x925–956, "Publish" x965–1059 (teal), chevron x1069–1100. No Save control. Palette x0–235 ends with LIBRARY / "Saved graphs" / "No saved graphs yet" at y820. Inspector x1100–1440, "GRAPH" (Name 19/80, Run input, Fan-in). Cost strip y128–280 ($0.03 / $10.00 ceiling, four count bars). Four cards 186px wide (Request x575–761, y318–378). Minimap x903–1087, y668–820 with ×. Problems dock y835–900 "✓ Ready to publish". App bar: Build/Run toggle x1157–1284, y13–39; account chip x1303–1422.
- `B-document-menu-1440x900-dark.png` — "saved · v2" (green, x478–553). Kebab menu x745–956, y110–330, 34px pitch: Versions y128, Export .builder.json y162, Import .builder.json… y196, Duplicate y230, Unpublish y264 (disabled on a draft), rule y287, Delete… y309. No Save item. Library card "Minimal gated agent · v2 · DRAFT" x12–222, y813–862, highlighted. A fifth node "Transform 1" x762–~905, y707–790 runs under the minimap (x903–1087, y650–805); its kind label reads "05 · TR". Inspector shows that node with a red `node-unreachable` error; dock "1 error".
- `B-document-menu-published-1440x900-dark.png` — "saved · v3 · v3 is live" (green dot, x568–638). Publish reads "Republish" x949–1059; kebab shifted to x910–941; menu x730–940, same six items, Unpublish now enabled. Library: "Minimal gated agent · v3 · PUBLISHED" (green pill) y813–862, then "Minimal gated agent imported" cut at y881–900.
- `B-version-browser-head-1440x900-dark.png` — Versions panel docked at y120–245 inside the document column (x236–1100): "VERSIONS / 2 stored", × x1028–1059, y125–156; rows at 33px pitch: v2 HEAD DRAFT "Minimal gated agent · 5 nodes · saved · just now · 1.4 KB" y178–204 highlighted; v1 DRAFT "· 4 nodes · created · just now · 1.2 KB" y223. Cost strip pushed to y262–410; canvas re-fits, cards 136px wide (Request x532–668, y443–487), subtitle text about 5px.
- `B-version-viewing-v1-read-only-1440x900-dark.png` — bar tinted amber, lock x275–291, "viewing v1 of v2 · read-only" x500–688. Panel adds a banner row y180–215: "Viewing v1 of v2 — read-only. Restore it to make it the next version, or go back to v2." with "Back to v2" x838–935 and "Restore v1" x947–1049 (teal). Rows v2 y244, v1 y277 highlighted. Palette dimmed behind an amber banner x12–222, y150–208. Inspector inputs disabled. Publish dimmed. Undo/redo disabled. Cards 116px wide (Request x610–726, y491–531) with a lock glyph each; title about 7px, subtitle about 4px. Dock still reads "✓ Ready to publish" (x344–464, y851).
- `B-version-rows-after-restore-1440x900-dark.png` — toast x990–1285, y8–44 "restored v1 as v3 — head is one undo away." with ×; Build/Run moved to x835–963. Bar "saved · v3". Panel "3 stored": v3 HEAD DRAFT "4 nodes · restored from v1 · just now · 1.2 KB" y190; v2 y223; v1 y256. Undo enabled. Cards 130px (Request x603–733).
- `B-delete-confirm-docked-1440x900-dark.png` — on the v2 DRAFT with the Versions panel still open (y120–245); delete strip y250–355, red tint: "Delete **Minimal gated agent** and every stored version of it? A published graph cannot be deleted; unpublish it first, then delete it. Type **Minimal gated agent** to confirm." Input x276–765, y307–341; "Keep it" x772–841; "Delete" x849–916 dimmed until the 19-character name matches. Cards 100px (Request x568–668, y543–572); text about 4px.
- `B-delete-refused-published-docked-1440x900-dark.png` — published v3; strip y120–232: "Not deleted — it is still published." (y136), then "document ug_309cd317 is published - v3 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it" (y163, red); "Unpublish" gradient button x276–769, y181–216 (493px wide); "Keep it published" x777–907. Cards 146px (Request x595–741).
- `B-duplicate-opened-1440x900-dark.png` — toast x990–1285 "duplicated as 'Minimal gated agent copy'."; Build/Run at x843–971. Bar "Minimal gated agent copy · saved · v1". Library: the copy highlighted y813–862; the original cut at y881.
- `B-import-result-1440x900-dark.png` — two-line toast x848–1285, y8–44 "imported alice.builder.json as a new draft, 'Minimal gated agent imported'."; Build/Run at x702–830 (−455px). Bar "Minimal gated agent imported · saved · v1". Library: imported highlighted, "Minimal gated agent copy" cut at y881.
- `B-gallery-delete-refused-unpublish-1440x900-dark.png` — gallery route. "START FROM / A shape that already works" y93–119; "Import .builder.json" x1075–1228, y101–133; four template cards x212–455 / 470–713 / 728–971 / 986–1229, y147–595. "YOUR GRAPHS / Saved here" y633–659. Row "Minimal gated agent · PUBLISHED · v3 · 3 Sept, 07:47" y688–752, trash x1187–1219, y704–736. Red-bordered strip y763–880: "Not deleted — it is still published." y781, Unpublish x226–334 and "Keep it published" x343–473 at y800–837, THEN the server sentence at y856 — below the buttons, the reverse of the docked strip. Next row starts at y890, cut.
- `B-gallery-library-after-duplicate-and-import-1440x900-dark.png` — same gallery, now three rows: "…imported · DRAFT · v1 · 3 Sept, 07:47" y688–752, "…copy · DRAFT · v1 · 3 Sept, 07:47" y764–828, "Minimal gated agent · DRAFT · v3 · 3 Sept, 07:47" y840–900 cut at the viewport edge. Three identical timestamps to the minute. Each row: a trash icon only; no duplicate, export or versions affordance.
- `B-palette-library-after-duplicate-and-import-1440x900-dark.png` — canvas on the imported doc, no toast, Build/Run back at x1157–1284. Library: imported highlighted y813–862, "Minimal gated agent copy" cut at y881–900; no scrollbar visible in the palette.

**Pointer actions from a saved document open on the canvas**

| job | A | B |
| --- | --- | --- |
| see stored versions | none — no Versions item in the 8-item menu (S) | 2: kebab → Versions (S) |
| open an older one | none (S) | 3: + the v1 row (row highlighted in the viewing state, S; the click itself I) |
| get back | none (S) | 4 cumulative; 1 from the viewing state via "Back to v2" (S) |
| restore as next head | none (S) | 4 cumulative; 1 from the viewing state via "Restore v1"; result "v3 · HEAD · restored from v1" (S); no confirm step, undo offered in the toast (S) |
| export | 2: gear → Export Agents (S); no on-screen result afterwards (S) | 2: kebab → Export .builder.json (S); result uncaptured (I) |
| import | 4: gear → Load Agents (S) → picker ×2 (I); result, and whether it replaces the open canvas, I | 4 from the canvas: kebab → Import… (S) → picker ×2 (I); result is a toast plus a new saved draft (S). 3 from the gallery's Import button (S) |
| duplicate | 2: gear → Duplicate Agents (S); result I | 2: kebab → Duplicate (S); the copy opens as "saved · v1" with a toast (S) |
| delete | 3: gear → Delete Agents → Delete (S); no undo shown (S) | 3 + 19 keystrokes: kebab → Delete… → type the name → Delete (S); on a published doc refused with a one-click Unpublish (S) |
| unpublish | none — no publish state or menu item (S) | 2: kebab → Unpublish, enabled only when live (S); result I. 1 from inside a refused delete (S) |
| save (added — the surface starts here) | 1: save icon; first save 2 plus a name in a modal (S) | 0 pointer — "Ctrl+S" hint only; no Save in the bar or in the saved-doc kebab (S); the never-saved kebab is uncaptured (I) |

A is Flowise's own Agentflow canvas (the "Save New Agents / My New Chatflow" modal, "Load Agents", "Export As Template" and the fit-and-lock control bar are its), so B is the product under test and A is the reference this dimension is scored against.

## Scores

| dim | A | B | delta (B − A) |
| --- | --- | --- | --- |
| 4 | 4 | 7 | +3 |

Neither side meets the gate of 8: A's six lost points are D-1 (−3), D-4 (−1), D-5 (−1), D-7 (−0.5) and D-11 (−0.5); B's three are D-2 (−1), D-3 (−1), D-6 (−0.5), D-8 with D-9 (−0.25 together, one cause: the 900px height budget) and D-10 with D-12 and D-13 (−0.25 together, one cause: strip copy that does not track document state).

## Ranked defects (most damaging first)

D-1. [dim 4] **A** — no version browser exists: the eight-item settings menu (x1214–1440, y70–522, 51px pitch) has no Versions or History entry, so four of the nine lifecycle jobs (see versions, open an older one, get back, restore as head+1) have no pointer path at all, while B does them in 2/3/4/4 pointer actions from a docked panel with an undoable restore. `A-agentcanvas-settings-menu-saved` vs `B-version-browser-head`, `B-version-viewing-v1-read-only`, `B-version-rows-after-restore`; 1440x900; A card 202px, B card 186px. A 4 / B 7 / +3

D-2. [dim 4] **B** — save has no pointer action: the document bar (y65–102) carries "unsaved changes · Ctrl+S" at x495–640 and no Save control; the kebab on a saved document (x745–956, six items) has no Save; none of the 13 captures shows one. A saves in one click (icon x1333–1365, y17–49). The kebab on a never-saved document is uncaptured, so a Save item there is inferred-absent. `B-canvas-document-bar`, `B-document-menu`; 1440x900; card 186px. A 4 / B 7 / +3

D-3. [dim 4] **B** — the read-only preview is illegible: cards go 186px (panel closed) → 136px with the Versions panel docked at y120–245 → 116px viewing v1 with the banner row added (Request x610–726, title about 7px, subtitle about 4px) → 100px with Versions plus the delete strip (x568–668); every docked strip is taken out of the canvas by the auto-fit, so the graph you opened an old version to look at cannot be read at this viewport. `B-version-browser-head`, `B-version-viewing-v1-read-only`, `B-delete-confirm-docked`; 1440x900; cards 136/116/100px. A 4 / B 7 / +3

D-4. [dim 4] **A** — saved versus unsaved is not legible: the save icon at x1333–1365, y17–49 is pixel-identical in the never-saved and saved captures and no header text states either; B prints "unsaved changes · Ctrl+S" (amber) / "saved · v2" (green) / "v3 is live" at x478–640 in every capture. A modified-after-save state is uncaptured for A, so its absence there is inferred. `A-agentcanvas-settings-menu`, `A-agentcanvas-settings-menu-saved` vs `B-canvas-document-bar`, `B-document-menu`, `B-document-menu-published`; 1440x900; A card 202px. A 4 / B 7 / +3

D-5. [dim 4] **A** — delete has no recovery and no distinction: "Delete Agents" (y472) sits 51px under "Export Agents" with no rule, same icon and colour; the confirm's "Delete" (x859–933, y481–515) is the same primary blue as "Save Template" (x881–1012, y711–745); no type-to-confirm, no undo. B rules off "Delete…" (y287), keeps Delete dimmed until the 19-character name is typed, and on a published document refuses with a one-click Unpublish (x276–769, y181–216). `A-agentcanvas-settings-menu-saved`, `A-agentcanvas-delete-confirm-dialog`, `A-agentcanvas-save-as-template-dialog` vs `B-document-menu`, `B-delete-confirm-docked`, `B-delete-refused-published-docked`; 1440x900; A card 202px. A 4 / B 7 / +3

D-6. [dim 4] **B** — a lifecycle toast displaces the Build/Run toggle: it sits at x1157–1284 with no toast and moves to x835–963 after a restore (−322px), x843–971 after a duplicate (−314px) and x702–830 after an import (−455px; two-line toast x848–1285, y8–44); a persistent mode control jumps on every success. `B-canvas-document-bar`, `B-version-rows-after-restore`, `B-duplicate-opened`, `B-import-result`; 1440x900; card 186px. A 4 / B 7 / +3

D-7. [dim 4] **A** — export gives no on-screen result: `A-agentcanvas-after-export` is identical to the saved capture minus the menu (header y0–65, canvas, control bar y845–870); no toast, no banner. B toasts every captured lifecycle result at x990–1285, y8–44, though B's own export result is uncaptured (I). `A-agentcanvas-after-export`; 1440x900; card 202px. A 4 / B 7 / +3

D-8. [dim 4] **B** — the palette library clips at the viewport: LIBRARY heading y775, list from y813, so 87px remain; with two or more saved graphs the second entry's title sits at y881 with its card cut at y900 in five captures, and no scrollbar or "N more" affordance is visible. `B-document-menu-published`, `B-duplicate-opened`, `B-import-result`, `B-palette-library-after-duplicate-and-import`, `B-delete-refused-published-docked`; 1440x900; card 186px. A 4 / B 7 / +3

D-9. [dim 4] **B** — the gallery buries the user's own graphs: four template cards occupy y147–595, so "Saved here" starts at y659 and 64px rows at 76px pitch show two and a half entries, the third cut at y900; all three rows read "3 Sept, 07:47" so they cannot be ordered; each row exposes only a trash icon (x1187–1219), so duplicate, export and versions cost one extra open first. `B-gallery-library-after-duplicate-and-import`, `B-gallery-delete-refused-unpublish`; 1440x900; rows 64px. A 4 / B 7 / +3

D-10. [dim 4] **B** — the delete strip warns on the wrong state and blocks after the work: on the v2 DRAFT (pill x60–95, y847) the strip (y262–290) says "A published graph cannot be deleted; unpublish it first", which cannot apply; on the published v3 the refusal appears after the attempt while the bar already shows "v3 is live" (x568–638, y83) — whether the type-to-confirm was completed before the server refused is inferred, because the published-doc confirm state is uncaptured. `B-delete-confirm-docked`, `B-delete-refused-published-docked`; 1440x900; cards 100/146px. A 4 / B 7 / +3

D-11. [dim 4] **A** — one action, three names: the menu item "Save As Template" (y269) opens a 598x608 modal titled "Export As Template" (y175) whose confirm reads "Save Template" (x881–1012, y728); four fields for a duplicate-to-gallery, one marked required. `A-agentcanvas-settings-menu-saved`, `A-agentcanvas-save-as-template-dialog`; 1440x900; card 202px. A 4 / B 7 / +3

D-12. [dim 4] **B** — the read-only state contradicts itself: the problems dock reads "✓ Ready to publish" (x344–464, y851) while the Publish button (x965–1059, y83) is disabled and the bar reads "viewing v1 of v2 · read-only". `B-version-viewing-v1-read-only`; 1440x900; card 116px. A 4 / B 7 / +3

D-13. [dim 4] **B** — the same refusal is laid out two ways: docked, the server sentence sits above the buttons (text y163, buttons y181–216); in the gallery it sits below (buttons y800–837, text y856); both print the internal id "ug_309cd317" and say "it is still published" twice. `B-delete-refused-published-docked`, `B-gallery-delete-refused-unpublish`; 1440x900; card 146px / row 64px. A 4 / B 7 / +3

## Rejected observations

- B's fifth node "Transform 1" (x762–~905, y707–790) runs under the minimap (x903–1087, y650–805) in `B-document-menu`, truncating its kind label to "05 · TR" — a canvas-layout defect, not the document lifecycle; outside dimension 4.
- Both B version rows read "just now" (`B-version-browser-head`) — an artefact of capture timing; a relative time is legitimate, and whether an absolute time appears on hover is uncaptured.
- B's kebab moves 15px left (x940 → x925) when Publish becomes Republish — inside the 32px target; cosmetic.
- B's type-to-confirm costs 19 keystrokes on delete — keyboard, not pointer, and deliberate friction paired with the absence of a delete undo; recorded in the table, not counted against the budget.
- A's modals cover the Start node (Save 442x190 at y355–545 over the node at y420–550; Delete 442x148) — persistence dialogs do not need the canvas visible; that belongs to an inspector-over-node dimension, not this one.
- A's export produces no in-page result; the download itself would live in cropped browser chrome, so only the absence of feedback was counted (D-7), not the download.
- B's post-export state is uncaptured — no defect assigned and no credit given for export feedback.
- A's library (behind the back chevron at x25–57, y17–49) is uncaptured — nothing scored for or against it.
- A's canvas holds one 202px node and B's four or five at 186px — card widths were not compared across products, only B against itself across its own states (D-3).
- Whether A's "Load Agents" replaces the open canvas is inferred and unverifiable from the captures; not counted.
- B's disabled "Unpublish" on a draft and disabled Publish while read-only are correct states, not defects.

### Critic 2 of 2 — engineering, rows 11, 12, 14, 16

## Persona
staff frontend engineer, round 2 (engineering, rows 11, 12, 14, 16)

## Commands run and their result lines

All from `D:\MultiAgentSystem-wt\integration` (branch `gauntlet/plans`, `git status --porcelain` printed nothing) with `PYTHONPATH="$PWD/src"` on every Python command. Backend probed at `http://127.0.0.1:8099` only; nothing sent to :8000.

- `curl -s -w ' [%{http_code}]' http://127.0.0.1:8099/api/builder/workflows/ug_00000000/versions` → `{"detail":"document not found"} [404]` (this worktree, not FastAPI's bare `Not Found`).
- `python -m unittest discover -s tests -t .` (one run; no `Errno 22` / `crewai:<hex>.lock` line appeared, so no second run was needed) → `Ran 1642 tests in 84.496s` / `OK (skipped=6)`.
- `cd frontend && npm test` → `Test Files  62 passed (62)` / `Tests  1157 passed (1157)` / `Duration  9.58s`.
- `cd frontend && npx vue-tsc -b --force` → exit 0, no diagnostics; `npm run build` → `dist/assets/index-D-Deh241.js   598.24 kB │ gzip: 192.82 kB` / `✓ built in 759ms`, exit 0.
- `cd frontend && npx playwright test e2e/isolation.spec.ts --reporter=line` → `Running 4 tests using 1 worker` … `4 passed (8.3s)` (the fourth is `@launch`; Bob's launch 404, Alice's control 202).
- `cd frontend && npx playwright test e2e/builder.spec.ts -g "opens a prior version read-only" --reporter=line` → `[1/1] [chromium] › e2e\builder.spec.ts:965:3 › Flow builder › opens a prior version read-only, and Restore makes it the next head` / `1 passed (5.4s)`.
- `cd frontend && npx playwright test --list` → `Total: 34 tests in 5 files`; `npx playwright test --list --grep @launch` → `Total: 8 tests in 3 files`.
- `TEST_DATABASE_URL="postgresql+psycopg://postgres:test@127.0.0.1:5433/postgres" python -m unittest tests.pg.test_two_writers -v` → five tests `ok` (`test_document_save_one_cas_the_other_a_version_conflict`, `test_gate_reply_one_cas_the_other_a_conflict`, `test_orphan_sweep_one_claim_the_other_finds_the_row_already_terminal`, `test_pending_feedback_one_insert_the_other_handled_as_already_pending`, `test_reopen_gate_one_cas_the_other_a_no_op`) → `Ran 5 tests in 26.281s` / `OK`.
- Each criterion-named module alone, `-m unittest discover -s <dir> -t . -p "<module>.py"`:
  - `tests/builder/test_export.py` → `Ran 38 tests in 0.004s` / `OK`
  - `tests/service/test_builder_import.py` → `Ran 29 tests in 2.237s` / `OK`
  - `tests/service/test_builder_duplicate.py` → `Ran 18 tests in 1.182s` / `OK`
  - `tests/builder/test_upgrade.py` → `Ran 12 tests in 1.653s` / `OK`
  - `tests/service/test_additive_migration.py` → `Ran 24 tests in 1.234s` / `OK`
  - `tests/service/test_isolation_matrix.py` → `Ran 32 tests in 3.411s` / `OK`
- `cd frontend && npx vitest run tests/builderPersistence.spec.ts` → `Tests  33 passed (33)`; `tests/versionBrowser.spec.ts` → `Tests  31 passed (31)`.
- Knob scan (the multiline regex over `config.py` + `service/app.py`) → `41`; `grep -n 'forty' docs/tech-stack.md` → `306:## 6. Environment knobs — there are forty-one`.
- `grep -nE 'postgres|TEST_DATABASE_URL|services:' .github/workflows/ci.yml` → `98:    services:` / `100:        image: postgres:18` / `113:      TEST_DATABASE_URL: postgresql+psycopg://postgres:test@127.0.0.1:5432/postgres`.
- `git ls-files .agent/plans/` → includes `.agent/plans/15-persistence.md`; `git status --porcelain -- .agent/` → empty (tracked, unmodified; contents not opened).
- `python -c "…NodeKind…"` → `('input', 'agent', 'crew', 'gate', 'router', 'transform', 'output')` (no v2 kinds at head; the only secret-bearing schema field is `AgentConfig.credential_id`, `builder/document.py:202`).
- Probe script (`scratchpad/critic15-eng/probe.py`, urllib, users `alice-p` / `bob-p` / no header; document = `tests.service.builder_auth.document_payload()`, plus a copy with `config.credential_id = "cr_0badf00d"` on the agent node). Verbatim `body [status]`:
  - `POST /api/builder/workflows` as alice → `{"id":"ug_63f922fd",…} [201]`, `location=/api/builder/workflows/ug_63f922fd`; as bob → `[201]`; as nobody → `[201]` (unowned row).
  - `GET /api/builder/workflows` as alice → sees alice=True bob=False anon=False; as bob → alice=False bob=True anon=False; as nobody → alice=False bob=False anon=True.
  - `GET /api/builder/workflows/{alice}` as bob → `{"detail":"document not found"} [404]`; as nobody → same; `GET …/ug_00000000` as bob → same; `foreign-vs-unknown 404 bodies identical: True; content-length 31 vs 31`.
  - `GET …/{alice}?version=99` as alice → `{"detail":"document ug_9da5a0ea has no version 99; the newest is v1"} [404]`; as bob → `{"detail":"document not found"} [404]`.
  - `GET …/{alice}` with `X-Synthetic-User: BAD USER!` → `{"detail":"X-Synthetic-User must match ^[a-z0-9_-]{1,64}$"} [400]`.
  - `PUT …/{alice}` `expected_version:1` as alice → `[200]` `version now 2 head 2 status draft`; again with `1` → `{"detail":"document ug_9da5a0ea is at version 2, not 1; reload it before saving again"} [409]`; with `7` → `…is at version 2, not 7…` `[409]`; as bob (`2`) → `{"detail":"document not found"} [404]`; as bob (`99`) → `[404]` (not 409); as nobody → `[404]`; without `expected_version` → `{"detail":"expected_version is required on a save; send the version you loaded so a concurrent edit is a conflict rather than a silent overwrite"} [422]`.
  - `PUT …/{alice}` with `nodes[1].kind = "SECRET-KIND-ECHO-CHECK"` → `{"detail":"nodes.1.kind: Input should be 'input', 'agent', 'crew', 'gate', 'router', 'transform' or 'output'"} [422]` (no echo); with `credential_id = "sk-or-v1-LEAKY-SECRET-VALUE"` → `{"detail":"nodes.1.credential_id: String should match pattern '^cr_[0-9a-f]{8}$'"} [422]` (no echo); with `name` of 500 chars → `{"detail":"name: String should have at most 80 characters"} [422]`.
  - `PUT …/{unowned}` as alice → `{"detail":"document ug_4ef6aaa3 has no owner and is read-only for every signed-in user; Duplicate it to get a copy you own"} [403]`; as nobody → `[200]`.
  - `GET …/{alice}/versions` as alice → `[{"version":2,"status":"draft","created_at":"2026-09-03T07:57:51.618769","bytes":866,"source":"saved","name":"Test graph v2","node_count":3},{"version":1,…,"source":"created",…}] [200]`; as bob → `[404]`; as nobody → `[404]`.
  - `GET …/{alice}/export` as alice → `[200]`, `content-disposition=attachment; filename="Test graph v2.builder.json"; filename*=UTF-8''Test%20graph%20v2.builder.json`, `content-type=application/json`; envelope keys `['document','export','exported_at','name','needs_credentials','source_version']`; `export.document` keys `['edges','input_field','joins','name','nodes','schema']` — no `id`, no `user_id`, no `budget`; body mentions alice's id: False, `alice-p`: False; `two exports identical apart from exported_at: True`; as bob → `[404]`; as nobody → `[404]`; `?version=99` as alice → `…has no version 99; the newest is v2 [404]`; as bob → `document not found [404]`.
  - Export of the credential-bearing v3 → `needs_credentials=['scoper']`, `'cr_0badf00d' in body=False`, `document.nodes[1].config.credential_id=None`.
  - `POST /api/builder/workflows/import` (that exact envelope) as bob → `[201]` `id=ug_8e47e2f2 version=1 status=draft needs_credentials=[] name='Test graph imported 3'`, `location=/api/builder/workflows/ug_8e47e2f2`; body mentions alice's id: False, `alice-p`: False, `cr_0badf00d`: False; `GET` it as alice → `[404]`; `ROUND TRIP export->import graph identical: True`.
  - Import refusals: `not json at all` → `{"detail":"the file is not JSON (Expecting value at line 1 column 1)"} [422]`; `[1,2,3]` → `{"detail":"an import is a JSON object carrying \`export\` and \`document\`; this file is a JSON list"} [422]`; no `export` → `{"detail":"export: Field required"} [422]`; `export:"somebody.else/v9"` → `{"detail":"export must be one of ['builder.flow/v1', 'builder.flow/v2']; this file says 'somebody.else/v9'"} [422]`; `document:{"garbage":…}` → `{"detail":"name: Field required"} [422]`; 600 `needs_credentials` → `{"detail":"needs_credentials: List should have at most 24 items after validation, not 600"} [422]`; extra envelope key → `{"detail":"extra_key: Extra inputs are not permitted"} [422]`; 500-char name → `{"detail":"name: String should have at most 80 characters"} [422]`. Hand-edited file carrying alice's id, `user_id`, `version: 9` and a `credential_id` → `[201]` fresh id, `needs_credentials=['scoper']`, echoes alice's id / `alice-p` / `cr_`: False / False / False.
  - Oversize import via curl: 300,996-byte body with declared length → `{"detail": "the request body is limited to 262144 bytes"} [413]`; the same body chunked → `{"detail":"a builder document is limited to 262144 bytes; this file is 300996"} [413]`.
  - `POST /api/builder/workflows` with `{"document": "ECHO-"×40000}` as bob → `{"detail":[{"type":"dict_type","loc":["body","document"],"msg":"Input should be a valid dictionary","input":"ECHO-ECHO-ECHO-…` `[422]`, **body 200,113 bytes, echoes the string: True**. `PUT …/{bob}` same → 200,113 bytes, echo True. `POST /api/builder/validate` same → 200,113 bytes, echo True. `POST /api/builder/workflows` with an extra top-level key of 22 KB → `{"detail":[{"type":"extra_forbidden","loc":["body","sneaky"],…"input":"ECHO-EXTRA-…` `[422]`, 22,113 bytes, echo True. `PUT` with `expected_version:"soon-ECHO"` → `…"input":"soon-ECHO"}]} [422]`. `PUT` with `source:"vandalise"` → `…"input":"vandalise",…}]} [422]`.
  - `POST …/{alice}/duplicate` as alice → `[201]` `version=1 status=draft name='Test graph copy' published=False`, `location=…`; `ROUND TRIP duplicate graph identical to head: True`, `credential_id kept='cr_0badf00d'`; again → `name='Test graph copy 2'`; as bob → `{"detail":"document not found"} [404]`; as nobody → `[404]`; `?version=99` as alice → `…has no version 99; the newest is v3 [404]`; as bob → `document not found [404]`; `?version=1` → `graph==v1: True`; bob duplicating the unowned row → `[201]` owned by bob.
  - Restore: `PUT …/{alice}` with v1's document, `expected_version:3, source:"restore", restored_from:1` → `[200]` `version=4 head_version=4 (was 3)`, `graph identical to v1: True`; the same PUT re-sent with `expected_version:3` → `…is at version 4, not 3; reload it before saving again [409]`; versions afterwards → `v6:autosaved, v5:restored, v4:restored from v1, v3:saved, v2:saved, v1:created` (each with name, node count, bytes).
  - `POST …/{alice}/publish` as bob → `[404]`; as nobody → `[404]`; as alice → `{"workflow_id":"ug_4a219042","graph_version":"534834428e735229","version":6,"input_field":"idea","static_cost_usd":0.05234625,"gated_before_spend":false,"reserved_input_keys":["__builder__","out__idea","out__report","out__scoper"]} [200]`; `?version=99` as bob → `document not found [404]`; on the unowned row as alice/bob → `…has no owner and is read-only… [403]`.
  - `GET /api/workflows` as alice → `['brief-flow','idea-validator','ug_4a219042']`; as bob → `['brief-flow','idea-validator']`; as nobody → the two literals. `GET /api/workflows/{alice}/graph` as alice → `[200]` `etag="534834428e735229"`; as bob → `{"detail":"workflow not found"} [404]`, no ETag; as nobody → same; `…/ug_00000000/graph` → byte-identical body; with `If-None-Match` as bob → still `[404]`. `POST /api/sessions/probe-bob/runs` as bob → `{"detail":"workflow not found"} [404]`; as nobody → same.
  - Versions after publish → `v6:published, v5..v1:draft`; a save after publish → `status=draft version=7 published=False`, versions → `v7:draft, v6:published, …`.
  - `DELETE …/{alice}` as bob → `[404]`; as nobody → `[404]`; as alice while published → `{"detail":"document ug_4a219042 is published - v6 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it"} [409]`; `POST …/unpublish` as bob/nobody → `[404]`; as alice → `[200]` `status=draft published=False`, then `GET /api/workflows/{alice}/graph` as alice → `workflow not found [404]`, `/api/workflows` no longer lists it; unpublish again → `[200]`; `DELETE` as alice → `[204]`; then `GET`, `GET /versions`, `GET ?version=1`, `GET /export`, `DELETE` → all `{"detail":"document not found"} [404]`; alice's list no longer contains it.
  - `POST /api/builder/validate` with `version:"ECHO-CHECK-vvv…"` → `{"detail":"version must be a whole number; this document carries 'ECHO-CHECK-vvvv…'"} [422]`; credential-bearing doc as alice → `valid=False identity_checked=True problems=[('credential-missing','scoper')]`; as nobody → `valid=True identity_checked=False problems=[]`.
  - Bob publishing a document naming `cr_0badf00d` (a credential he does not own) → `{"detail":{"message":"this graph has 1 problem(s) that stop it compiling: scoper names the credential cr_0badf00d, which is not in your vault; pick one of your own or create it first","problems":[{"code":"credential-missing",…"node_id":"scoper"}]}} [422]`.
- Second probe (`probe2.py`, users `alice-q`/`bob-q`, the 16-node `builderValidatorTemplate.json` document):
  - Template stored → `nodes=16 edges=22 joins={'score': 'all'} problems=[]`; `stored v1 graph == posted template graph: False` — the only difference is `credential_id: null` added to each of the 8 agent nodes' `config` (schema default on ingress).
  - `export->import as bob: [201] graph identical: True`; `duplicate: [201] graph identical: True name='Idea validator copy'`; edit to 15 nodes saved as v2; `restore v1 -> v3 (head 3): graph identical to v1: True`; versions → `v3:restored from v1/16n, v2:saved/15n, v1:created/16n`.
  - Clean reproduction of the import loss: alice's doc stored with `credential_id='cr_0badf00d'` → `EXPORT: needs_credentials=['scoper']  document.nodes[1].config.credential_id=None` → `IMPORT of that exact file as bob: [201] needs_credentials=[]  imported credential_id=None  problems=[]`; the same import as alice herself → `needs_credentials=[]`.

## Capture review

- **Set A (6 captures, "Round 1 reference flow").** A near-empty canvas with a single Start node and a top-right settings menu (View Messages / View Leads / Configuration / Save As Template / Duplicate Agents / Load Agents / Export Agents / Delete Agents). Every persistence action is a centred modal over the canvas: "Save New Agents" name prompt with a "My New Chatflow" placeholder, "Export As Template" form (Name / Description / Badge / Usecases), "Delete Agent Round 1 reference flow?" with Cancel/Delete. The after-export frame shows no confirmation of any kind on screen. No version list, no refusal state, no owner.
- **Set B (13 captures, "M2 Flow builder").** A document bar with `saved · v3 · v3 is live` chips; a ⋮ menu (Versions / Export .builder.json / Import .builder.json… / Duplicate / Unpublish / Delete…); a docked delete confirm that requires typing the graph's name; the delete refusal rendered docked in both the canvas and the gallery as "Not deleted — it is still published." followed by the server's sentence verbatim (`document ug_309cd317 is published - v3 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it`) with an Unpublish action; a version browser with HEAD/DRAFT/PUBLISHED chips, a source column (`created`, `saved`, `restored from v1`), sizes, and a read-only banner ("Viewing v1 of v2 — read-only. Restore it to make it the next version, or go back to v2.") with the palette greyed and a lock icon; an import success notice naming the new draft; a duplicate opened as "… copy" v1 draft.
- Belief: **B is the product under review** — its chrome matches this tree's components (`DocumentBar`, `VersionBrowser`, the seven palette kinds, `builder-notice`, `ug_` ids, the exact 409 sentence from `builder_api.py:889-893`), while A's vocabulary ("Agents", "Chatflow", "Load Agents") is a Flowise-style reference.

## Scores

| dim | ours | ref | delta | carried by |
| --- | ---: | ---: | ---: | --- |
| 11 | 10 | 10 | 0 | — |
| 12 | 6 | 10 | 4 | D-1 (2), D-2 (1), D-3 (1) |
| 14 | 10 | 10 | 0 | — |
| 16 | 7 | 10 | 3 | D-1 (3) |

## Ranked defects (most damaging first)

D-1. `[dim 12, dim 16]` **Importing the file the export wrote never names the nodes that lost a credential, so the import notice built for that case is unreachable and the graph silently drops from the author's key to the platform key.** `builder/export.py:175-181` — `_scrub` records a node only `if item not in (None, "")`, and the export has already nulled the key, so on re-import there is nothing to note; `service/builder_api.py:669-676` says the opposite ("the file's nulled `credential_id` / `server_id` keys are the evidence, and the strip reads them directly") and discards the envelope's own list; the UI notice at `frontend/src/components/builder/BuilderView.vue:730` renders only when `needs_credentials.length > 0`. Criterion 2 as written ("Import of *that file* as user B creates a draft … and a problem group naming each `needs_credentials` node") is false, and the module it names pins the defect: `tests/service/test_builder_import.py:153` (`test_the_envelope_s_own_list_is_never_trusted`) asserts `body["needs_credentials"] == []` for an envelope declaring `["scoper"]`. Command: `probe2.py` → `EXPORT: needs_credentials=['scoper']  document.nodes[1].config.credential_id=None` then `IMPORT of that exact file as bob: [201] needs_credentials=[]  imported credential_id=None  problems=[]` (and `needs_credentials=[]` when alice re-imports her own file). ours 6 / ref 10 / delta 4 (row 12, 2 points); ours 7 / ref 10 / delta 3 (row 16, 3 points).

D-2. `[dim 12]` **Three of the four persistence doors reflect the request back on a refusal, up to the 256 KiB body ceiling.** `service/builder_api.py:196` (`BuilderDocumentRequest`) is a typed FastAPI body used at `:599` (create), `:728` (save) and `:943` (validate); anything pydantic refuses *above* the untyped `document` dict — a non-dict `document`, a bad `expected_version`, an extra key — is answered by FastAPI's default 422 with `"input"` echoed, which is exactly what `:1079` `_import_envelope` was hand-written to avoid for import (D-15-9) and what the class's own docstring at `:199-203` claims the handlers prevent. Command: `probe.py` → `POST /api/builder/workflows as bob-p -> {"detail":[{"type":"dict_type","loc":["body","document"],"msg":"Input should be a valid dictionary","input":"ECHO-ECHO-…` `[422]` / `create with a 200KB string document -> 422 body is 200113 bytes; echoes the string: True` (same figure on `PUT` and `/validate`; `22113 bytes` for the extra-key case). ours 6 / ref 10 / delta 4 (1 point).

D-3. `[dim 12]` **Every refusal on the import, duplicate, export, restore and open-version paths is shown in a notice that clears itself after 4000 ms, with no persistent surface to re-read the server's sentence** — while delete (docked confirm, capture `B-delete-refused-published-docked`) and a save conflict (`ConflictDialog.vue:269` shows `conflict.detail`) both get one. `frontend/src/components/builder/BuilderView.vue:415-430` (`say()` → `window.setTimeout(… noticeAction.value ? 8000 : 4000)`), error sites `:557`, `:626` (which also omits `kind: 'error'`, so a refused version open renders as `info`), `:649`, `:690`, `:725`, `:764`. Command: `grep -n "8000 : 4000" …/BuilderView.vue` → `429:  }, noticeAction.value ? 8000 : 4000)`; corroborated by `frontend/e2e/isolation.spec.ts:352-353` ("Asserted right after the refusal because the notice retires itself after four seconds"). ours 6 / ref 10 / delta 4 (1 point).

## Criteria I could not verify, and why

- **2** — verified false as written (D-1); listed here because the *ownership* half (fresh `ug_` id, owner B, no reference to A) is true: `id=ug_8e47e2f2 … [201]`, `GET` as alice `[404]`, body mentions alice's id / handle: False.
- **7** — verified through `test_additive_migration.py` only (24 OK; `inspect(` at `:75`, `:78`, `:135`); I did not run `create_all` against the PostgreSQL 18 instance myself.
- **8** — the scan answers **41** and `docs/tech-stack.md:306` says forty-one; "regenerated in the same commit" needs `git log`, which this brief forbids.
- **10** — 32 tests OK and the test's docstring carries both the matrix and the unowned-row table; "the table in D9 exactly" cannot be checked because D9 lives under `.agent/`, which is off-limits.
- **11** — 33 passed; "passes *unchanged*" needs `git diff`/history, forbidden.
- **Row 16, the ticks** — `.agent/plans/15-persistence.md` is tracked and clean (`git ls-files`, `git status --porcelain`), but its contents are off-limits, so I scored every one of the eleven criteria as if ticked.
- **4** — the E2E step and `versionBrowser.spec.ts` (31) pass and the API restore made `v4` from `expected_version: 3` through the CAS (a stale re-send answered 409); I did not drive the browser by hand.

## Rejected observations

- `GET …?version=0` answers FastAPI's list 422 with `"input":"0"` echoed — a one-character query value; folded into D-2's class rather than counted twice.
- `PUT` with `source: "vandalise"` and `expected_version: "soon-ECHO"` echo the scalar — same class as D-2, not counted separately.
- `/validate` with a non-numeric `version` echoes the value verbatim (`this document carries 'ECHO-CHECK-vvv…'`, `builder_api.py:1245-1247`) — one scalar, and `/validate` is not one of the four persistence paths; not counted.
- Unowned rows (created with no header on this auth-off backend) are readable and duplicable by alice and bob and writable only by an anonymous caller (`[403] … Duplicate it to get a copy you own` for a signed-in writer) — that is the second table in `test_isolation_matrix.py`'s docstring, and `owner_for_new_row` (`builder_api.py`) refuses creating such a row with 401 once `AUTH_BASE_URL` is set; by design, not an isolation defect.
- A 300 KB body with a declared `Content-Length` is refused at the edge before it is drained, so urllib sees `ConnectionResetError` while curl reads the 413 — edge behaviour, legible to any client that reads first; not counted.
- The stored v1 of the validator template differs from the posted document only by `credential_id: null` filled onto each agent node — a schema default on ingress; every export/import/duplicate/restore compared against the stored copy was identical, so row 11 carries nothing.
- `POST /api/builder/workflows/{id}` → 405 was my own mistaken verb, not a finding.
- `store.delete` (`builder/store.py:683-716`) deletes the version and test-input rows explicitly because SQLite ignores `ON DELETE CASCADE` without the pragma; the DDL at `persistence.py:275` still carries the cascade, and the post-delete probes all answered 404, so criterion 5 holds as written.
- Two exports of the same version are byte-identical apart from `exported_at`; the `Content-Disposition` carries both `filename` and RFC 5987 `filename*` — fine.
- Outside these four dimensions: the E2E web server logs a Vite `configLoader: 'native'` warning about `import "./syntheticUser"` lacking an extension (`e2e/vite.e2e.config.ts:4`); the Python suite prints crewai `ImportWarning`/`DeprecationWarning` lines. Neither is this feature's.

### Row verifier — plan 15, the twelve ledger rows (not a critic; it knew what was claimed and what was changed)


HEAD `44f3582` on `gauntlet/plans` (one docs-only commit past the brief's `56bb8ab`: `PLANS.md`, 2 lines; the source tree is identical); `POST /api/builder/workflows/ug_00000000/unpublish` → `{"detail":"document not found"} [404]`; 2026-09-03 16:09–16:20 MPST.

Suites run once each at HEAD, in this worktree (`PYTHONPATH=$PWD/src`; `frontend/` vitest): `test_additive_migration` Ran 24 OK · `test_builder_versions` Ran 21 OK · `test_builder_duplicate` Ran 18 OK · `test_builder_import` Ran 29 OK · `test_isolation_matrix` Ran 32 OK · `test_builder_export_route` Ran 13 OK · `test_builder_unpublish` Ran 12 OK · `test_builder_delete` Ran 12 OK · `versionBrowser.spec.ts` Tests 31 passed · `builderCanvas.spec.ts` Tests 66 passed · `builderImport.spec.ts` Tests 10 passed · `documentLifecycle.spec.ts` Tests 13 passed · `builderShell.spec.ts` Tests 34 passed · `builderApi.spec.ts` Tests 38 passed · `e2e/builder-layout.spec.ts -g "saved-graphs library"` 1 passed (6.5s).

#### Per row

##### D-15-1 — ABSENT
- **probe:** `benchmarks/ours/15/2/15-version-viewing-v1-read-only-1440x900-dark.png` (the round-1 surface `A-version-viewing-v1-read-only`), measured against the head capture `15-version-browser-head` for the Publish colour; `frontend/tests/versionBrowser.spec.ts`; source `DocumentBar.vue:271-294,472-473,522`, `SaveChip.vue:62,80-82,115`, `NodePalette.vue:176,212`, `BuilderNode.vue:459-466`.
- **output:** Document bar (y66–102) is tinted amber across its width (`.document-bar.is-read-only { background: linear-gradient(to bottom, var(--warn-bg), transparent) }`); a lock glyph at x≈283 precedes the title; the chip at x≈500–690 reads `viewing v1 of v2 · read-only` in amber with an eye icon (`SaveChip.vue:62`: `` `viewing v${props.version} of v${props.headVersion} · read-only` ``), where the head capture's chip reads `✓ saved · v2` in green; Publish at x≈965–1060 is rendered muted grey-cyan (head capture: full cyan) and is `:disabled="readOnly"` with title `you are viewing v1; publish works on head (v2)`; the palette's seven kind tiles are dimmed and a yellow banner at y≈152–210 reads `Read-only — a stored version is on the canvas. Restore it, or go back to head, to edit.`; each of the four node cards carries a lock in its eyebrow (x≈718 on every card, `BuilderNode.vue:464 v-if="readOnly"`); the inspector's Name field is greyed. `versionBrowser.spec.ts`: `Tests 31 passed (31)` — including `opens an older version read-only on every surface, and publish refuses it`, `disables every tile, says why, and drops nothing on click`, `puts a lock in the card eyebrow only while the canvas is read-only`.
- **verdict:** The row's sentence is not true at HEAD — the document bar, chip, Publish, palette and node cards all now carry read-only cues, and the chip text `viewing v1 of v2 · read-only` in amber is the one line that shows it.

##### D-15-2 — ABSENT
- **probe:** `15-version-browser-head-1440x900-dark.png` and `15-delete-confirm-docked-1440x900-dark.png` (the round-1 surfaces), node positions measured; `frontend/tests/builderCanvas.spec.ts`; `BuilderCanvas.vue:161-230` (`ResizeObserver` on the dock, `refit()` → `flow.fitView({ padding: 0.14 })`, `watch(() => props.dock)` observing a dock that arrives after mount, `disconnect()` on unmount).
- **output:** Head capture: Versions panel y≈120–245 (as round 1), budget y≈255–405, canvas ≈y410–815; all five nodes visible — Request y≈443–487, Confirm y≈545–592, Draft y≈648–697, Result y≈745–795, Transform 1 y≈727–783, nothing below the canvas bottom. Delete-confirm capture: Versions y≈120–245, delete strip y≈250–355, budget y≈362–515, canvas ≈y520–815; all five nodes visible at a smaller scale (cards ≈100px wide) — Request y≈545–572, Confirm y≈617–655, Draft y≈693–732, Result y≈767–801, Transform 1 y≈750–793. 5 of 5 visible in both; round 1 had 3 of 5 and 2 of 5 visible. `builderCanvas.spec.ts`: `Tests 66 passed (66)` — including `re-fits when a docked strip opens, even after the author has gestured (D-15-2)` and `observes a dock that arrives after mount, the way a template ref does (D-15-2)`.
- **verdict:** The row's sentence is not true at HEAD — with both strips docked all five nodes sit inside the y520–815 canvas, and the delete-confirm capture's Result card at y≈767–801 (above the canvas bottom) shows it.

##### D-15-3 — ABSENT
- **probe:** `15-version-browser-head-1440x900-dark.png` and `15-version-rows-after-restore-1440x900-dark.png`; `GET /api/builder/workflows/{id}/versions` from the D-15-10 probe; `tests/service/test_additive_migration.py::VersionSourceColumnTests`, `test_builder_versions.py::WhatARowSays`, `frontend/tests/versionBrowser.spec.ts`; `VersionBrowser.vue:118-171,270-272`.
- **output:** Head capture rows: `v2  HEAD DRAFT  Minimal gated agent · 5 nodes   saved   ⏱ just now  1.4 KB` and `v1  DRAFT  Minimal gated agent · 4 nodes   created  ⏱ just now  1.2 KB`. After restore: `v3 HEAD DRAFT Minimal gated agent · 4 nodes  restored from v1 ⏱ just now 1.2 KB`, `v2 … 5 nodes  saved`, `v1 … 4 nodes  created`. Server row (probe d10): `{"version":2,"status":"draft","created_at":"2026-09-03T08:12:35.735669","bytes":860,"source":"saved","name":"renamed","node_count":3}` beside `{"version":1,…,"source":"created","name":"Test graph","node_count":3}`. The relative time's `title` is the seconds stamp (`stamp()`: `2 Sep 2026, 10:14:32`); `ago()` shows `${seconds} s ago` under a minute. Python: `test_additive_migration` Ran 24 OK (`VersionSourceColumnTests` ×3), `test_builder_versions` Ran 21 OK (`test_every_row_carries_its_name_node_count_and_source`, `test_the_save_source_is_composed_from_what_the_client_declared`). `versionBrowser.spec.ts` Tests 31 passed — `tells two versions from the same minute apart by label, source and seconds (D-15-3)`, `reads the real clock by default, and a naive stamp as UTC (D-15-3)`.
- **verdict:** The row's sentence is not true at HEAD — v1 and v2 now differ by node count (`4 nodes` / `5 nodes`) and source (`created` / `saved`) in the row text itself, not only by the KB figure; residual: two rows saved seconds apart with equal node counts both read `just now` on screen and differ only in the hover stamp and size.

##### D-15-4 — ABSENT
- **probe:** `15-import-result`, `15-duplicate-opened`, `15-palette-library-after-duplicate-and-import`, `15-gallery-library-after-duplicate-and-import` (all `-1440x900-dark.png`); `cd frontend && npx playwright test e2e/builder-layout.spec.ts -g "saved-graphs library" --reporter=line` (run once, against :8099); `tests/service/test_builder_duplicate.py`, `test_builder_import.py::ImportedNamesStayDistinct`; `builderShell.spec.ts`, `versionBrowser.spec.ts`; `builder.css:741-785`.
- **output:** Import is named `Minimal gated agent imported` (bar title, inspector `28 / 80 characters`, library row), duplicate is `Minimal gated agent copy` (`24 / 80`), original stays `Minimal gated agent` — three distinct names. Palette library (x≈12–222): row 1 `Minimal gated agent imported` shown whole on one line, `v1 DRAFT` under it; row 2 `Minimal gated agent copy` visible whole at y≈881 with its meta line cut by the viewport edge (y=900); row 3 not in the PNG; no scrollbar visible in the PNG. Gallery: three rows whole — `Minimal gated agent imported DRAFT v1 3 Sept, 07:47`, `Minimal gated agent copy DRAFT v1`, `Minimal gated agent DRAFT v3`. DOM/CSS: `.builder-library { … min-height: 0; … overflow-y: auto; }` (`builder.css:744`), `.builder-library-name { -webkit-line-clamp: 2; overflow-wrap: anywhere; white-space: normal }`, `:title="entry.name"` on every row name (`NodePalette.vue:287`). E2E result line: `[1/1] [chromium] › e2e\builder-layout.spec.ts:349:3 › the saved-graphs library (D-15-4) › shows every distinguishing word and keeps every row reachable` → `1 passed (6.5s)` — it seeds six rows, asserts `scrollWidth <= clientWidth+1` and `scrollHeight <= clientHeight+1` on the first two names, the palette bottom `<= 901`, and the sixth row's box inside the palette after `scrollIntoViewIfNeeded()`. Python: `test_builder_duplicate` Ran 18 OK, `test_builder_import` Ran 29 OK (`ImportedNamesStayDistinct` ×3). `builderShell.spec.ts` Tests 34 passed (`carries the whole name in the row title, so a clipped tail is one hover away (D-15-4)`); `versionBrowser.spec.ts` Tests 31 passed (`carries each library row's whole name in its title (D-15-4)`).
- **verdict:** The row's sentence is not true at HEAD — the three rows read `… imported`, `… copy` and the bare name, no name is ellipsised, and the list scrolls (`overflow-y: auto`, E2E `1 passed` with the sixth row reachable); the PNG alone cannot show the overlay scrollbar, which is why the E2E line is the evidence.

##### D-15-5 — ABSENT
- **probe:** `15-import-result-1440x900-dark.png` (the round-1 surface), toast box and text measured; `frontend/tests/builderImport.spec.ts`, `versionBrowser.spec.ts`; `BuilderView.vue:417-439,738,859,1355-1366,1851`.
- **output:** Header toast at x≈850–1285 (≈435px), y≈8–45, bordered, with a green circle-check icon at x≈866 and a `×` dismiss at x≈1268; text wraps to two lines and reads in full `imported alice.builder.json as a new draft, “Minimal gated agent imported”.` — no ellipsis, the name complete. Restore toast (`15-version-rows-after-restore`): `✓ restored v1 as v3 — head is one undo away. ×`. Source: `say(message, { kind, action })` with `noticeAction` rendered as `data-testid="notice-action"` (`BuilderView.vue:1355`) and `data-testid="notice-dismiss"` (`:1366`); the viewed-version notice passes `action: { label: \`Back to v${head}\` … }` (`:859`); the import notice (`:738`) passes `kind: 'success'` and no action. `builderImport.spec.ts`: `Tests 10 passed (10)` — `says what it imported in full, as a success, with a dismiss (D-15-5)`; `versionBrowser.spec.ts` Tests 31 passed (the `Back to v2` action).
- **verdict:** The row's sentence is not true at HEAD — the toast has an icon, a dismiss and room to wrap, and the capture shows the whole name `“Minimal gated agent imported”.`; residual: the import notice itself carries no action button (the action slot exists and is used by the viewed-version notice), and the imported document is already open on the canvas behind it.

##### D-15-6 — ABSENT (A's half; B is the reference product and not in this tree)
- **probe:** `15-document-menu-1440x900-dark.png` (draft, the round-1 surface) and `15-document-menu-published-1440x900-dark.png`; `frontend/tests/documentLifecycle.spec.ts`; `DocumentBar.vue:425-451,592-597`.
- **output:** Draft menu at x≈745–957: `Versions` y≈129, `Export .builder.json` y≈163, `Import .builder.json…` y≈197, `Duplicate` y≈231, `Unpublish` y≈265 (greyed, `:disabled="!canUnpublish"`, title `Nothing is published`), a full-width rule at y≈288 (`<hr class="document-menu-separator" role="separator" … data-testid="menu-separator" />`), then `🗑 Delete…` y≈310 in the error colour (`.document-menu-item.is-danger { color: var(--err-text); }` — at rest, not only on hover). Delete is now 79px below Duplicate with Unpublish and a separator between them (round 1: 34px, no separator, same colour). Published menu (x≈730–940): identical order, `Unpublish` enabled, same separator and red `Delete…`. `documentLifecycle.spec.ts`: `Tests 13 passed (13)`.
- **verdict:** The row's A-clause is not true at HEAD — Delete sits under a separator in destructive colour at rest, and the red `Delete…` row beneath the rule at y≈288 in both menu captures shows it; the B-clause concerns the reference product and nothing here can change it.

##### D-15-7 — ABSENT
- **probe:** `verifier15/probe.py d7` against :8099 — `POST /api/builder/workflows` with NO header, then `PUT` as `bob`, `publish` as `bob`, `DELETE` as `alice`, `unpublish` as `alice`, `duplicate` as `bob`, `GET` as nobody and as `alice`, then the same writes as nobody; `tests/service/test_isolation_matrix.py`; `store.py:120 class DocumentReadOnly`, `:872 def _writable_by`.
- **output:**
  `POST /api/builder/workflows as nobody -> {"id":"ug_76354aa7", … "version":1 …} [201]` (status `draft`)
  `PUT /api/builder/workflows/ug_76354aa7 (expected_version=1) as bob -> {"detail":"document ug_76354aa7 has no owner and is read-only for every signed-in user; Duplicate it to get a copy you own"} [403]`
  `POST /api/builder/workflows/ug_76354aa7/publish as bob -> {"detail":"document ug_76354aa7 has no owner and is read-only for every signed-in user; Duplicate it to get a copy you own"} [403]`
  `DELETE /api/builder/workflows/ug_76354aa7 as alice -> {"detail":"document ug_76354aa7 has no owner and is read-only for every signed-in user; Duplicate it to get a copy you own"} [403]`
  `POST /api/builder/workflows/ug_76354aa7/unpublish as alice -> {… same sentence …} [403]`
  `POST /api/builder/workflows/ug_76354aa7/duplicate as bob -> {"id":"ug_d3b3c08d", … "name":"Test graph copy" …} [201]`
  `GET /api/builder/workflows/ug_76354aa7 as nobody -> {…} [200]` · `GET … as alice -> {…} [200]`
  `PUT … as nobody -> {… "name":"renamed by nobody","version":2 …} [200]` · `POST …/publish as nobody -> {"workflow_id":"ug_76354aa7", …} [200]` · `POST …/unpublish as nobody -> {…} [200]` · `DELETE … as nobody ->  [204]` · `GET … as nobody -> {"detail":"document not found"} [404]`
  `test_isolation_matrix`: `Ran 32 tests … OK` (round 1 said 16; `UnownedRowCarveOut`, `UnownedCreationWithAuthConfiguredButNotRequired`, `UnownedRowWithAuthOff` including `test_nobody_can_create_an_unowned_row_while_auth_is_required` and `test_anonymous_creates_saves_and_deletes_an_unowned_row`).
- **verdict:** The row's sentence is not true at HEAD — `bob`'s PUT, `bob`'s publish and `alice`'s DELETE on the unowned row all answer `[403]` naming Duplicate where round 1 saw `[200]`/`[204]`; on this `SYNTHETIC` (auth-off) backend an anonymous caller can still create an unowned row and still write, publish and delete it (`PUT … as nobody [200]`, `DELETE … as nobody [204]`), which the amended D9 table declares as the auth-off rule.

##### D-15-8 — ABSENT
- **probe:** `verifier15/probe.py d8` — create as `alice`, PUT once (head v2), then `?version=99` on GET, `/export`, `/duplicate`, `/publish` as `alice` and as `bob`; `tests/service/test_builder_duplicate.py`, `test_builder_export_route.py`, `test_builder_versions.py`; `builder_api.py:1283-1292`.
- **output:**
  `GET /api/builder/workflows/ug_c9bc9d4a?version=99 as alice -> {"detail":"document ug_c9bc9d4a has no version 99; the newest is v2"} [404]`
  `GET /api/builder/workflows/ug_c9bc9d4a/export?version=99 as alice -> {"detail":"document ug_c9bc9d4a has no version 99; the newest is v2"} [404]`
  `POST /api/builder/workflows/ug_c9bc9d4a/duplicate?version=99 as alice -> {"detail":"document ug_c9bc9d4a has no version 99; the newest is v2"} [404]`
  `POST /api/builder/workflows/ug_c9bc9d4a/publish?version=99 as alice -> {"detail":"document ug_c9bc9d4a has no version 99; the newest is v2"} [404]`
  as `bob`, all four: `{"detail":"document not found"} [404]`
  control `GET …?version=1 as alice -> {…} [200]`.
  `_guarded`: `detail = "document not found" if exc.version is None else str(exc)`. Python: `test_builder_duplicate` Ran 18 OK (`test_a_version_that_does_not_exist_is_a_404_naming_the_version`, `assertIn("newest is v1", detail)`), `test_builder_export_route` Ran 13 OK (same name), `test_builder_versions` Ran 21 OK, `test_isolation_matrix` Ran 32 OK (the stranger keeps the constant).
- **verdict:** The row's sentence is not true at HEAD — the caller's own document with an unknown version answers `document ug_c9bc9d4a has no version 99; the newest is v2` on all four routes, and a stranger still gets the constant.

##### D-15-9 — ABSENT
- **probe:** `verifier15/probe.py d9` — `POST /api/builder/workflows/import` as `alice` with (1) a raw document, no envelope, whose name carries the marker `ECHO-MARKER-raw-document`; (2) `{"foo":1}`; (3) a valid envelope with `needs_credentials` = 500 entries `ECHO-CRED-0…499`; (4) `not json at all`; each body searched for the marker and for `"input"`; `tests/service/test_builder_import.py::MalformedFilesEchoNothing`; `builder_api.py:625-700` (`_import_envelope(await http_request.body())`).
- **output:**
  (1) `-> {"detail":"export: Field required"} [422]` · `echoes marker? False; 'input' key in body? False; bytes=35`
  (2) `-> {"detail":"export: Field required"} [422]` · `echoes 'foo'? False; 'input' key in body? False; bytes=35`
  (3) `-> {"detail":"needs_credentials: List should have at most 24 items after validation, not 500"} [422]` · `echoes ECHO-CRED? False; count of 'ECHO-CRED' in body: 0; bytes=91`
  (4) `-> {"detail":"the file is not JSON (Expecting value at line 1 column 1)"} [422]` · `echoes 'not json'? False; bytes=70`
  `test_builder_import`: `Ran 29 tests … OK` (`test_a_raw_document_with_no_envelope_names_export_and_echoes_no_node`, `test_an_unrelated_object_names_the_first_missing_field_only`, `test_a_json_list_is_named_as_a_list`); `builderImport.spec.ts` Tests 10 passed (`renders the server refusal in its own words, and opens nothing`).
- **verdict:** The row's sentence is not true at HEAD — every malformed import answers one `detail` sentence of 35–91 bytes naming the first problem, and `count of 'ECHO-CRED' in body: 0` on the 500-item list is the line that shows nothing is echoed.

##### D-15-10 — ABSENT
- **probe:** `verifier15/probe.py d10` — create as `alice`, publish, `GET /api/workflows/{id}/graph`, DELETE, PUT (head → v2 draft, v1 still registered), `/versions`, graph, DELETE, graph, then the remedy (`/unpublish`), graph, `/versions`, DELETE, graph, document; `grep` of the confirm copy in `BuilderView.vue:1596-1600` and `TemplateGallery.vue:384-387`; captures `15-delete-confirm-docked`, `15-delete-refused-published-docked`, `15-gallery-delete-refused-unpublish`; `tests/service/test_builder_unpublish.py`, `test_builder_delete.py`; `documentLifecycle.spec.ts`, `builderShell.spec.ts`, `builderApi.spec.ts`.
- **output:**
  `POST …/ug_fa884040/publish as alice -> {"workflow_id":"ug_fa884040", … "version":1 …} [200]` · `graph after publish -> {…} [200]`
  `DELETE while published head as alice -> {"detail":"document ug_fa884040 is published - v1 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it"} [409]`
  `PUT … (expected_version=1) as alice -> {… "version":2 …} [200]` · `/versions -> [{"version":2,"status":"draft",…},{"version":1,"status":"published",…}] [200]` · `graph after save (head draft) -> {…} [200]`
  `DELETE with head draft, v1 registered as alice -> {"detail":"document ug_fa884040 is published - v1 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it"} [409]` · `graph after that DELETE -> {…} [200]` · `document after that DELETE -> {…} [200]`
  `POST …/unpublish as alice -> {… "version":2 …} [200]` · `graph after unpublish -> {"detail":"workflow not found"} [404]` · `/versions -> [{"version":2,"status":"draft",…},{"version":1,"status":"draft",…}] [200]`
  `DELETE after unpublish as alice ->  [204]` · `graph after delete -> {"detail":"workflow not found"} [404]` · `document after delete -> {"detail":"document not found"} [404]`
  UI copy: `grep -rn "running graphs are not affected\|unregisters it" frontend/src` → `(none)`; `BuilderView.vue:1597-1599`: `Delete <strong>{{ doc.name }}</strong> and every stored version of it? A published graph cannot be deleted; unpublish it first, then delete it. Type <strong>{{ doc.name }}</strong> to confirm.`; `TemplateGallery.vue:385`: `A published graph cannot be deleted; unpublish it first, then delete it.` Capture `15-delete-confirm-docked` shows that sentence at y≈270–290; `15-delete-refused-published-docked` shows `Not deleted — it is still published.` over the server's 409 verbatim (`document ug_309cd317 is published - v3 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it`) with an `Unpublish` button (x≈275–770, y≈182–216) and `Keep it published`; `15-gallery-delete-refused-unpublish` shows the same pair under the gallery row. Python: `test_builder_unpublish` Ran 12 OK, `test_builder_delete` Ran 12 OK (`test_a_save_does_not_make_a_registered_graph_deletable`, `test_unpublish_then_delete_removes_the_row_and_the_registration`). Frontend: `documentLifecycle.spec.ts` Tests 13 passed, `builderShell.spec.ts` Tests 34 passed, `builderApi.spec.ts` Tests 38 passed.
- **verdict:** The row's sentence is not true at HEAD — a save no longer makes a registered graph deletable (`DELETE with head draft, v1 registered … [409]`, `graph after that DELETE … [200]`), the remedy the 409 names is a route that unregisters (`graph after unpublish … [404]`), and the confirm copy says the server's sentence rather than the opposite.

##### D-15-11 — PARTLY (the literal "six" survives in the criterion's first sentence; the "unamended" clause does not)
- **probe:** `.agent/plans/15-persistence.md:354-366` (criterion 7 and the note under it) and the `## Status` row 7; `sed -n '418,424p' src/brief_crew/service/persistence.py`; `tests/service/test_additive_migration.py:186`; `PYTHONPATH=$PWD/src python -m unittest tests.service.test_additive_migration`.
- **output:** Criterion 7 at HEAD still opens: `7. \`tests/service/test_additive_migration.py\` covers \`runs.mode\`; \`create_all\` on a database that already has \`runs\` yields all six new tables with their indexes — asserted with the inspector. Rubric 16.` Directly under it: `*Amended 2026-09-03 (D-15-11):* **five** new tables plus \`runs.mode\`, not six. D6 declares five — \`user_credentials\`, \`user_skills\`, \`mcp_servers\`, \`user_tools\`, \`builder_test_inputs\` — and \`GAUNTLET_TABLES\` (\`service/persistence.py\`) and \`test_all_five_tables_arrive_with_their_indexes_and_constraints\` both say five; "six" counted the additive column as a table. … The earlier wording stands above so the correction is visible. (D-15-3 has since added a second additive column, \`builder_document_versions.source\`; the count of tables is unchanged.)` `GAUNTLET_TABLES` = `("user_credentials", "user_skills", "mcp_servers", "user_tools", "builder_test_inputs")` — 5. `test_additive_migration.py:186 def test_all_five_tables_arrive_with_their_indexes_and_constraints`; `Ran 24 tests … OK`. Status row 7 still reads `| 7 | done, one correction | \`tests/service/test_additive_migration.py\` (19). The criterion says **six** new tables; the DDL and S1 ruling 2 have **five** plus \`runs.mode\`. The test is right |`.
- **verdict:** The "leaves the criterion unamended" clause is not true at HEAD — a dated amendment sits under criterion 7 naming the five tables and why "six" was wrong, and it matches `GAUNTLET_TABLES`; the "its text is false" clause survives literally, because the sentence a reader meets first still says `yields all six new tables` and is corrected on the next line by the plan's stated convention of keeping the earlier wording visible.

##### D-15-12 — PARTLY (the literal "forty" survives in criterion 8's first sentence; the criterion-10 clause and the count do not)
- **probe:** `.agent/plans/15-persistence.md:367-380` (criteria 8 and 10 with their notes), D9's dated amendment (`:296-331`), Status rows 8 and 10; the knob scan `PYTHONPATH=$PWD/src python -c "import re,pathlib;pat=re.compile(r'(?:os\.getenv|os\.environ\.get|_env_[a-z_]+)\(\s*\"([A-Z_][A-Z0-9_]*)\"',re.S);print(len(sorted({…})))"`; `grep -n forty docs/tech-stack.md`; `python -m unittest tests.service.test_isolation_matrix`.
- **output:** Scan at HEAD: `41`. `docs/tech-stack.md:306`: `## 6. Environment knobs — there are forty-one`. Criterion 8 at HEAD still opens: `8. The knob scan in CLAUDE.md answers forty after \`VALIDATOR_RUN_RETENTION_DAYS\` lands, and \`docs/tech-stack.md\` §6 is regenerated in the same commit. Rubric 16.` Under it: `*Amended 2026-09-03 (D-15-12):* the scan answers **41**, and \`docs/tech-stack.md\` §6 says forty-one (\`52bdc2e\`). "Forty" assumed one new knob over thirty-nine; Stage 1 landed the six config knobs of S1 ruling 3 before the count was regenerated, and the criterion was ticked against the prose rather than the scan. Re-run on 2026-09-03 while building round 2, with no knob added or removed by it: **41**. The command is the contract; the earlier wording stands above.` Criterion 10: `10. \`tests/service/test_isolation_matrix.py\` passes with the table in D9 exactly. Rubric 14.` followed by `*Amended 2026-09-03 (D-15-12):* "the table in D9" means the table **and its dated amendment** — the unowned row per verb, and the test-inputs row asserted at the table level until plan 13's route exists. The test's own docstring carries the same two tables.` D9 now carries the dated amendment with the per-verb unowned-row table and item 2: `The test-inputs row is asserted at the table level, and the table now says so. … Criterion 10's "exactly" is measured against this note`. `test_isolation_matrix`: `Ran 32 tests … OK`. Status rows: `| 8 | done | \`docs/tech-stack.md\` §6 regenerated at 41 (\`52bdc2e\`) |`, `| 10 | done, one scope note | \`tests/service/test_isolation_matrix.py\` (16). The **test-inputs row is covered at the table level** … |`.
- **verdict:** The criterion-10 clause is not true at HEAD — the narrowing now lives in D9's dated amendment and under criterion 10, not only in the Status table — and the count is right everywhere the scan is quoted (`41`, `forty-one`); the criterion-8 "text is false" clause survives literally, because the sentence a reader meets first still says `answers forty` and is corrected on the next line by the same keep-the-earlier-wording convention.

#### Anything else seen

- HEAD in the worktree is `44f3582`, not the `56bb8ab` the brief named; the one commit between them touches only `PLANS.md` (+2/−2), so every probe above is against the same source tree.
- Two untracked files were already present in the worktree before this run: `benchmarks/rounds/01-2.md` and `benchmarks/rounds/15-2.md` (not opened).
- Test counts have moved past what the ledger's "red-then-green" column and the plan's Status table record: `test_isolation_matrix` runs 32 (ledger: 16 → 31; Status row 10 still says `(16)`), `test_additive_migration` runs 24 (Status row 7 still says `(19)`), `versionBrowser.spec.ts` 31, `builderCanvas.spec.ts` 66, `builderShell.spec.ts` 34, `builderApi.spec.ts` 38.
- The version row's `created_at` is a naive stamp (`2026-09-03T08:12:35.735669`, no offset) while the local clock read 16:12 MPST (UTC+8) — the UTC reading `d9672a0` adds is what keeps `just now` from reading `8 h ago`.
- `POST …/unpublish` moves every version's status to `draft`, not only the head's: `/versions` after unpublish answered `[(2,'draft'),(1,'draft')]` where before it was `[(2,'draft'),(1,'published')]`.
- Anonymous creation and anonymous write on an unowned row remain open on this `SYNTHETIC` backend (`POST … as nobody [201]`, `PUT … as nobody [200]`, `DELETE … as nobody [204]`); the amended D9 table says this is the auth-off rule, and the auth-on refusal is only asserted by `test_nobody_can_create_an_unowned_row_while_auth_is_required`, not probed here (no auth-on backend was available).
- The import notice has no action button; the notice mechanism's `action` slot is exercised only by the viewed-version notice (`Back to v2`).
- In the palette captures the second library row (`Minimal gated agent copy`) is cut by the viewport bottom at y=900 and no scrollbar is painted; the list's `overflow-y: auto` and the passing E2E (sixth row reachable inside the palette box) are the only evidence it scrolls.
- The delete-confirm capture's re-fit shrinks the five cards to roughly 100px wide (from 186px), which keeps every node visible at the cost of card legibility; the row measured visibility, not legibility.
- The E2E seeded six `LP …` documents on the shared :8099 store and deleted them in `afterEach`; the four probe scripts left two documents behind that were not deleted (`ug_d3b3c08d`, bob's duplicate of the unowned row, and `ug_c9bc9d4a`, alice's D-15-8 document).
- Playwright printed a Vite warning about `import "./syntheticUser"` without a file extension in `e2e/vite.e2e.config.ts:4`; it did not affect the run.

## Result

| dim | ours | ref | delta | gate (8) | critic | round 1 |
| --- | ---: | ---: | ---: | --- | --- | --- |
| 4 | 7 | 4 | −3 | **not met** (ours under 8; beats the reference) | visual, blind | 7 / 3 |
| 11 | 10 | 10 | 0 | met, no defect | engineering | 10 / 10 |
| 12 | 6 | 10 | 4 | **not met** | engineering | 7 / 10 |
| 14 | 10 | 10 | 0 | met, no defect | engineering | 9 / 10 |
| 16 | 7 | 10 | 3 | **not met** | engineering | 8 / 10 |

Dimension 4 did not move (7, with the reference read one point higher
than round 1 by a different critic); 14 reached the reference; 12 and 16
each fell a point, carried by one new defect the round-1 critic had not
found — an import of the file the export wrote that never names the nodes
whose credential was stripped (engineering D-1), which the critic scored on
both rows because it is a data defect *and* a ticked criterion whose text
a one-line probe breaks.

**The suite figures the critic measured agree with the builder's**, which
it never saw: Python `Ran 1642 tests … OK (skipped=6)`, Vitest `62 passed
(62)` / `1157 passed (1157)`, `vue-tsc -b --force` and `npm run build`
exit 0, `e2e/isolation.spec.ts` `4 passed`, the version-browser step
`1 passed`, `34 tests in 5 files` / `8` tagged `@launch`,
`tests.pg.test_two_writers` `Ran 5 tests … OK` against `PostgreSQL 18`.
Neither suite trap fired: no `Errno 22` lock error appeared (the
`%TEMP%\crewai` file held 90 streams at the start of the session, well
under the ceiling), and the flaky `builder.spec.ts` cases were not run,
because only the version-browser step of that file is a criterion's.

### Ledger decisions, by the closing rule

A row closes only when its dimension scores at or above the reference, the
verifier shows the defect absent, the critic does not land it again, and
the fixing commit is named. Applied literally:

**Closed (5), each pointing at the capture or output in which the defect
is absent:**

| id | dim | closed by | absent in |
| --- | --- | --- | --- |
| D-15-1 | 4 | `e27a1f4` | `15-version-viewing-v1-read-only`: amber bar, lock, `viewing v1 of v2 · read-only`, Publish disabled, palette dimmed, locks on every card |
| D-15-3 | 4 | `d9672a0` | `15-version-rows-after-restore`: `4 nodes · restored from v1`, `5 nodes · saved`, `4 nodes · created`; server rows carry `source`, `name`, `node_count` |
| D-15-5 | 4 | `9555fb6` | `15-import-result`: icon, dismiss, two lines, the whole name |
| D-15-6 | 4 | `4a1f328` | `15-document-menu`: `Delete…` under a rule, in the error colour at rest |
| D-15-7 | 14 | `95dfd70` | verifier: `PUT` / `publish` as bob and `DELETE` as alice on an unowned row all `[403] … read-only for every signed-in user; Duplicate it` |

**Kept open, landed again (2)** — the round-1 clause is absent per the
verifier, and the round-2 critic landed the same surface with a new
sentence, now appended under the old one in the ledger:

- **D-15-2** — the verifier finds all five nodes on screen with both strips
  docked (round 1: 3 of 5 hidden); the critic's D-3 finds the re-fit that
  put them there shrinks the cards to 136 / 116 / 100 px and the graph
  "cannot be read at this viewport". Hidden became unreadable; the row's
  subject — the operator cannot see the graph under a docked strip — is
  the same, so it stays open.
- **D-15-4** — the three names are distinct and whole (`… imported`,
  `… copy`, the bare name; verifier), and the list scrolls
  (`overflow-y: auto`, `e2e/builder-layout.spec.ts` "saved-graphs
  library" `1 passed` with the sixth row reachable); the critic's D-8 finds
  the second row cut at y=900 with "no scrollbar or 'N more' affordance
  visible" in five captures. The clipped-row clause of the round-1 sentence
  survives as a visible fact even though the DOM scrolls, so it stays open.

**Held open by the dimension score (5)** — the verifier shows each defect
absent (two of them *partly*: the literal "six" and "forty" still open
criteria 7 and 8, corrected on the next line by the plan's stated
keep-the-earlier-wording convention), and the critic did not land any of
them again, but dimension 12 scored 6 and dimension 16 scored 7 against a
reference of 10, and the rule as written closes nothing on a dimension
below its reference. Each row now carries a dated note naming the
verifier's output and the commit that would close it:

| id | dim | would close by | verifier |
| --- | --- | --- | --- |
| D-15-8 | 12 | `a324aa0` | `?version=99` → `has no version 99; the newest is v2 [404]` on all four routes; strangers keep the constant |
| D-15-9 | 12 | `c44deaf` | every malformed import → one sentence, 35–91 bytes, `count of 'ECHO-CRED' in body: 0` |
| D-15-10 | 12 | `9e85e9f` | `DELETE with head draft, v1 registered … [409]`; unpublish → graph `[404]` → `DELETE [204]`; confirm copy is the server's sentence |
| D-15-11 | 16 | `c6d4038` | PARTLY: dated amendment under criterion 7 names five tables; "six" survives above it |
| D-15-12 | 16 | `3e988b4` | PARTLY: scan `41`, §6 `forty-one`, amendments under 8 and 10 and in D9; "forty" survives above it |

This is the one place the Integrator applied a rule whose consequence
looks wrong on the page — five fixed defects stay open because of three
new ones on the same dimension — and it is recorded rather than bent,
because the rule is the owner's and the ledger is only useful if it is
applied the same way every round. The owner can rule that a verified-absent
row closes on the verifier's output alone; until then the rows stay.

**New (10)**, each the critic's sentence verbatim, in the critic's order,
visual first as in round 1:

| id | dim | from | subject |
| --- | --- | --- | --- |
| D-15-13 | 4 | visual D-2 | save has no pointer action |
| D-15-14 | 4 | visual D-6 | a lifecycle toast displaces the Build/Run toggle |
| D-15-15 | 4 | visual D-9 | the gallery buries the user's own graphs; identical timestamps; trash-only rows |
| D-15-16 | 4 | visual D-10 | the delete strip warns on the wrong state |
| D-15-17 | 4 | visual D-12 | the problems dock reads "Ready to publish" while read-only |
| D-15-18 | 4 | visual D-13 | the same refusal laid out two ways, with the internal id printed |
| D-15-19 | 12 | engineering D-1 | importing the file the export wrote never names the nodes that lost a credential |
| D-15-20 | 16 | engineering D-1 | criterion 2 is ticked over a sentence the import test itself contradicts |
| D-15-21 | 12 | engineering D-2 | create, save and validate echo a refused body up to 256 KiB |
| D-15-22 | 12 | engineering D-3 | every refusal on the import/duplicate/export/restore/open paths clears itself after 4 s |

The engineering critic gave its D-1 two score lines (2 points on row 12,
3 on row 16); the ledger takes one dimension per row, so it is two rows
with the same location. The visual critic's A-side defects (its D-1, D-4,
D-5, D-7, D-11) are the reference's and get no row. Its D-3 and D-8 are
appended to D-15-2 and D-15-4 rather than given rows.

**Recorded without a row**, because no critic scored them: the verifier's
four observations — `POST …/unpublish` moves *every* version's status to
`draft`, not only the head's; an anonymous caller on this auth-off backend
can still create, write, publish and delete an unowned row (the amended D9
table declares this the auth-off rule, and the auth-on refusal is asserted
only by a test); the Status table's per-module test counts are stale
(`test_isolation_matrix` 16 → 32, `test_additive_migration` 19 → 24); and
the import notice has no action button although the notice mechanism
carries one. The first and third belong with the next build; the second is
decision 26's.

**Open after round 2: 17** (2 landed again, 5 held, 10 new). Closed: 5.
Neither critic is retired: neither passed the artefact.

**Round 2 outcome: the gate is not met.** Dimensions 4, 12 and 16 are under
8; 11 and 14 are at 10. `PLANS.md` records round 2 with seventeen open
defects; the ledger read top to bottom is the round-3 build list for plan
15, and round 3's critic is the CrewAI power user.



