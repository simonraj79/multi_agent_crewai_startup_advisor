# Open comparison defects

One row per defect the critic landed and nobody has closed. A row leaves
this table only when a later round scores the dimension at or above the
reference and the fix is named. Format is fixed; the method is in
`README.md`.

| id | plan | round | date | dim | ours | ref | delta | located defect | status | closed by |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| D-01-1 | 01 | 1 | 2026-09-03 | 14 | 6 | 10 | 4 | Any caller — Bob or nobody — can confirm that Alice's published workflow id exists and enumerate its node/state names, unthrottled: `CreateRunRequest.inputs`'s validator (`src/brief_crew/service/models.py:213-222`) calls `config.declared_reserved_run_input_keys(workflow_id)` (`config.py:1707`), which returns the target graph's own registered keys (`register_workflow_reserved_run_input_keys`, `config.py:1630-1647`, populated at publish with `__builder__` and every `out__<node>`) regardless of who is asking, and pydantic runs before `create_run`'s rate limiter and its `workflow_visible_to` 404 (`service/app.py:1065-1106`) — so `POST /api/sessions/x/runs {"workflow_id":"ug_0b94fc25","inputs":{"idea":"p","__builder__":"x"}}` answers `422 … reserved control key __builder__` for Alice's graph and `404 workflow not found` for `ug_deadbeef`, `out__scoper` → 422 while `out__market` → 404, the anonymous caller gets the same 422, and 15 rapid probes as a fresh user never trip the 10/60 s limiter that a clean 404 probe trips on the 11th. `graph.py:413-427`'s own docstring says the predicate "must not become a second oracle for which ids exist"; this is that oracle, one layer up. **Round 2 (2026-09-03): defect absent, row held open by the dimension score.** Verifier: as Bob and as nobody, `__builder__`, the declared `out__idea`, the undeclared `out__not_a_node_here` and a clean body on Alice's id and on `ug_deadbeef` all answer the byte-identical `{"detail":"workflow not found"} [404]`; a fresh user's 15 rapid `__builder__` probes read `404 ×10, 429 ×5`, the same as both clean controls; only the owner still gets `422 … reserved control key`. The round-2 critic's own probes agree (`identical across foreign/invented for every body? True`; carol/dave/erin `404 ×10, then 429`). Dimension 14 scored 9 against a reference of 10 (carried by D-01-5), and a row closes only when its dimension is at or above the reference; would close by `0fe472f`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `0fe472f` |
| D-01-2 | 01 | 1 | 2026-09-03 | 14 | 6 | 10 | 4 | With a handoff naming Alice's workflow, Bob's Run console at 1440×900 (default fit) draws the kicker **PUBLISHED GRAPH / Test graph**, a 14-node fabricated topology (Scoper … Validation brief, `mock-of-d8be7621`), a WORKFLOW card reading "Test graph", and an enabled green **Launch**, with only a 12 px "Mock Mode" header chip and a dismissible "workflow not found" banner to say otherwise — `studioApi.ts:204-240` treats every non-401 probe failure (this 404 included) as "no backend" and sets `mode='mock'`, and `StudioView.vue:83` labels the mock graph with the foreign handoff's name. Seen in `critic01_bob_console_on_alice_wf.png` (Playwright, cookie `critic-bob`, `/#/`, 1440×900). The E2E asserts the launch's 404 sentence and never asserts what the canvas shows before it. (CLAUDE.md remaining-work item 43.) **Round 2 (2026-09-03): defect absent, row held open by the dimension score.** Verifier, Bob's console pointed at Alice's workflow (1440×900): `live_status_chip: "Ready"`, `vue_flow_node_count: 0`, no `mock-` string anywhere in the page, the server's sentence `This graph cannot be launched from here. The server answered: workflow not found` in a banner with no dismiss, `Launch` carrying `disabled=""` before and after Review + a valid idea; capture `benchmarks/ours/01/2/01-bob-console-on-alice-workflow-1440x900-dark.png`. Dimension 14 scored 9 against 10; would close by `413c4dd`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `413c4dd` |
| D-01-3 | 01 | 1 | 2026-09-03 | 16 | 7 | 10 | 3 | Criterion 6 is marked `done` in `.agent/plans/01-auth-and-workspaces.md` (Status table, row 6, "done, one deviation") while its text — "a frame whose `details` carries **every** D6 key name round-trips as `***`" — is not met: `src/brief_crew/events/redaction.py:23-33` states "`fields` is NOT here, and plan 01 D6 asks for it", `is_secret_key('fields')` → `False`, and `tests/service/test_secret_redaction.py:79-83` pins the opposite of the criterion. The deviation may be right; the criterion text was left unamended and ticked anyway, and the Status header reads "Criteria complete". **Closed round 2 (2026-09-03):** Absent at `56bb8ab` (verifier): criterion 6 in `.agent/plans/01-auth-and-workspaces.md` now carries the dated `fields` exclusion in its own text, `is_secret_key('fields')` is still `False` by that amendment, `ListTests::test_the_plan_records_the_fields_exclusion_beside_the_pin` … `ok`, `Ran 23 tests … OK`; `a952c74` regenerated the Status row's count afterwards. Dimension 16 scored 10 and the round-2 critic landed nothing on it. | closed | `dbe803d` |
| D-01-4 | 01 | 1 | 2026-09-03 | 16 | 7 | 10 | 3 | Criterion 1 is marked `done` ("the 404 fires before any admission counter moves") on the strength of `tests/service/test_workflow_ownership.py` (14), which sends only a clean `{"idea": …}` body; neither it nor `test_isolation_matrix.py` sends a state key (`grep -n "out__\\\|__builder__\\\|reserved"` over both → no output), and D-1's single curl falsifies the criterion's claim for any body carrying one. A ticked criterion that a one-line probe breaks is the process failure row 16 exists to catch. **Closed round 2 (2026-09-03):** Absent at `56bb8ab` (verifier): `grep -n "out__\\\|__builder__\\\|reserved"` over the two files now returns 23 lines; `StateKeyProbeTests`, `StateKeyProbeIsChargedTests` and `AnonymousStateKeyProbeTests` send `__builder__`, a declared `out__<node>` and `out__not_a_node_here` as Bob and as nobody, `Ran 23 tests … OK`; Status row 1 says in its own words that the round-1 tick rested on clean bodies only. Dimension 16 scored 10. | closed | `fd7c74f` |
| D-01-5 | 01 | 2 | 2026-09-03 | 14 | 9 | 10 | 1 | A sign-out ends the token and nothing else, so the next person on the same browser inherits the previous user's flow and run pointers in plaintext. Located: `frontend/src/composables/useAuthGate.ts:97-103` (`endSession` = `clearAccessToken(); await signOut()` — no `localStorage`/`sessionStorage` key is removed anywhere on the auth path); `frontend/src/composables/useBuilderPersistence.ts:502-506` (`writeDraft(model.document)` on every successful load, so `localStorage['builder-draft:<id>']` holds the whole document — nodes, prompt inputs, `config.credential_id` — even for a *saved* document, not only unsaved work); `frontend/src/composables/useValidatorRun.ts:526` and `589-593` (`runId.value = context.runId` is set before the fetch and never reset in the `catch`, so a restore the server refused with 404 still renders the previous user's run id through `StatusPanel.vue:248` and relabels the button "Relaunch"). Shown by `node shared_browser.cjs`: after the identity changed to bob-c2 the console printed `run id on screen: 0e2d40fc` (Alice's run), `handoff banner: Running your published graph alice gated graph`, `error banner: run not found`, and storage still held `builder-draft:ug_dbc5c011: 955 bytes; contains cr_421c20d8? true`. The server refused every request (all 404), so this is client residue rather than an API hole, and it needs a shared browser profile — which is why it costs one point and not more; the measurement swapped the harness's synthetic-user cookie because a real Google sign-out cannot run here, and the code of `endSession` shows a real sign-out leaves the same keys behind. **Closed round 3 (2026-09-04):** ABSENT on all four clauses, measured by the row verifier in Chromium at 1440x900 against the free `SYNTHETIC=1` backend, with a working control on each. (A) One sign-out removed EIGHT keys across both storages - Alice's four scoped ones and all four planted legacy shapes - keeping only `builder-vocabulary` (nobody's) and Bob's own session id. (B) After an in-place cookie swap Bob's readable set is two keys, neither a draft nor anything of hers; `contains alice credential cr_d1795e63? false`; restore bar 0, library 0; Alice's copy still on disk under her prefix, which proves it was one profile. (C) `run id on screen: (none rendered)`, `handoff banner: (none)`, with the same handoff planted under Bob's own key rendering it verbatim, so the absence is not a broken locator. (D) A refused restore gives `error banner: run not found`, badge `error`, button Launch not Relaunch, the refused id absent from the status panel's markup and the pointer cleared. The round-3 engineering critic did not land it again. Closed under the corrected rule: dimension 14 scored 9 against a reference of 10, and a dimension below its reference no longer holds another row open. | closed | `e22a32f` |
| D-15-1 | 15 | 1 | 2026-09-03 | 4 | 7 | 3 | -4 | A's read-only state is contradicted by its own document bar: while viewing v1 the status chip reads "✓ saved · v1" in the identical style to the editable "saved · v2", Publish stays full-colour and enabled-looking (x965–1060, y66–102), the palette is undimmed and node cards show no lock — the only read-only cues are a 39px banner (y178–217) and greyed inspector fields 700px away on the right; the document bar, `A-version-viewing-v1-read-only-1440x900-dark.png`, 1440x900, node cards 186px. **Closed round 2 (2026-09-03):** Absent in `benchmarks/ours/15/2/15-version-viewing-v1-read-only-1440x900-dark.png` (1440×900): the bar is amber-tinted with a lock glyph, the chip reads `viewing v1 of v2 · read-only` in amber, Publish is dimmed and disabled, the palette is dimmed under a read-only banner and every node card carries a lock; `versionBrowser.spec.ts` 31 passed (verifier). The round-2 critic did not land it; its D-12 — the problems dock still reading `Ready to publish` while read-only — is the new row D-15-17. | closed | `e27a1f4` |
| D-15-2 | 15 | 1 | 2026-09-03 | 4 | 7 | 3 | -4 | A's docked surfaces stack and push the canvas down without re-fitting: the Versions panel (y120–245, 125px) alone hides Result and Transform 1 below the y818 canvas bottom (2 of 5 nodes), and with the delete strip docked beneath it (y250–355, 105px) the canvas starts at ~y520 and only Request and Confirm remain (3 of 5 hidden) — the operator confirming a delete cannot see most of the graph they are deleting; the version list and the delete strip, `A-version-browser-head-1440x900-dark.png` and `A-delete-confirm-docked-1440x900-dark.png`, 1440x900, node cards 186px. **Round 2 (staff frontend engineer), landed again as its D-3, dim 4, A 4 / B 7:** B — the read-only preview is illegible: cards go 186px (panel closed) → 136px with the Versions panel docked at y120–245 → 116px viewing v1 with the banner row added (Request x610–726, title about 7px, subtitle about 4px) → 100px with Versions plus the delete strip (x568–668); every docked strip is taken out of the canvas by the auto-fit, so the graph you opened an old version to look at cannot be read at this viewport. `B-version-browser-head`, `B-version-viewing-v1-read-only`, `B-delete-confirm-docked`; 1440x900; cards 136/116/100px. The round-1 clause (nodes hidden below the canvas bottom, no re-fit) is absent at `56bb8ab` — every node is on screen in all three captures — and the fix traded hidden for unreadable. **Round 3 (2026-09-04), CrewAI power user: PARTLY absent, and landed again on the same surface for the third round running, so the row stays open.** Verifier, real browser, 1440x900: the round-2 sentence is ABSENT - cards go 185 -> 176 -> 176 -> 176px, not 186 -> 136 -> 116 -> 100, and the node title holds at 11.00px in every docked state rather than at about 7, the fit stopping exactly on the 11/15 floor the fix declares. The round-1 clause is PRESENT: with Versions docked alone 2 of the 4-node template's nodes are outside the pane; with Versions plus the delete strip on the 16-node validator template 15 of 16 are, and a space-held pan clears the bottom overflow but leaves 10 outside because they move above the top. The visual critic, blind to this row, wrote the same thing from the pixels as its D-2 - `Request` whole at (577-759, 320-382) with the panel closed, cut to (493-665, 412-450) with it open, gone but for a sub-label fragment in the read-only state - and called it clipping instead of re-fitting, at 1440x900. Three rounds, three sentences, each fix a genuine trade on one surface (hidden, then unreadable, then clipped-but-legible) rather than a failure to fix; the plan records the trade and amended its own E2E assertion into a disjunction to match. Closing it needs a different shape of answer - a strip that overlays instead of displacing, or a minimap that shows what is off-pane - not a fourth re-tuning of the same fit. **Round product-1 (2026-09-04), design director: PRESENT for the fourth round running, and for the first time with NOTHING docked.** Measured at `5cb7092`, 1440x900 dark: the 16-node `Idea validator` opens at scale **0.733** - title **11.00 px**, the declared floor - with **7 of 16 nodes wholly off-pane**; pressing the new `7 off-pane · Fit` chip brings all 16 on-pane at scale **0.436**, card 105 px, title **6.53 px**. On the 6-node `Sequential pipeline` with the Versions panel docked, the pane is 380 px tall: 2 of 6 wholly inside, 2 clipped (75 %, 41 %), 2 wholly outside; `Fit` -> 6 of 6 on-pane at scale **0.376**, card 90 px, title **5.64 px**. The graph is never simultaneously whole and legible at this viewport. The `N off-pane · Fit` chip (`b981916`, `d1a2096`, `80609d9`) is a real fix and is one of the two answers this row itself named, but it trades clipped-but-legible for whole-but-unreadable, because nothing bounds the fit from below - Flowise pins `minZoom: 0.5` on the v2 canvas precisely so this cannot happen. Also filed as the round's **P-03** (dim 6, ours 7 / ref 8). The round-3 closing-shape sentence stands: a strip that overlays instead of displacing, or a navigable minimap viewport, not a fifth re-tuning of the same fit. | open | — |
| D-15-3 | 15 | 1 | 2026-09-03 | 4 | 7 | 3 | -4 | A's version rows cannot be told apart by what they display: v1 and v2 both read "3 Sept, 00:19" (minute resolution), both carry DRAFT, neither has a label, message or node count, and the only differing value is "1.4 KB" vs "1.2 KB" in ~11px right-aligned text at x1020–1050 — choosing which to restore is guesswork; the version list, `A-version-browser-head-1440x900-dark.png`, 1440x900, zoom n/a. **Closed round 2 (2026-09-03):** Absent in `benchmarks/ours/15/2/15-version-rows-after-restore-1440x900-dark.png`: the rows read `v3 HEAD DRAFT Minimal gated agent · 4 nodes · restored from v1 · just now · 1.2 KB`, `v2 … 5 nodes · saved`, `v1 … 4 nodes · created`, and the server row carries `source`, `name` and `node_count` (verifier's `/versions` output); the round-2 critic's only remark was a rejected observation that both rows read `just now`. | closed | `d9672a0` |
| D-15-4 | 15 | 1 | 2026-09-03 | 4 | 7 | 3 | -4 | A's library is ambiguous after duplicate + import: the import keeps the exact original name so two rows read "Minimal gated agent" distinguished only by a ~10px v1/v2 badge, the duplicate's row truncates to "Minimal gated age…" (~110px of text) so the word "copy" is lost, and a third row is clipped at y≈895 with no scrollbar — the delete target is not identifiable from the list; the saved-graphs library, `A-import-result-1440x900-dark.png` and `A-duplicate-opened-1440x900-dark.png`, 1440x900, zoom n/a. **Round 2 (staff frontend engineer), landed again as its D-8, dim 4, A 4 / B 7:** B — the palette library clips at the viewport: LIBRARY heading y775, list from y813, so 87px remain; with two or more saved graphs the second entry's title sits at y881 with its card cut at y900 in five captures, and no scrollbar or "N more" affordance is visible. `B-document-menu-published`, `B-duplicate-opened`, `B-import-result`, `B-palette-library-after-duplicate-and-import`, `B-delete-refused-published-docked`; 1440x900; card 186px. The round-1 name clauses are absent at `56bb8ab` — the import is "Minimal gated agent imported" and the duplicate "Minimal gated agent copy", both whole (`15-palette-library-after-duplicate-and-import`, `15-gallery-library-after-duplicate-and-import`); the clipped-row clause survives. **Closed round 3 (2026-09-04):** ABSENT (verifier, real browser, 1440x900). With three saved graphs the palette library is a 168px box, heading y662, all three rows fully on screen, the last ending y876 rather than cut at y900; with nine it is a genuine `overflow-y: auto` scroller whose last row is reachable, under a `library-count` badge reading 9 - the visible N-more affordance the round-2 sentence says is missing, stated as a total. The round-1 clipped-row clause is gone too: clipping now happens inside a scrollable box, not at the viewport edge. Neither round-3 critic landed it again. | closed | `38d7635` |
| D-15-5 | 15 | 1 | 2026-09-03 | 4 | 7 | 3 | -4 | A's success feedback is a bare text string in the header with no icon, no dismiss and no action, and it truncates: `imported alice.builder.json as a new draft, "Minimal g…` (x1047–1335, ~288px) cuts off the new document's name — the one fact needed to find it in a library that now has two rows with that name (D-7); the header toast, `A-import-result-1440x900-dark.png`, 1440x900, zoom n/a. **Closed round 2 (2026-09-03):** Absent in `benchmarks/ours/15/2/15-import-result-1440x900-dark.png`: a bordered toast with a check icon and a × dismiss, wrapped to two lines, reading the whole name `imported alice.builder.json as a new draft, “Minimal gated agent imported”.`; `builderImport.spec.ts` 10 passed (verifier). The round-2 critic's D-6 — the toast displaces the Build/Run toggle — is the new row D-15-14. | closed | `9555fb6` |
| D-15-6 | 15 | 1 | 2026-09-03 | 4 | 7 | 3 | -4 | Both menus put Delete directly under a safe item with identical styling, no separator and no destructive colour: A "Delete…" 34px below "Duplicate" (x745–957, y248–282); B "Delete Agents" 51px below "Export Agents" (x1213–1440, y447–497). A's type-to-confirm catches the slip; B's one blue click (D-2) does not; the actions menus, `A-document-menu-1440x900-dark.png` and `B-agentcanvas-settings-menu-saved-1440x900-dark.png`, 1440x900, zoom n/a. **Closed round 2 (2026-09-03):** Absent in `benchmarks/ours/15/2/15-document-menu-1440x900-dark.png` and `15-document-menu-published-1440x900-dark.png`: `Delete…` sits under a full-width rule at y≈288 in the error colour at rest, 79 px below Duplicate with Unpublish between them; `documentLifecycle.spec.ts` 13 passed (verifier). The B clause is the reference's and gets no row. | closed | `4a1f328` |
| D-15-7 | 15 | 1 | 2026-09-03 | 14 | 9 | 10 | 1 | Any signed-in user can overwrite, publish and delete an unowned document, and on this backend anybody can create one: `store._visible_to` (`src/brief_crew/builder/store.py:716-724`) is the sole gate on `load`, `save` (:483), `mark_published` (via `load`, :541) and `delete` (:586), so "an unowned row is readable by everyone" silently became "controllable by everyone"; seen with `POST /api/builder/workflows` (no header) → `[201] ug_8c0b01be`, then bob `PUT …/ug_8c0b01be` → `[200] version 2`, then alice `DELETE …/ug_8c0b01be` → `[204]`, then anonymous `GET` → `[404]`; `tests/service/test_isolation_matrix.py:266-284` covers unowned rows for GET/versions/export only, never PUT/DELETE/publish. **Closed round 2 (2026-09-03):** Absent in the verifier's probe: on a row created with no header, `PUT` as bob, `publish` as bob and `DELETE` as alice all answer `{"detail":"document ug_76354aa7 has no owner and is read-only for every signed-in user; Duplicate it to get a copy you own"} [403]`; `test_isolation_matrix.py` Ran 32 OK; the engineering critic scored dimension 14 at 10 and landed nothing on it. | closed | `95dfd70` |
| D-15-8 | 15 | 1 | 2026-09-03 | 12 | 7 | 10 | 3 | An unknown version on the caller's own document is refused with the wrong sentence: `_guarded` (`src/brief_crew/service/builder_api.py:1027-1028`) flattens every `DocumentNotFound` to the constant `"document not found"`, discarding the version the store put in the message (`store.py:89-95`); `GET /api/builder/workflows/ug_6730814a?version=99` → `{"detail":"document not found"} [404]` while that document's head was 2 and on screen, and the client renders exactly that string (`BuilderView.vue:575 say(messageOf(error, …))`, `serverLimits.ts:49`). Same for `/export?version=`, `/duplicate?version=`, `/publish?version=`. The tests pin only the status (`tests/service/test_builder_duplicate.py:101-104`, `test_builder_export_route.py:148-150`). **Round 2 (2026-09-03): defect absent, row held open by the dimension score.** Verifier: `GET /api/builder/workflows/ug_c9bc9d4a?version=99` as the owner → `{"detail":"document ug_c9bc9d4a has no version 99; the newest is v2"} [404]`, the same sentence on `/export`, `/duplicate` and `/publish`, and a stranger still gets the constant `document not found`. Dimension 12 scored 6 against a reference of 10 (carried by D-15-19, D-15-21, D-15-22), and a row closes only when its dimension is at or above the reference; would close by `a324aa0`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `a324aa0` |
| D-15-9 | 15 | 1 | 2026-09-03 | 12 | 7 | 10 | 3 | A malformed or foreign import file is refused with FastAPI's raw pydantic list that echoes the whole body back: `BuilderImportRequest` (`builder_api.py:262-285`) is typed, and `service/app.py` registers no `RequestValidationError` handler (grep `exception_handler` → nothing; `_validation_detail` at `app.py:448` serves only the WebSocket), so `POST /api/builder/workflows/import` with a raw document (no envelope) answered `[422]` with the entire document under `"input"`, `{"foo":1}` answered three fragments, and `needs_credentials` ×500 echoed all 500 entries — the very reflection the module's own docstring (`builder_api.py:196-201`) says it avoids for `document`. The client's `readErrorDetail` (`src/data/serverLimits.ts:50-54`) reduces this to `Field required; Field required; Extra inputs are not permitted`, naming nothing. **Round 2 (2026-09-03): defect absent, row held open by the dimension score.** Verifier: a raw document with no envelope → `{"detail":"export: Field required"} [422]` (35 bytes, no `input` key); `{"foo":1}` → the same sentence; a 500-item `needs_credentials` → `{"detail":"needs_credentials: List should have at most 24 items after validation, not 500"} [422]` with `count of 'ECHO-CRED' in body: 0`. The round-2 critic landed the same reflection on the create, save and validate doors, which is the new row D-15-21. Dimension 12 scored 6 against 10; would close by `c44deaf`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `c44deaf` |
| D-15-10 | 15 | 1 | 2026-09-03 | 12 | 7 | 10 | 3 | The docked delete copy promises the opposite of what the server does, and the 409 it then shows is defeated by its own remedy: `BuilderView.vue:1464-1466` and `TemplateGallery.vue:345` read "Deleting a published graph unregisters it; running graphs are not affected" (capture `A-delete-confirm-docked-1440x900-dark.png`, 1440×900), while `DELETE` on a published head answers `[409] … cannot be deleted; save a new version to return it to draft, then delete it` (`builder_api.py:801-809`); following that sentence — `save [200]` → `/versions [(2,'draft'),(1,'published')]`, `GET /api/workflows/{id}/graph` still `[200]` — then `delete [204]` unregisters the still-live, still-launchable v1 (`graph after delete [404]`), which is the exact hazard the 409's docstring names (`:783-787`). One condition (`stored.status == STATUS_PUBLISHED`, `:801`) instead of `registered_workflow(id) is not None` alone is what makes the guard one save deep. **Round 2 (2026-09-03): defect absent, row held open by the dimension score.** Verifier: after publish then a save (head v2 draft, v1 registered), `DELETE` → `{"detail":"document ug_fa884040 is published - v1 is registered as a launchable workflow - and cannot be deleted; unpublish it first, then delete it"} [409]` and `GET /api/workflows/{id}/graph` still `[200]`; `POST …/unpublish` → `[200]`, then the graph `[404]`, then `DELETE` → `[204]`; `grep -rn "running graphs are not affected" frontend/src` → none, the confirm copy is the server's sentence. Dimension 12 scored 6 against 10; would close by `9e85e9f`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `9e85e9f` |
| D-15-11 | 15 | 1 | 2026-09-03 | 16 | 8 | 10 | 2 | Criterion 7 is ticked "done" while its text is false: it says `create_all` "yields all six new tables"; the DDL declares five (`GAUNTLET_TABLES`, `src/brief_crew/service/persistence.py:406-412`) and the test asserts five (`tests/service/test_additive_migration.py:186 test_all_five_tables_arrive_with_their_indexes_and_constraints`). The `## Status` table records "done, one correction … The test is right" and leaves the criterion unamended. **Round 2 (2026-09-03): PARTLY absent, row held open by the dimension score.** Verifier: a dated amendment under criterion 7 names the five tables (`GAUNTLET_TABLES` = 5, `test_all_five_tables_arrive_with_their_indexes_and_constraints`, Ran 24 OK); the first sentence still reads `yields all six new tables` by the plan's convention of keeping the earlier wording visible above its correction. Dimension 16 scored 7 against 10 (carried by D-15-20); would close by `c6d4038`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `c6d4038` |
| D-15-12 | 15 | 1 | 2026-09-03 | 16 | 8 | 10 | 2 | Criterion 8 is ticked "done" while its text is false: it says the knob scan "answers forty"; my run of the scan answers `41`, and `docs/tech-stack.md:306` says forty-one. Criterion 10 is likewise ticked with a self-declared narrowing ("test-inputs row is covered at the table level … Stage 1 has no route"). The Status stamp is "Criteria complete", not "Built", which is honest — but every row is ticked. **Round 2 (2026-09-03): PARTLY absent, row held open by the dimension score.** Verifier: the scan answers `41` and `docs/tech-stack.md:306` reads `forty-one`; dated amendments sit under criteria 8 and 10 and D9 carries the per-verb unowned-row table (`test_isolation_matrix.py` Ran 32 OK); the first sentence of criterion 8 still reads `answers forty` by the same keep-the-earlier-wording convention. Dimension 16 scored 7 against 10; would close by `3e988b4`. Closed 2026-09-03 by the owner's ruling on the round-2 verifier's output. | closed | `3e988b4` |
| D-15-13 | 15 | 2 | 2026-09-03 | 4 | 7 | 4 | -3 | B — save has no pointer action: the document bar (y65–102) carries "unsaved changes · Ctrl+S" at x495–640 and no Save control; the kebab on a saved document (x745–956, six items) has no Save; none of the 13 captures shows one. A saves in one click (icon x1333–1365, y17–49). The kebab on a never-saved document is uncaptured, so a Save item there is inferred-absent. `B-canvas-document-bar`, `B-document-menu`; 1440x900; card 186px. **Closed round 3 (2026-09-04):** ABSENT (verifier, real browser). `document-save-button` at x725 y68 w32 h32 with `aria-label=Save`, and `saved-by-pointer true` - the chip reached saved after a click with no keyboard - and `Save` is the FIRST kebab item on a saved document and on a never-saved one, which is the case the row could only infer. The chip still reads `unsaved changes . Ctrl+S`, but as a status line beside a control rather than as the only mention of saving. The visual critic saw the same control and did not land it. | closed | `0902bc4` |
| D-15-14 | 15 | 2 | 2026-09-03 | 4 | 7 | 4 | -3 | B — a lifecycle toast displaces the Build/Run toggle: it sits at x1157–1284 with no toast and moves to x835–963 after a restore (−322px), x843–971 after a duplicate (−314px) and x702–830 after an import (−455px; two-line toast x848–1285, y8–44); a persistent mode control jumps on every success. `B-canvas-document-bar`, `B-version-rows-after-restore`, `B-duplicate-opened`, `B-import-result`; 1440x900; card 186px. **Closed round 3 (2026-09-04):** ABSENT (verifier, real browser). `.workspace-switch` sits at exactly x1154 in all three states - no notice, a 299px notice after a duplicate, a 385px notice after an import - because the notice is now `position: absolute` and centred in the header, so it occupies no space in the flow. `e2e/builder-layout.spec.ts:243` green. | closed | `9b65607` |
| D-15-15 | 15 | 2 | 2026-09-03 | 4 | 7 | 4 | -3 | B — the gallery buries the user's own graphs: four template cards occupy y147–595, so "Saved here" starts at y659 and 64px rows at 76px pitch show two and a half entries, the third cut at y900; all three rows read "3 Sept, 07:47" so they cannot be ordered; each row exposes only a trash icon (x1187–1219), so duplicate, export and versions cost one extra open first. `B-gallery-library-after-duplicate-and-import`, `B-gallery-delete-refused-unpublish`; 1440x900; rows 64px. **Closed round 3 (2026-09-04):** ABSENT on every clause (verifier, real browser). `galleryLibraryDomIndex 0` - the author's own graphs are now the FIRST section at y82-366 and the templates the second at y457; all three rows fully on screen, the last ending y366 rather than cut at y900; stamps distinct and ordered newest-first (`just now` / `10 s ago` / `12 s ago`); each row exposes Versions, Duplicate, Export and Delete rather than a trash icon alone. The visual critic's complaint that those new glyphs are unlabelled is a defect on the REMEDY and is the new row D-15-26, not this row's own defect. | closed | `f6ed348` |
| D-15-16 | 15 | 2 | 2026-09-03 | 4 | 7 | 4 | -3 | B — the delete strip warns on the wrong state and blocks after the work: on the v2 DRAFT (pill x60–95, y847) the strip (y262–290) says "A published graph cannot be deleted; unpublish it first", which cannot apply; on the published v3 the refusal appears after the attempt while the bar already shows "v3 is live" (x568–638, y83) — whether the type-to-confirm was completed before the server refused is inferred, because the published-doc confirm state is uncaptured. `B-delete-confirm-docked`, `B-delete-refused-published-docked`; 1440x900; cards 100/146px. **Closed round 3 (2026-09-04):** ABSENT on both clauses (verifier, real browser). On the v2 draft the strip carries `draft-delete-problem null` - no publication warning at all. On the published document the refusal is the FIRST thing shown, with `published-has-name-field 0` and `published-has-submit 0`, so there is no type-to-confirm to complete and the attempt cannot precede the refusal; the remedy the server names is offered where the refusal is read. The row's own inferred clause no longer has anything to infer about. | closed | `3cec3f7` |
| D-15-17 | 15 | 2 | 2026-09-03 | 4 | 7 | 4 | -3 | B — the read-only state contradicts itself: the problems dock reads "✓ Ready to publish" (x344–464, y851) while the Publish button (x965–1059, y83) is disabled and the bar reads "viewing v1 of v2 · read-only". `B-version-viewing-v1-read-only`; 1440x900; card 116px. **Closed round 3 (2026-09-04):** ABSENT (verifier, real browser). While a stored version is on screen the dock headline in the same 164px slot reads `viewing v1 . read-only` and its body `Read-only. This is v1; publishing and editing act on head.`; the string Ready to publish appears nowhere in the dock, and the bar, the dock and Publish's disabled state agree. The visual critic, scoring the same capture blind to this row, listed the dock's padlock among six concurrent read-only signals it called the strongest thing in either capture set. | closed | `236a02f` |
| D-15-18 | 15 | 2 | 2026-09-03 | 4 | 7 | 4 | -3 | B — the same refusal is laid out two ways: docked, the server sentence sits above the buttons (text y163, buttons y181–216); in the gallery it sits below (buttons y800–837, text y856); both print the internal id "ug_309cd317" and say "it is still published" twice. `B-delete-refused-published-docked`, `B-gallery-delete-refused-unpublish`; 1440x900; card 146px / row 64px. **Closed round 3 (2026-09-04):** ABSENT (verifier: HTTP for the sentence, real browser for both layouts). Both surfaces now put the server's sentence ABOVE the buttons (docked text y271-289 over buttons y298-334; gallery text y394-412 over buttons y421-459); neither prints a `ug_` id, and the server's own 409 answers `contains internal id: False`; and the duplicated Not-deleted-it-is-still-published label renders empty, leaving one statement of the fact in one vocabulary. | closed | `0376987` |
| D-15-19 | 15 | 2 | 2026-09-03 | 12 | 6 | 10 | 4 | Importing the file the export wrote never names the nodes that lost a credential, so the import notice built for that case is unreachable and the graph silently drops from the author's key to the platform key. `builder/export.py:175-181` — `_scrub` records a node only `if item not in (None, "")`, and the export has already nulled the key, so on re-import there is nothing to note; `service/builder_api.py:669-676` says the opposite ("the file's nulled `credential_id` / `server_id` keys are the evidence, and the strip reads them directly") and discards the envelope's own list; the UI notice at `frontend/src/components/builder/BuilderView.vue:730` renders only when `needs_credentials.length > 0`. Command: `probe2.py` → `EXPORT: needs_credentials=['scoper']  document.nodes[1].config.credential_id=None` then `IMPORT of that exact file as bob: [201] needs_credentials=[]  imported credential_id=None  problems=[]` (and `needs_credentials=[]` when alice re-imports her own file). **Closed round 3 (2026-09-04):** ABSENT (verifier, the row's own probe). Alice exports a document whose `scoper` node names her key; `IMPORT as bob -> [201] needs_credentials=['scoper']` where the row measured `[]`, and alice re-importing her own file answers the same. In Chromium the notice the row calls unreachable paints: `1 node needs a credential you own - the export carried none. Choose a key  scoper`, with the fix button wired. The engineering critic re-derived the identical result independently while blind to this row, and confirmed `InspectorRail.focusField` now has exactly one real caller. | closed | `d458639` |
| D-15-20 | 15 | 2 | 2026-09-03 | 16 | 7 | 10 | 3 | Criterion 2 as written ("Import of *that file* as user B creates a draft … and a problem group naming each `needs_credentials` node") is false, and the module it names pins the defect: `tests/service/test_builder_import.py:153` (`test_the_envelope_s_own_list_is_never_trusted`) asserts `body["needs_credentials"] == []` for an envelope declaring `["scoper"]`. (The same defect as D-15-19, scored on row 16 by the critic: a ticked criterion whose text a one-line probe breaks.) **Closed round 3 (2026-09-04):** ABSENT (verifier). `test_the_envelope_s_own_list_is_never_trusted` no longer exists anywhere under `tests/`; `test_builder_import.py:153` is now `test_the_file_the_export_wrote_names_the_node_that_lost_a_key`, asserting `[scoper]` in both directions. The one surviving `== []` assertion is the opposite case, correctly asserted. The engineering critic scored criterion 2 TRUE end to end. | closed | `d458639` |
| D-15-21 | 15 | 2 | 2026-09-03 | 12 | 6 | 10 | 4 | Three of the four persistence doors reflect the request back on a refusal, up to the 256 KiB body ceiling. `service/builder_api.py:196` (`BuilderDocumentRequest`) is a typed FastAPI body used at `:599` (create), `:728` (save) and `:943` (validate); anything pydantic refuses *above* the untyped `document` dict — a non-dict `document`, a bad `expected_version`, an extra key — is answered by FastAPI's default 422 with `"input"` echoed, which is exactly what `:1079` `_import_envelope` was hand-written to avoid for import (D-15-9) and what the class's own docstring at `:199-203` claims the handlers prevent. Command: `probe.py` → `POST /api/builder/workflows as bob-p -> {"detail":[{"type":"dict_type","loc":["body","document"],"msg":"Input should be a valid dictionary","input":"ECHO-ECHO-…` `[422]` / `create with a 200KB string document -> 422 body is 200113 bytes; echoes the string: True` (same figure on `PUT` and `/validate`; `22113 bytes` for the extra-key case). **Closed round 3 (2026-09-04):** ABSENT (verifier, the row's own probe, all five shapes on all three doors). A 200 KB `document` string carrying an `ECHO-ECHO-DO-NOT-REFLECT` marker answers `[422] body=57 bytes; echoes marker: False` on create, on `PUT` and on `/validate`; the bad `expected_version` answers 100 bytes and the extra key 53. 200113 bytes became 57, and the `input` key is gone from every shape. The engineering critic's thirteen independent malformed-input probes agree: one sentence naming the field, never echoing the body. | closed | `2a19f0a` |
| D-15-22 | 15 | 2 | 2026-09-03 | 12 | 6 | 10 | 4 | Every refusal on the import, duplicate, export, restore and open-version paths is shown in a notice that clears itself after 4000 ms, with no persistent surface to re-read the server's sentence — while delete (docked confirm, capture `B-delete-refused-published-docked`) and a save conflict (`ConflictDialog.vue:269` shows `conflict.detail`) both get one. `frontend/src/components/builder/BuilderView.vue:415-430` (`say()` → `window.setTimeout(… noticeAction.value ? 8000 : 4000)`), error sites `:557`, `:626` (which also omits `kind: 'error'`, so a refused version open renders as `info`), `:649`, `:690`, `:725`, `:764`. Command: `grep -n "8000 : 4000" …/BuilderView.vue` → `429:  }, noticeAction.value ? 8000 : 4000)`; corroborated by `frontend/e2e/isolation.spec.ts:352-353` ("Asserted right after the refusal because the notice retires itself after four seconds"). **Closed round 3 (2026-09-04):** ABSENT (verifier, real browser). A refused import - the server's sentence about an unknown `export` field - survived 6.5 seconds byte-identical, with class `builder-notice is-error` and its own dismiss control. The `8000 : 4000` line the row greps for still exists and the grep still hits it, but it is now unreachable for a refusal: `if (noticeKind.value === 'error') return` sits one line above it. Every former error site carries `kind: 'error'`, the `:626` omission included. | closed | `36d3478` |
| D-15-23 | 15 | 3 | 2026-09-04 | 4 | 8 | 3 | -5 | B - the browser calls the live version a draft. In `B-document-menu-published` the header (474-640, 76-92) says `v3 is live` and the library row (12-212, 720-767) says `v3 PUBLISHED`, while the v3 version row carries `HEAD` + `DRAFT` at (341-419, 183-199). Badge the published version on its row, or drop status badges from version rows entirely. 1440x900; card 146px. | closed | `0610084` |
| D-15-24 | 15 | 3 | 2026-09-04 | 4 | 8 | 3 | -5 | B - version rows carry no diff. Rows differ only by `5 nodes`/`4 nodes` and `1.4 KB`/`1.3 KB` at (836-1058, 176-239) - two clicks per candidate to eyeball, and it does not scale. Compute a node/edge delta per row. `B-version-browser-head`; 1440x900; card 176px. | closed | `8fc03fa` |
| D-15-25 | 15 | 3 | 2026-09-04 | 4 | 8 | 3 | -5 | B - the `⋮` menu occludes the version rows it operates on. Menu (730-940, 108-365) hides `restored from v1` at (836-930, 184-198), and nothing says which version `Export .builder.json` writes. Left-align the menu clear of the panel and label the item `Export head`. `B-document-menu-published`; 1440x900. | closed | `09d970d` |
| D-15-26 | 15 | 3 | 2026-09-04 | 4 | 8 | 3 | -5 | B - four unlabelled 28px gallery glyphs, delete 34px from export. Group at x1085-1220, y166-194, no labels, no visible tooltip, 1440x900. Label them or separate the trash by at least 16px. `B-gallery-library-after-duplicate-and-import`; rows 68px. | closed | `80609d9` |
| D-15-27 | 15 | 3 | 2026-09-04 | 4 | 8 | 3 | -5 | B - ragged template grid. The `Idea validator` caveat (1000-1213, 648-825) makes that card about 3.4x its siblings, whose content ends by y650. Clamp to two lines with a disclosure, or equalise heights and scroll inside the block. `B-gallery-library-after-duplicate-and-import`; 1440x900. | closed | `80609d9` |
| D-15-28 | 15 | 3 | 2026-09-04 | 11 | 5 | 10 | 5 | A graph with an MCP or Skill node cannot be imported from its own export. Location: `src/brief_crew/builder/export.py:183-198` (`_scrub`) against `document.py:635` (`server_id: NodeId`, required and non-nullable), `:656` (`skill_id: NodeId`, the only field on `SkillConfig`) and `:159` (`extra="forbid"`). Command, live against 8098, same account, created / exported / re-imported through the routes: `tool EXPORT ok -> IMPORT 201`; `mcp EXPORT ok -> IMPORT 422 {"detail":"nodes.3.server_id: Input should be a valid string"}`; `skill EXPORT ok -> IMPORT 422 {"detail":"nodes.3.skill_id: Field required"}`. Mechanism, both halves measured: `_scrub:195` drops `skill_id` because "the skill is re-resolved by `skill_name`", but `grep -rn skill_name src/` finds that string only inside `export.py` (a constant in `__all__` with no consumer plus two comments) and `grep -rn skill_name frontend/src/` finds nothing - `SkillConfig` has no such field and `extra="forbid"` means it never can, so the exported node is `"config": {}`; `mcp` breaks twice, `server_id` nulled where the schema requires a string AND a `server_hint` key written that `McpConfig` forbids, so restoring `server_id` by hand still fails. Both kinds are shipped and one keystroke away (`nodeKinds.ts:485` `mcp` hotkey `M`, `:511` `skill` hotkey `K`, both in `NODE_KIND_ORDER`), with no feature flag. It survived 1742 green Python tests, 1318 green frontend tests and 59 green E2E tests because the only test of the skill path (`tests/builder/test_export.py:215`) asserts against a hand-written dict where `skill_id` sits on an *agent* config beside a `skill_name` that is not a schema field. | closed | `aeaedc9` |
| D-15-29 | 15 | 3 | 2026-09-04 | 12 | 7 | 10 | 3 | The import refusal for the product's own file is a JSON array index. The 422 detail reaches the author unmodified through `frontend/src/services/builderApi.ts:427` (`readErrorDetail`) - no path rewrites it, because the message is not a publish refusal and not a 409. Command/output as D-15-28: `{"detail":"nodes.3.skill_id: Field required"}`. It counts separately because every other refusal in this feature names something the author can see - the delete 409 says `“Deletable” is live as v1 and cannot be deleted; unpublish it first, then delete it` - while this one says `nodes.3`, for a node the canvas calls Skill, describing a field the author never typed, on a fault they did not cause and cannot fix. The same server on a genuinely malformed upload writes `the file is not JSON (Expecting value at line 1 column 1)`, so the product knows how to write these sentences. | closed | `b67f8ab` |
| D-15-30 | 15 | 3 | 2026-09-04 | 16 | 6 | 10 | 4 | The export's only test of the skill path proves a mechanism that does not exist. Location `tests/builder/test_export.py:215-218` and its fixture `raw_document()` at `:63-128`. Command: `python -c "from tests.builder.test_export import raw_document; from brief_crew.builder.document import BuilderDocument; BuilderDocument.model_validate(raw_document())"` -> `fixture DOES NOT PARSE: 8 validation errors for BuilderDocument`; `grep -rn skill_name src/` -> only `export.py:56, :197, :250`; `grep -rn skill_name frontend/src/` -> nothing. Fix: add one export test over a document `BuilderDocument` accepts, for every kind in `NODE_KIND_ORDER`, asserting the stripped document re-parses - it fails today for two kinds and would have caught D-15-28. Keep the raw-dict fixture, whose forward-coverage argument is sound, but stop letting it be the only one. | closed | `aeaedc9` |
| D-15-31 | 15 | 3 | 2026-09-04 | 16 | 6 | 10 | 4 | Criterion 11's count is wrong, and unstruck. `npx vitest run tests/builderPersistence.spec.ts` -> `Tests  37 passed (37)`; `grep -c "^\s*\(it\|test\)(" frontend/tests/builderPersistence.spec.ts` -> `37`. The criterion says **33** and says the file "passes unchanged"; both cannot be true. Criteria 7 and 8 in the same document are struck-through corrections of exactly this failure, made under a ruling that a convention guaranteeing a fourth is the wrong convention. This is the fourth. | closed | `f98ef5e` |
| D-15-32 | 15 | 3 | 2026-09-04 | 11 | 5 | 10 | 5 | The CI job that would prove criterion 9 is green when it proves nothing, and does not run on this branch. Location `.github/workflows/ci.yml:95` and `:136`. Command: `Remove-Item Env:\TEST_DATABASE_URL; python -m unittest tests.pg.test_two_writers -v` -> `Ran 5 tests in 0.000s / OK (skipped=5) / EXIT CODE: 0`. `ci.yml:95` is `if: github.ref == 'refs/heads/main'`, so no branch build has ever run it; `:136` is the job's only step, and the run above is what it does when the variable does not reach the process. `ci.yml:133-135` names this hazard in a comment and closes nothing; a guard step (`[ -n "$TEST_DATABASE_URL" ]`, or a `--fail-on-skip` shim) is two lines. **The critic scored this on dimensions 11, 14 AND 16; it is filed once, under 11**, because it is one location and one two-line fix, and three rows would make one fix have to close three things - the other two dimensions carry it in their score. | closed | `13ae696` |
| D-15-33 | 15 | 3 | 2026-09-04 | 16 | 6 | 10 | 4 | Criterion 6 claims a v2 upgrade the shipped path does not perform. Location `src/brief_crew/builder/upgrade.py:117` (the `while schema != BUILDER_DOCUMENT_SCHEMA` guard) and `tests/builder/test_upgrade.py:82-90`. `test_v1_passes_through_while_the_service_still_compiles_v1` asserts `BUILDER_DOCUMENT_SCHEMA == SCHEMA_V1` and that every fixture comes back unchanged, so on the shipped path the answer is v1, not v2; the v2 claim needs `mock.patch.object`, and cleanliness under the patch is asserted for one fixture in four. All four are clean under the patch when run by hand (`problems=0 errors=[]` each) - the substance is fine, the sentence is not. Rewrite it as "upgrades cleanly, and would upgrade cleanly to v2 with the constant moved", which is what the module's own honest note already says. | closed | `9ce52c5` |
| D-15-34 | 15 | 3 | 2026-09-04 | 14 | 9 | 10 | 1 | Rubric 14 names credentials; the isolation matrix does not. `tests/service/test_isolation_matrix.py` - its two docstring tables have no credentials row, and `grep -c credential` over the file answers **1** across 32 tests. The coverage exists in fact (live: `bob GET/DEL/TEST cr_9abb7cbc -> 404 {"detail":"credential not found"}`, `anon GET -> 401`, `bob list -> []`), so this is a gap in the file that owns the claim rather than in the behaviour, which is why it is ranked last and costs one point. | closed | `b49c45b` |
| D-01-6 | 01 | 3 | 2026-09-04 | 14 | 9 | 10 | 1 | A vault field name is pinned as *not* secret, and it is the one holding the header secret. `src/brief_crew/config.py:2568-2569` defines `"http_header": ("name", "value")` and `"mcp_header": ("name", "value")`, so for those kinds `value` **is** the secret; `src/brief_crew/events/redaction.py` does not list it in `SECRET_KEYS` and it matches no `SECRET_KEY_SUFFIXES` entry, and `tests/service/test_secret_redaction.py:184` pins that deliberately with `self.assertEqual(is_secret_key(field), field not in HEADER_PAIR)` over `HEADER_PAIR = frozenset({"name", "value"})` at `:96`. Command: `python -c "from brief_crew.events.redaction import is_secret_key; from brief_crew.config import CREDENTIAL_FIELDS; print(CREDENTIAL_FIELDS['http_header'], is_secret_key('value'))"` -> `('name', 'value') False`. Unreachable today - the only caller of `resolve_credential` outside its own module is `builder/runtime.py:694` inside `_agent_api_key`, which refuses any kind but `openrouter` - but **nothing pins that unreachability**: no test asserts a non-`openrouter` kind is unconsumable, and the only mention of `http_header` under `tests/` is a comment at `test_secret_redaction.py:91`. The day a second resolver lands, a secret flows under a key name the serializer and the persistence sanitiser both pass through in clear, and the test that would catch it is asserting the opposite. Fix: add `value` to `SECRET_KEYS` and give the gate `derived` entry's name slot a different key, or pin the unreachability with a test that fails the moment a second kind becomes resolvable. | closed | `70a0e55` |
| D-01-7 | 01 | 3 | 2026-09-04 | 14 | 9 | 10 | 1 | `/ws` has no `Origin` check, and unowned runs are streamable cross-origin. `src/brief_crew/service/app.py:1700-1704` says so in its own docstring, and `tests/service/test_cors.py` pins that `/ws` is not covered by `CORS_ALLOW_ORIGINS`. The narrowing is real - an owned run is `4404` to everyone but its owner, measured over four handshakes - but the ownership gate at `:1749` is `if record.user_id is not None and (…)`, so a run with **no** owner is streamable by any origin that can name a valid `run_id` and its matching `session_id`. Unowned runs are every run on an auth-off checkout and every run in `SYNTHETIC` mode, which is the configuration this entire judging round was conducted in. Two guesses are needed, so it is weak, but it is the one acknowledged-open hole in an otherwise closed surface. (Also CLAUDE.md remaining-work item 13; the critic found it from the source's own docstring, not from that document.) | closed | `cd3ce75` |
| D-01-8 | 01 | 3 | 2026-09-04 | 16 | 7 | 10 | 3 | Criterion 9's amendment states five pin counts and two are wrong. The criterion says "Pinned by `frontend/tests/authGate.spec.ts` (5), `frontend/tests/identityStorage.spec.ts` (9), `frontend/tests/builderRunHandoff.spec.ts` (3), the D-01-5 blocks in `builderPersistence.spec.ts` (4) and `runRecovery.spec.ts` (4)". Command: `for f in authGate identityStorage builderRunHandoff; do npx vitest run tests/$f.spec.ts \| grep -oE "Tests  [0-9]+ passed"; done` -> `Tests  4 passed` (criterion says 5), `Tests  8 passed` (criterion says 9), `Tests  3 passed` (correct). No `it.skip`/`it.todo`/`.only` in either file. The four `authGate` tests are a complete set for the property - the substance is right and the number is wrong, in an amendment written to repair a text/reality drift. | closed | `f98ef5e` |
| D-01-9 | 01 | 3 | 2026-09-04 | 16 | 7 | 10 | 3 | Criterion 12 has no verifier and, unlike criterion 13, does not admit it. Command: `grep -rln "tech-stack" tests/` -> nothing. Criterion 13 labels itself a review checklist item; criterion 12 makes an equally manual claim about `docs/tech-stack.md` §6 unlabelled, so it reads as verified. Only the `src` half is checkable and it holds - the knob scan reports 41 including `CREDENTIALS_MASTER_KEY`. Criterion 6 shows the project already owns the technique: its pin test opens the plan file, regex-extracts the criterion and asserts the note is present. | closed | `e767dbb` |
| D-01-10 | 01 | 3 | 2026-09-04 | 16 | 7 | 10 | 3 | Criterion 6's second exclusion is carried by a test constant, not a dated amendment. The `fields` amendment states its own rationale - "Recorded as a dated amendment rather than an edit to the sentence, because round 1 ticked this criterion with its text unchanged and the deviation living only in a Status row" - and that reasoning applies verbatim to `value` (see D-01-6), unapplied. Fix: a second dated amendment, or better, make `value` secret and close D-01-6 with it. | closed | `ed002d8` |

**Round 1 ran on 2026-09-03 for plans 01 and 15** (`rounds/01-1.md`,
`rounds/15-1.md`), the first rounds of the gauntlet; the two plans were
built and integrated before any of the plans the README expected to go
first, so the first entries came from them rather than from
`rounds/02-canvas-1.md`. Every row above is the critic's sentence
verbatim; rows for plan 15 dimension 4 carry the visual critic's blind
scores (ours 7, Flowise 3.1.4 3), where ours beat the reference and still
missed the gate. Each row's number is the critic's ranking within its
plan and dimension group, so the build list for round 2 is the table read
top to bottom.

**Round 2 was BUILT for plan 15 on 2026-09-03** on `gauntlet/plans`,
`a952c74` → `90699e9`, one commit per id, and every D-15 row above stays
`open`: closing is the critic's, after it re-runs each command itself. The
fixing commits, for the `closed by` column when a row does close; where an
id has more than one, the **last** is the one to name (the earlier ones
record the measurement that corrected them):

| id | fixing commit(s) | red-then-green |
| --- | --- | --- |
| D-15-1 | `e27a1f4` | `frontend/tests/versionBrowser.spec.ts` — the shell test pins the chip, bar, lock, disabled name and Publish; palette and card tests standalone |
| D-15-2 | `569198f`, `fa6829f`, `b249d89` | `frontend/tests/builderCanvas.spec.ts` — a driven `ResizeObserver` honouring `disconnect`; the dock arriving after mount |
| D-15-3 | `57246b8`, `d9672a0` | `tests/service/test_additive_migration.py::VersionSourceColumnTests`, `test_builder_versions.py::WhatARowSays`; `versionBrowser.spec.ts` same-minute rows, save origins, the real clock |
| D-15-4 | `6b25a5b`, `7cf3326` | `test_builder_duplicate.py` (+2), `test_builder_import.py::ImportedNamesStayDistinct`; title assertions in `builderShell` and `versionBrowser`; `e2e/builder-layout.spec.ts` measures the wrap and the reachable last row |
| D-15-5 | `9555fb6` | `frontend/tests/builderImport.spec.ts` — full name, success class, icon, dismiss; `versionBrowser.spec.ts` — the "Back to v2" action |
| D-15-6 | `4a1f328` | `frontend/tests/documentLifecycle.spec.ts` — separator precedes Delete, destructive class at rest |
| D-15-7 | `95dfd70` | `tests/service/test_isolation_matrix.py` 16 → 31 — a row per verb for the unowned case |
| D-15-8 | `a324aa0` | the version-404 tests in `test_builder_duplicate.py` and `test_builder_export_route.py` assert the sentence; `test_builder_versions.py` covers the four routes; the matrix keeps B's constant |
| D-15-9 | `c44deaf` | `tests/service/test_builder_import.py::MalformedFilesEchoNothing` and the three refusal cases asserting a string |
| D-15-10 | `9e85e9f` | `tests/service/test_builder_unpublish.py` (12), `test_builder_delete.py`'s flipped save test; `documentLifecycle.spec.ts`, `builderShell.spec.ts`, the route mirror at 13 |
| D-15-11 | `c6d4038` | docs — a dated note under criterion 7 |
| D-15-12 | `3e988b4` | docs — dated notes under criteria 8 and 10 (the D9 amendment is in `95dfd70`) |

Captures for the six visual rows, 1440x900 dark, are under
`docs/comparison/ours/round2/` (ignored by the `*.png` rule), taken by the
untracked `frontend/e2e/_round2_capture.spec.ts`. Two of the later commits
above exist only because those captures were looked at.

**Round 2 was JUDGED for plans 01 and 15 on 2026-09-03** (`rounds/01-2.md`,
`rounds/15-2.md`; persona staff frontend engineer; plan 15 split into a
blind visual critic for dimension 4 and an engineering critic; and, new
this round, one row verifier per plan that re-ran every round-1 row's own
probe knowing exactly what had been claimed and changed). Scores: plan 01
→ 14 = **9**, 16 = **10**; plan 15 → 4 = **7** against Flowise 3.1.4 at 4
(blind), 11 = **10**, 12 = **6**, 14 = **10**, 16 = **7**. The verifiers
found every one of the sixteen round-1 defects absent as written (two
partly: the literal "six" and "forty" still open plan 15's criteria 7 and 8
above their dated amendments). By the closing rule — a row closes only when
its dimension scores at or above the reference, the defect is shown absent
and the fixing commit is named — **seven rows closed** (D-01-3, D-01-4,
D-15-1, D-15-3, D-15-5, D-15-6, D-15-7), **two stay open landed again** with
the round-2 sentence appended under the round-1 one (D-15-2, D-15-4),
**seven stay open held by a dimension under its reference** (D-01-1, D-01-2
on 14 at 9; D-15-8, D-15-9, D-15-10 on 12 at 6; D-15-11, D-15-12 on 16 at
7), each carrying a dated note with the verifier's output and the commit
that would close it, and **eleven new rows opened** (D-01-5, D-15-13 …
D-15-22), each the critic's sentence verbatim. Open after round 2:
**20** (3 + 17). The paragraph above about the round-2 build is kept for
its commit table; its "every row stays open" is superseded by the status
column. Neither plan met the gate; round 3's critic is the CrewAI power
user.

**Round 3 was JUDGED for plans 01 and 15 on 2026-09-04** (`rounds/01-3.md`,
`rounds/15-3.md`; persona **CrewAI power user**; plan 15 split into a visual
critic for dimension 4 and an engineering critic for 11, 12, 14 and 16; plan 01
one engineering critic for 14 and 16; and one row verifier per plan, re-running
each open row's own probe knowing exactly what had been claimed and changed).
Scores: plan 01 -> 14 = **9**, 16 = **7**; plan 15 -> 4 = **8** against Flowise
3.1.4 at **3**, 11 = **5**, 12 = **7**, 14 = **9**, 16 = **6**.

The verifiers found **twelve of the thirteen open rows absent as written**, one
partly. By the closing rule as corrected - a row closes when its OWN defect is
absent, and a dimension below its reference is an open defect in its own right
but does not by itself hold another row open - **twelve rows closed** (D-01-5,
D-15-4, D-15-13 ... D-15-22), **one stays open landed again** (D-15-2, for the
third round and with the third different sentence), and **seventeen new rows
opened** (D-15-23 ... D-15-34, D-01-6 ... D-01-10). Open after round 3: **18**
(5 + 13). Neither plan met the gate; round 4's critic is the first-time user who
has never seen a node graph.

**The round's own finding, and it is the one nothing had seen: `D-15-28`.** Two
of the ten node kinds - `mcp` and `skill`, both first-class palette items with
hotkeys - export with 200 and then cannot be imported anywhere, by anybody,
including their own author. It is a cross-plan defect: plan 15's export was
built against seven node kinds and plan 03's ten landed above it. It survived
1742 green Python tests, 1318 green frontend tests and 59 green E2E tests,
because the only test of that path asserts over a hand-written dict that raises
eight validation errors against `BuilderDocument`.

**The blind leg was compromised and the round files say so.** Two files
auto-load into every subagent's context before its brief and neither could be
prevented from that session: the MAIN tree's stale `CLAUDE.md`, whose
flow-builder section states the `$10.00` ceiling and the 13-billable /
8-escalation bounds - figures printed on our own canvas in the captures - and
the user's `MEMORY.md`, which names defect ids in exactly the `D-15-13` form.
The worktree's own `CLAUDE.md` reached nobody. Round 3's row-4 score therefore
stands as a **critique, not a blind comparison**; the defects are
coordinate-backed and kept. The limitation is now recorded in `README.md` under
*The critic*, because it is structural - the `Agent` tool takes no working
directory - and a re-run would arrive contaminated identically.

**Not run in round 3, for the third round running:** `tests/pg/test_two_writers.py`
against PostgreSQL 18. Docker Desktop was down; `docker ps` answered
`open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file
specified`. Criterion 9's first half is unverified and dimensions 11 and 14 each
carry that as a gap rather than as a pass.

**Round PRODUCT-1 was JUDGED on 2026-09-04** (`rounds/product-1.md`; persona
**design director**; the first WHOLE-PRODUCT round rather than a per-plan one,
run in `D:\MultiAgentSystem-wt\judge` on `judge/product-1` at HEAD `5cb7092`,
against a `SYNTHETIC=1` backend on 8092). A verifier re-ran each of the eighteen
open rows' own located commands. **Seventeen were ABSENT and are closed here**
(D-15-23 ... D-15-34, D-01-6 ... D-01-10); **D-15-2 is PRESENT for the fourth
round running** and stays open, with the round's measurement appended to its
sentence above. Open after product-1: **1**.

All sixteen dimensions were scored in one pass. Ours / reference: 1 = 9/7,
2 = 9/8, 3 = 9/6, 4 = 8/7, 5 = 8/8, 6 = **7**/8, 7 = 9/7, 8 = 8/8, 9 = 9/7,
10 = 9/6, 11 = 9/10, 12 = **7**/10, 13 = 8/10, 14 = 9/10, 15 = **6**/10,
16 = **4**/10. **The gate is not met**: four dimensions are under 8 and every
engineering dimension is under its reference. The round ranked eleven defects
**P-01 ... P-11** rather than filing them as ledger rows, so they are not in the
table above; `rounds/product-1.md` carries each with its file, viewport and
measurement, and `PLANS.md`'s `## Log` records the round.

**Dimensions 1-10 were NOT scored blind, and the round file says so first.** The
structural limitation `README.md` records under *The critic* applies in full -
every agent spawned here auto-loads the MAIN tree's `CLAUDE.md` and the user's
`MEMORY.md` before its brief, and between them they name the `$10.00` ceiling,
the 13-billable / 8-escalation bounds and the `D-15-13` id form. The reference
scores come from `docs/flowise-notes.md` and `docs/chatdev-notes.md`, which are
readings of those products' source, not captures taken side by side.

The fixing commits, each checked to be an ancestor of `a3c9a31`. Where an id has
more than one, the **last** is the one named in `closed by` above; the
red-then-green column is the verifier's own measurement at `5cb7092`:

| id | fixing commit(s) | red-then-green |
| --- | --- | --- |
| D-15-23 | `0610084` | version rows read `v2 · head · draft` and `v1 · published`; the published version is badged on its own row and carries no `DRAFT`; header, library row and version rows agree |
| D-15-24 | `f60d0d0`, `8fc03fa` | the v2 row reads `Sequential pipeline · 6 nodes · saved · -1 node, -1 edge · just now · 5.4 KB` - a computed node/edge delta per row, not just a byte size |
| D-15-25 | `1846302`, `09d970d` | with the version panel open, menu box (925-1137, 106-367) against version rows (276-1060, 443-506): **overlap 0 px²**; the item reads `Export head (v2)` |
| D-15-26 | `8415706`, `80609d9` | four glyphs at **32x32** (was 28), each with a native `title` and an `aria-label` naming the document; Export right edge 1149 to Delete left edge 1172 = **23 px** (the row asked for >= 16) |
| D-15-27 | `677657f`, `80609d9` | template card heights equal within each grid row (401/401/401/401, 459/459, 401/401); worst ratio **1.14x**, was ~3.4x; `.template-caveat` clientHeight 66 vs scrollHeight 198, so it scrolls inside a clamped box |
| D-15-28 | `d875762` (schema), `aeaedc9` (round trip) | live through the routes on 8092, all ten kinds in one document: `CREATE 201 -> EXPORT 200 -> IMPORT 201`, kinds preserved, `needs_credentials: ['n_agent','n_tool','n_mcp']`; `McpConfig.server_id` and `SkillConfig.skill_id` nullable, `skill_name` a real field |
| D-15-29 | `b67f8ab` | three malformed creates name the node the canvas names - `the "Idea" input node: field - Field required (nodes.0.field)` - and no bare `nodes.3` anywhere |
| D-15-30 | `aeaedc9` | `tests/builder/test_export.py::RoundTripEveryKindTests`, three tests including one that fails when a kind is added; `raw_document()` still deliberately does not parse and a test now says so |
| D-15-31 | `f98ef5e` | plan 15 criterion 11 reads `(~~33~~ **37**)`; `npx vitest run tests/builderPersistence.spec.ts` -> `Tests  37 passed`. Struck through, not silently edited |
| D-15-32 | `13ae696` | with `TEST_DATABASE_URL` unset, bare `unittest tests.pg.test_two_writers` -> exit 0 over five skips, `scripts/run_without_skips.py` -> **exit 1** naming all five. Residual, and it is a ruling not a defect: `ci.yml:95` is still `main`-only (decision 25), so the guard has never executed in CI - logged as **P-10** |
| D-15-33 | `9ce52c5` | criterion 6 reads "...upgrades ~~to a clean v2 document~~ **cleanly, and would upgrade cleanly to v2 with `BUILDER_DOCUMENT_SCHEMA` moved**", with a dated amendment stating the shipped path's actual answer |
| D-15-34 | `b49c45b` | `grep -c -i credential tests/service/test_isolation_matrix.py` -> **22** (was 1); a `credentials` row in the docstring table and `CredentialRoutes` asserting it in the same three-caller shape; module green at **36 tests** |
| D-01-6 | `70a0e55` | `CREDENTIAL_FIELDS['http_header']` and `['mcp_header']` are `('name', 'header_value')` and `is_secret_key('header_value')` -> **True**; swept every kind, no credential field other than `name` is non-secret. The row's own suggested fix was tried and measured wrong - six tests red across four modules - and the field was renamed instead |
| D-01-7 | `cd3ce75` | four live handshakes against `ws://127.0.0.1:8092/ws` with `CORS_ALLOW_ORIGINS=()`: no `Origin` -> **4404**, `http://evil.example.com` -> **4406**, same-origin -> **4404**, `Origin: null` -> **4406**. The origin is asked *before* the run is looked up, so it is not an existence oracle either |
| D-01-8 | `f98ef5e` | plan 01 criterion 9 reads `authGate.spec.ts (~~5~~ **4**)`, `identityStorage.spec.ts (~~9~~ **8**)`, `builderRunHandoff.spec.ts (3)`; measured **4**, **8**, **3** passed |
| D-01-9 | `e767dbb` | `grep -rln "tech-stack" tests/` -> `tests/test_env_knob_doc.py` (was nothing); ran it, **6 tests, OK**, including `test_the_heading_count_is_the_scan_count` |
| D-01-10 | `ed002d8` | criterion 6 carries an amendment saying the second exclusion **no longer exists** rather than explaining it - `70a0e55` removed it by renaming the field. The better of the two fixes the row named |

**The suite was RED at the round's HEAD, and that is P-02 rather than an
environment artefact.** `npx playwright test` at `5cb7092`: **121 passed,
7 failed**, 128 tests in 23 files. One is P-01 (`builder-skills.spec.ts:170`);
the other six are the committed visual baselines `d1a2096` regenerated for the
off-pane strip and 21 further commits then moved. `ea4202f` re-baselined exactly
those six at 22:31, three minutes after the round's HEAD - **the suite has not
been re-run since, so no green is claimed here.**

## Conventions

- `id` is `D-<plan>-<n>`, monotonically increasing per plan; never reused.
- `ours` / `ref` are the critic's scores for that dimension, 1–10; `delta` is `ref − ours`.
- `located defect` is the critic's sentence verbatim, with viewport, zoom and component; a sentence without a location is not a defect and does not get a row.
- `status` is `open` or `closed`; a closed row stays in the table with its closing commit so a regression is a reopen, not a new id.
- A feature cannot pass its gate while any row for its plan is `open`, even at a score of 8 or above.
