T2.5 - the character's state is driven by run events only, never by a clock.

REGENERATED BY RV3, fourth pass, branch run-shell/cast at 8ae40ec, 2026-09-05T18:31:06+08:00.
W4 wrote the first copy of this file at 7da9bf5; this is the criterion's own
grep re-run at the branch head, from the repository root, output pasted
verbatim. Round five rewrote 61 lines of pip.ts (the bun crest replaces the
halo), so the character system is exactly what needed re-checking.

===========================================================================
1. THE CRITERION'S OWN GREP
===========================================================================

$ grep -rnE 'setTimeout|setInterval|Date\.now' \
      frontend/src/characters \
      frontend/src/components/AgentCharacter.vue \
      frontend/src/composables/useRunChoreography.ts

frontend/src/composables/useRunChoreography.ts:253:  const now = options.now ?? (() => Date.now())
frontend/src/composables/useRunChoreography.ts:284:  const receiptTimers = new Map<string, ReturnType<typeof setTimeout>>()
frontend/src/composables/useRunChoreography.ts:799:      setTimeout(() => {

THREE hits, all in one file, at the same three lines every pass has found.

===========================================================================
2. THE HALF THAT MATTERS - the character system itself, with the two other
   ways a clock reaches code added to the pattern
===========================================================================

$ grep -rnE 'setTimeout|setInterval|Date\.now|performance\.now|requestAnimationFrame' \
      frontend/src/characters/ frontend/src/components/AgentCharacter.vue

(no output; exit status 1)

ZERO. frontend/src/characters/pip.ts and AgentCharacter.vue cannot read a
clock by any of the five routes, which is what makes the same identity draw
byte-identical markup in two processes (G3) and what makes every state a
function of run events.

===========================================================================
3. EACH OF THE THREE HITS, AND WHY IT IS NOT STATE
===========================================================================

  :253   const now = options.now ?? (() => Date.now())
         An INJECTABLE clock with a real-clock default, used to timestamp a
         handoff so a token can be expired. Every test passes its own. It
         decides no character state.

  :284   const receiptTimers = new Map<string, ReturnType<typeof setTimeout>>()
  :799   setTimeout(() => { ... }, RECEIPT_MS)
         The one-shot arrival receipt: a node's medallion carries
         'is-receiving' for RECEIPT_MS so a CSS pulse can play when a token
         lands. REVEAL PACING, which the criterion allows in so many words.
         It sets a decoration class, not data-state.

The five states are derived in useRunChoreography from frame kind and stage
alone; frontend/tests/characterStates.spec.ts asserts each of them from
fixture frames with no timers involved.
