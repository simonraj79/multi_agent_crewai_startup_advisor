T2.5 - the character's state is driven by run events only, never by a clock.

REGENERATED BY RV3, sixth pass, branch run-shell/cast at a182ed6, 2026-09-05T19:42:31+08:00.

THIS IS THE PASS THAT MOST NEEDED RE-RUNNING. Round seven made the ELAPSED
figure 'a real ticking clock that freezes on stop' - a new setInterval in the
run shell. The whole point of T2.5 is that a clock must not reach a
character's state, so the question is WHERE that interval lives.

===========================================================================
1. THE CRITERION'S OWN GREP
===========================================================================

$ grep -rnE 'setTimeout|setInterval|Date\.now' \
      frontend/src/characters frontend/src/components/AgentCharacter.vue \
      frontend/src/composables/useRunChoreography.ts

frontend/src/composables/useRunChoreography.ts:259:  const now = options.now ?? (() => Date.now())
frontend/src/composables/useRunChoreography.ts:290:  const receiptTimers = new Map<string, ReturnType<typeof setTimeout>>()
frontend/src/composables/useRunChoreography.ts:826:      setTimeout(() => {

THREE hits, and they are the same three every one of the six passes has
found. NO setInterval appears here. The new clock is not in this grep.

===========================================================================
2. WHERE ROUND SEVEN'S CLOCK ACTUALLY IS
===========================================================================

$ grep -rnE 'setInterval|setTimeout' frontend/src/components/StatusPanel.vue \
      frontend/src/data/runStatusDisplay.ts frontend/src/views/StudioView.vue \
      frontend/src/components/DialogueRail.vue

frontend/src/components/StatusPanel.vue:211:let tickHandle: ReturnType<typeof setInterval> | null = null
frontend/src/components/StatusPanel.vue:234:    tickHandle = setInterval(() => {

It is in StatusPanel.vue, which is a PANEL OF NUMBERS in the control rail.
It drives the ELAPSED readout and nothing else: no character, no node state,
no data-state attribute. It is outside all three surfaces this criterion
names, and it is the right place for a clock - an elapsed time is the one
thing in this console that genuinely IS a function of wall time.

===========================================================================
3. THE HALF THAT MATTERS - the character system itself, with the two other
   ways a clock reaches code added to the pattern
===========================================================================

$ grep -rnE 'setTimeout|setInterval|Date\.now|performance\.now|requestAnimationFrame' \
      frontend/src/characters/ frontend/src/components/AgentCharacter.vue

(no output; exit status 1)

ZERO, for the sixth pass running, and now with a ticking clock in the same
application. pip.ts and AgentCharacter.vue cannot read one by any route.

===========================================================================
4. EACH OF THE THREE HITS, AND WHY IT IS NOT STATE
===========================================================================

  259:  const now = options.now ?? (() => Date.now())
  290:  const receiptTimers = new Map<string, ReturnType<typeof setTimeout>>()
  826:      setTimeout(() => {

  The first is an INJECTABLE clock with a real-clock default, used to
  timestamp a handoff so a token can be expired. Every test passes its own.
  It decides no character state.

  The other two are the one-shot arrival receipt: a node's medallion carries
  'is-receiving' for RECEIPT_MS so a CSS pulse can play when a token lands.
  REVEAL PACING, which the criterion allows in so many words. It sets a
  decoration class, not data-state.

The five states are derived from frame kind and stage alone;
frontend/tests/characterStates.spec.ts asserts each of them from fixture
frames with no timers involved, and it is green inside this pass's
npx vitest run (93 files / 2007 tests).
